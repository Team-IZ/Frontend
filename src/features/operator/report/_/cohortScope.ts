/**
 * 지금 보고 있는 기수 — 임시 상수. `projects/cohortScope.ts` · `admin/_/cohortScope.ts`와
 * 같은 이유(상단 기수 스위처가 아직 없다)로 세 번째 도메인이 이 값을 또 필요로 하게
 * 됐다 — 세 곳이 됐으니 다음에 손대는 사람이 `lib/`로 올린다(decision-log D14).
 */
export const COHORT_ID = '7'
