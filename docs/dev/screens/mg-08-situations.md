# MG-08 프로젝트 상세 — 상황 기록

실계정(이담 · 매니저 · 9기 C반)으로 `미니프로젝트 6차`
(`073c7e8b-dc69-5a8f-a658-5d872a45975f`)를 띄워 확인했다.

## 붙인 것

| 탭 | 오퍼레이션 | 확인 |
|---|---|---|
| 헤더 | `findProject` | ✅ 이름·상태·교안·검증 개념 3건·제출 마감 |
| 헤더 담당 반 | `findProjectClassProgress` | ✅ `C반 · 26명` |
| 팀 | `findTeams` + 생성·수정·배정·해제·확정·다시 열기·자동 배분 | ✅ 7팀 26명 |
| 제출 현황 | `findProjectSubmissionStatus` + `findProjectClassProgress` | ✅ 빈 상태(제출 잠김) |
| 결과 | `findProjectEvaluationSummary` | ✅ 응시 18 · 불합격 4 · 개념별 3행 |
| 개인 결과 | `findTraineeEvaluationDetail` | ✅ 3단/2단/1단 · 축 사다리 · 채점 근거 |

## 렌더에서 잡은 것

### 🔴 팀 목록이 뒤섞여 온다 — 화면이 정렬한다
`findTeams`가 `7 · 2 · 5 · 1 · 4 · 6 · 3팀` 순으로 줬다. 스펙에 순서 약속이 없다
(제출 현황만 「반 이름 → 팀 번호 순」이라고 적혀 있다). 표에 적힌 번호대로 못 읽는
목록이라 반 이름 → 팀 번호로 화면이 세운다(`TeamTab.tsx`). 정렬 파라미터가 없고
서버 판정을 뒤집는 것도 아니라 경계 문제는 아니다.

### ⚠ 제출은 잠겼는데 리포트는 발행 완료다
탭 배지가 `제출 현황 잠김` + `결과 발행 완료`로 동시에 떴다. `teamFormationStage`가
아직 확정 전(`전원 배정`)인데 `reportPublished`가 true다. **화면 버그가 아니라 서버
상태 그대로**이고 두 값의 출처도 다르다(`submission-status` vs `evaluations`).
회차가 재편성됐거나 시드 데이터의 사정일 수 있어 32차에서 물어본다.

### ⚠ 인원 0명인 팀이 있다
`7팀`이 0명인데 `unassignedCount`는 0이다 — 빈 팀을 만들어 두고 아무도 안 넣은
상태다. 팀 삭제 오퍼레이션이 없어(아래) 지울 방법이 화면에 없다.

## 목에서 뺀 것 — 서버에 자리가 없다

| 뺀 것 | 있던 자리 | 사정 |
|---|---|---|
| 팀 삭제 | 팀 탭 행 액션 | 생성·수정·확정·다시 열기·배정·해제·자동 배분만 있다 |
| 다시 보기 활성화·비활성화 | 결과 탭 하단 | `retrySentAt`·`retryDueAt`·`retryTakenAt` 대응 필드 없음 |
| 리포트 발행 | 결과 탭 헤더 | `reportPublished`는 읽기 전용 |
| 자동 배분 겹침 회피 | 자동 배분 모달 | `AutoAssignTeamsRequest`에 파라미터 없음, 응답에도 겹친 팀 없음 |
| 다른 팀에서 데려오기 | 팀 편집 모달 | 목의 ⑤ `SUBMITTING` 국면 자체가 서버에 없다 |
| 직전 회차 도달 단계 | 팀 편집 목록 | 팀·미배정 응답 둘 다 이름과 ID만 준다 |

전부 32차 요청서로 올린다.

## 목과 갈린 어휘

- **상태**: 목 `DONE` → 서버 `CLOSED`
- **팀 편성 국면**: 목 5국면(`BEFORE·FORMING·READY·LOCKED·SUBMITTING`) →
  서버 `NOT_STARTED·FORMING·READY_TO_CONFIRM·CONFIRMED·CLOSED`. 앞 넷은 1:1,
  다섯째만 다르다(제출 시작 여부는 `submissionOpened`로 따로 온다)
- **분석 상태**: 3종 → 5종. `PARTIAL`을 완료로 접지 않는다(스펙 🔴)
- **요구사항 판정**: 목 4종(`MET·UNUSED·EMPTY·NOMATCH`) → 서버 3종
  (`PENDING·PASS·FAIL`) + `evidence` + `judgedByAi`
- **채점 결과**: `resultStatus` 5종이 새로 생겼다. `AVAILABLE`이 아니면
  합격·불합격을 말하지 않는다(응시 중인 사람이 전부 불합격으로 잡힌다)
- **제출 마감**: `endDate`가 아니라 `submissionDueAt`. 둘은 서버에서 연결돼
  있지 않아 기간을 늘려도 마감은 안 움직인다(스펙 명시)
- **상세 응답 스키마가 목록과 다르다**: `ProjectDetailResponse`는 교안·개념을
  객체 배열로 주고 `requirementTitles`를 싣는다. 목록 타입으로 읽으면 안 된다

## 회차·반 파라미터

네 조회 모두 `roundNo`·`classId`가 선택이라 **안 보낸다** — 생략하면 서버가 현재
회차와 담당 반 전체를 고르고, 그것이 이 화면의 범위다. URL이 `projectId` 하나뿐이라
화면에 회차 선택기가 없다.

## 하드닝 — `screen-hardening.md` 절차 (2026-08-17)

### 조회는 병렬이다 — 실측

| 조회 | TTFB | 크기 |
|---|---|---|
| `/projects/{id}` | 2.99s | 1.5KB |
| `/projects/{id}/teams` | 2.48s | 4.8KB |
| `/projects/{id}/submissions` | 2.21s | 8.8KB |
| `/projects/{id}/class-progress` | 2.07s | 1.4KB |
| `/projects/{id}/evaluations` | 1.67s | 17.3KB |

다섯이 **`projectId`만 있으면 되므로 동시에 나간다.** 히트맵의 3단 직렬(12초)과
달리 첫 그림까지 ~3초다.

### 부분합을 세어 봤다 — 서버 숫자는 전부 맞는다

```
팀원 합계        26  = class-progress targetTraineeCount 26
제출한 팀의 인원  21  = class-progress submittedCount 21
제출 팀 수        5  = summary submittedTeamCount 5
개인 응시    DONE 18 · BLOCKED 5 · OPEN 2 · MISSED 1  = 26
```

### 🔴 화면이 제출 5건과 응시 18명을 숨기고 있었다

서버 응답이 **자기 자신과 어긋난다.**

```
submissionOpened   false   (teamFormationStage = READY_TO_CONFIRM · 7팀 전부 DRAFT)
submittedTeamCount 5 / 7   ← 이미 제출했다
analysis           5팀 SUCCEEDED
reportPublished    true    (발행 시각 2026-08-16T10:40:38Z)
```

스펙이 「화면은 `submissionOpened`만 보고 표를 그릴지 빈 상태를 보여줄지 정한다」고
해서 그대로 따랐더니, 화면이 **「아직 팀 편성 중이에요」**라고 말하며 매니저가 이 탭을
여는 이유인 값들을 통째로 숨겼다. 탭 배지도 「잠김」이었다.

**고침** — 그릴 것이 실제로 있으면 그린다. 판정 규칙을 새로 만드는 것이 아니라
(여전히 `submissionOpened`를 본다) 그 값이 **데이터와 어긋날 때만** 데이터를 택한다.
서버가 맞춰 주면 이 분기는 저절로 안 탄다. 32차로 올린다.

### 🔴 확정 경고가 사실이 아니었다

같은 상태에서 「확정하면 학생들이 코드를 제출할 수 있게 되고」 · 「제출이 시작되기
전까지는 되돌릴 수 있습니다」가 떠 있었다 — 둘 다 이미 지난 일이고, **「되돌려도
된다」로 읽혀 위험하다.** 제출이 있으면 다른 문구를 쓴다.

### 🔴 쓰기가 실패해도 화면이 아무 말도 안 했다

확정을 가로채 409를 만들었더니 **버튼만 원래대로 돌아가고 끝이었다.** 눌렀고,
실패했고, 사용자는 됐는지 안 됐는지 알 수 없다.

`팀 탭`(생성·확정·다시 열기) · `팀 편집`(배정·해제) · `자동 배분` 셋 다 같았다.
`lib/errorCopy` + `Alert variant="danger"`로 붙였다(OP 반 추가와 같은 패턴).

### 🔴 팀 편성 실패 8종이 한 문구로 뭉쳐 있었다

전부 「잠시 후 다시 시도해 주세요」가 나왔는데, **다시 눌러도 안 되는 것들이다.**

```
NO_TEAMS_TO_CONFIRM · TEAMS_NOT_READY · MANAGER_CLASSROOM_AMBIGUOUS
NO_MEMBERS_TO_ASSIGN · AUTO_ASSIGN_NOT_ALLOWED · TEAM_NOT_FOUND
PROJECT_MEMBERSHIP_NOT_FOUND · TEAM_MEMBERSHIP_NOT_FOUND
```

`lib/errorCopy`에 여덟 문구를 **추가**했다(공유 파일이라 수정이 아니라 추가만).
가로채기로 확인:

```
TEAMS_NOT_READY      아직 확정할 수 없습니다 — 미배정 인원이 남아 있거나 …
NO_TEAMS_TO_CONFIRM  확정할 팀이 없습니다 — 팀을 먼저 만들거나 …
500                  팀을 확정하지 못했습니다 — 잠시 후 다시 시도해 주세요
```

### 훑다가 다른 화면에서 나온 것 (MG-04 브리프)

`grep`으로 매니저 쓰기 전부를 훑었더니 **브리프 생성도 같은 구멍**이었다. 실패
표시가 목록에서 온 `stateHint`로만 되어 있어, **이 화면에서 누른 생성이 실패하면**
「아직 브리프가 없습니다」가 그대로 떠 있었다 — 눌렀는데 아무 일도 안 일어난 것처럼
보인다. `create.isError`를 함께 본다.

### 렌더 확인

```
제출 현황  팀 7행 + 개인 26행 · 응시 완료 18 · 미응시 1 · 응시 전/N시간 남음 2 · — 5
          반별 진행 줄  C반 제출 21/26 · 분석 21 · 응시 18
팀        전원 배정 · 7팀 · 미배정 0명 · 편집 7개
결과      26명 목록(막힘 N · 응시 중 · —) + 개념별 3행
```

## 미검증

- **되돌릴 수 없는 쓰기를 실제로 누르지 않았다.** 진행 중인 실제 회차이고,
  **팀 삭제 API가 없어** 팀 추가는 되돌릴 수 없다(0명짜리 `7팀`이 이미 그 상태로
  남아 있다). 확정·배정은 학생 제출과 채점에 영향이 가 실행하지 않았다.
  실패 경로는 위처럼 가로채기로 봤다 — **「전수 확인」이 아니라 「미측정」이다**
- **`FORMING`·`CONFIRMED`·`CLOSED` 국면** — 이 회차는 `READY_TO_CONFIRM` 하나다
- **요구사항 판정** — 이 프로젝트는 `requirements`가 0건이라 그 열이 늘 `—`다.
  펼침·`evidence`·`judgedByAi` 표시를 못 봤다
- **자동 배분** — 팀이 이미 있어 버튼이 안 나온다(`NOT_STARTED`에서만).
  `MANAGER_CLASSROOM_AMBIGUOUS`도 이 계정이 C반 하나뿐이라 못 태웠다
- **0명짜리 팀이 「미제출 ⚠」로 나온다** — 팀원이 없으면 낼 수가 없는데 조치가
  필요한 것처럼 보인다. 팀 삭제 API가 생기면 함께 정리한다

---

## 하드닝 2차 — 탭 셋을 화면 셋으로 (2026-08-17)

`screen-hardening.md` §「탭이 여섯이면 탭 하나를 화면 하나로 취급한다」대로 갈랐다.
**공통(머리·탭 줄·탭 전환)은 한 번만** 재고, 탭마다 축 → 실측 → 판정 → 고침을 돌았다.

```
공통       머리 56 + mb 16 · 탭 줄 36.6 + mb 16 · 탭 전환
팀 탭      컨트롤 4 + 행마다 편집·해체 · 국면 5종
제출 탭    컨트롤 0 (읽기 전용 표 32행)
결과 탭    마스터-디테일 27(종합 1 + 사람 26)
```

## 고친 것 (하드닝 2차)

### 1. 🔴 종료된 회차에서 **3.8초 동안 [팀 추가]·[자동 배분]이 열려 있었다**

제출 현황 조회를 6초 늦춰 재현했다.

```
6.2초 ── 10.0초   「편성 전」 6팀 · 미배정 0명   [팀 추가] [자동 배분] [편집]×6
10.0초             「종료됨」 6팀               [편집]×6(비활성)
```

원인은 두 줄이었다.

```tsx
stage ?? 'NOT_STARTED'      // 모르는데 「편성 전」이라고 단정한다
locked ?? false             // 모르는데 「안 잠겼다」고 단정한다
```

**자동 배분은 팀을 다시 짜는 되돌릴 수 없는 쓰기다.** 서버가 막더라도 화면이 권해서는
안 된다(규칙 C·F). `?? []` 금지와 같은 뿌리다 — 「모른다」를 값으로 메우면 화면이 거짓을
말한다(규칙 E).

고침 — `undefined`를 그대로 받고 **국면과 잠김을 둘 다 알 때만** 액션 줄을 그린다.
표는 그려도 된다(읽기라 틀릴 것이 없다). 국면 배지 자리는 스켈레톤으로 잡는다.

```
후   6.4초 ── 10.2초   액션 [] · 배지 자리만        → 「종료됨」
```

**회차 6개 전수로 회귀도 봤다** — 게이트가 정상 회차를 막지 않는다.

| 회차 | 국면 | 액션 | 행 버튼 |
|---|---|---|---|
| 1~5차 | 종료됨 | 없음 | 편집·해체 **비활성**(폭 유지) |
| 6차 | 전원 배정 | 팀 추가 · 팀 편성 완료 + 확정 경고 | 활성 |

### 2. 🔴 탭 이름 옆 숫자를 걷어냈다 (규칙 E)

`팀 6팀` · `제출 6/6` · `발행 완료`가 붙어 있었다. 값이 도착하면 탭 폭이 이만큼 는다.

```
팀        32 → 40px   (+8)
제출 현황 69 → 103px  (+34)
결과      43 → 81px   (+38)
                        ────
                      총 80px
```

**눌러야 할 것이 옆으로 미끄러진다.** 그 수는 탭을 열면 머리가 크게 말한다 — 셋 다
이미 그렇게 하고 있어 뺄 수 있었다.

```
팀        「편성 전 · 6팀 · 미배정 0명」
제출 현황 「제출 6/6팀 · 분석 실패 1팀」
결과      「프로젝트 종합 · 발행 완료」
```

### 3. 🔴 탭이 주소에 없었다 (규칙 J)

`defaultValue`만 있어 새로고침·뒤로가기가 탭을 잃었고, 다른 화면이 「이 회차의 결과 탭」을
가리킬 수도 없었다. `?tab=`으로 옮겼다.

```
결과 누름      ?tab=result       ✅
제출 현황      ?tab=submission   ✅
팀             ?tab=team         ✅
?tab=zzz       → 기본 탭(종료된 회차라 결과)  ✅  남이 보낸 링크가 낡았을 수 있다
```

`replace`로 바꾼다 — 탭을 다섯 번 누르고 뒤로가기를 다섯 번 하게 만들지 않는다.

### 4. 스피너 다섯 자리 → 스켈레톤

진입 · 팀 · 제출 · 결과 · 개인 패널. 전부 이 화면에서 잰 값을 박았다.

```
팀 탭     표 헤더 38.5 · 행 53 · 6행
제출 탭   머리 84 · 표 헤더 38.5 · 행 42.7 · 32행
결과 탭   틀 578 · 왼쪽 basis-64 · 항목 36
```

진입 스켈레톤은 처음에 **머리와 탭 줄만**(109px) 그렸더니 회차가 도착할 때 크게 튀었다.
표 여덟 줄을 얹어 「표가 온다」까지 말하게 했다.

```
          전(109px 진입)      후(539px 진입)
팀 탭     +409px              +12px
결과 탭   +594px              +164px
제출 탭   +1475px             +1045px   ← 32행짜리 표라 여기까지가 한계
```

**개인 패널은 오른쪽 칸만 채운다** — 스피너를 쓰면 사람을 바꿀 때마다 오른쪽이 통째로
사라졌다 생겨서, 마스터-디테일에서 방금 누른 사람이 어디였는지 눈이 놓친다.

### 5. 결과 목록에 `aria-current`

선택 표시가 **왼쪽 2px 선과 글자색뿐**이었다 — 27개 중 어느 것이 열려 있는지 눈으로만
보인다.

## 결과 탭 27/27 전수

```
27조합 · 진짜 문제 0 · 사람당 요청 1건
```

「오른쪽이 거의 비었다」가 6건 나왔는데 **전부 정상**이다 — 응시 중·중단·무효는 결과가
없고, 화면이 왜 없는지를 정확히 말한다.

```
문유진 · 백서준 · 유재윤 · 최윤슬   「아직 응시 중이라 결과가 없습니다.」
송지안                              「끝내지 못한 채 응시 창이 닫혔습니다.」
심예린                              「무효 확정된 수행이라 결과를 세지 않습니다.」
```

내 임계값(40자)이 낮았을 뿐이다. **숫자가 아니라 문장을 읽어야 판정할 수 있는 자리**다.

### 캐시 — 30초를 넘겨서 재면 「캐시가 안 먹는다」로 읽힌다

27명을 다 돌고 나서 앞사람을 다시 누르니 요청이 또 나갔다. 전역 `staleTime`이 30초라
그 사이에 지난 것이었다. **30초 안에서 다시 재니 요청 0 · 즉시 표시**다.

## 미검증 (하드닝 2차)

```
쓰기      자동 배분 · 팀 편성 완료 · 편성 다시 열기 · 해체 — 되돌릴 수 없어 실서버에서
          누르지 않았다(확인 모달과 실패 문구는 1차·32차에서 확인)
데이터    담당 반이 여럿인 매니저(자동 배분 400 MANAGER_CLASSROOM_AMBIGUOUS 경로)
접근성    탭 키보드 이동 · 결과 목록 27개의 포커스 순서
```

---

## 하드닝 2차 자체 감사 — `screen-hardening.md`와 한 줄씩 대조 (2026-08-17)

사용자 지시로 **문서를 다시 정독하고 이번 라운드(MG-04~08)가 실제로 따랐는지** 항목별로
대조했다. **다섯 개를 안 지켰다.**

### ✅ 지킨 것

```
컨트롤을 먼저 센다        MG-05 192 · MG-06 320 · MG-08 탭별
선택지는 열어서 DOM에서   코드에서 안 베꼈다(base-ui는 data-open으로 읽어야 한다)
개수는 더해서 검산        MG-06 6+4+6+2 = 18 = 전체
요청 수를 같이 본다       조합당 1건 · 재선택 0건 · 읽기에 쓰기 0건
가로채기가 먹었는지       500 주입 후 실패 화면이 실제로 뜨는 것 확인
부분합으로 한 번 더       MG-08 「빈 패널 6건」이 전부 정상(응시 중·중단·무효)이었다
빈 결과는 실패다          MG-05 첫 스윕 「축 6/8/12/16」을 결과로 안 적고 원인을 찾았다
발견의 주인을 가른다      MG-05·06·08 발견이 전부 ① 우리 코드였다 — 요청서로 안 올렸다
화면 규칙 A~L 대조        E(?? [] · 탭 숫자 · isPending) · J(URL 탭) · C·F(액션 게이트)
CI 가드 6종               전부 통과
같은 것이 다른 화면에     grep — staleProps 0 · 탭 숫자 0 · isPending은 MG-09만 남음
```

### 🔴 안 지킨 것 다섯

| | 문서가 뭐라 하나 | 내가 한 것 |
|---|---|---|
| **1. 한글 조합** | 「`type('미니')`로는 안 나온다 — CDP `imeSetComposition`을 쓴다」 | `pressSequentially`로 재고 **「자모 7타」라고 적었다**(거짓). MG-05 문서에서 정정하고 미검증으로 되돌렸다 |
| **2. 대비 계산** ✅ | 「색으로 순위·상태를 말하면 **계산해서** 잰다. 눈으로 판단하지 않는다」 | **MG-09 라운드에서 갚았다** — 캔버스 픽셀로 재서 `Badge neutral`의 3.93:1을 찾았고(공용), `check:design`의 빠진 짝까지 메웠다. MG-05·08도 다시 재서 미달 0건 확인 |
| **3. 미검증이 둘을 넘으면** ✅ | 「남기는 것으로 끝내지 말고, **그 자체를 하나의 발견으로** 다룬다」 | 접근성 미검증이 세 번 반복됐고, 네 번째(MG-09)에 뚫었다. **뚫어 보니 화면이 아니라 공용 토큰·가드 문제**였다 — 미룬 만큼 8개 파일이 그 상태로 있었다 |
| **4. 다이얼로그** | 「열고 닫기만 하면 안 본 것이다. **되돌릴 수 없는 것도 409·400을 가로채면** 화면이 뭐라 하는지 볼 수 있다」 | MG-08의 `TeamEditDialog`·`TeamAutoAssignDialog`를 **한 번도 안 열었다.** 「되돌릴 수 없어 안 눌렀다」로 미검증에 적었는데, 문서는 **실패 경로는 볼 수 있다**고 명시한다 |
| **5. 주소로 가지 말고 눌러서** | 「`goto`는 딥링크·새로고침 축을 볼 때만. 나머지는 사이드바부터 클릭」 | MG-05·06은 행을 눌러 갔는데 **MG-08은 대부분 `goto`였다.** OP-04의 크래시가 그 차이에서 나왔다 |

### 왜 이렇게 됐나

**1·2·4는 전부 「측정이 번거로운 것」이다.** 조합 전수처럼 반복문으로 도는 것은 다 했고,
CDP·캔버스·다이얼로그처럼 **한 번씩 손이 더 가는 것**만 빠졌다. 문서가 굳이 그 셋을
콕 집어 적어 둔 이유가 이것으로 보인다.

**3은 그 결과다** — 같은 미검증이 세 번 반복되는데도 「다음에」로 미뤘다. 문서의
「미검증이 둘을 넘으면 그 원인부터 뚫는다」가 정확히 이 상황을 막으려는 규칙인데,
그 규칙 자체를 안 봤다.

### 다음 라운드에 **먼저** 할 것

```
① 대비 계산    ✅ MG-09 라운드에서 완료 — 공용 Badge 미달 1건 + 가드 구멍을 찾아 고쳤다
② IME 조합     CDP imeSetComposition — MG-05 검색 · MG-03 검색 · MG-04 서술 입력
③ 다이얼로그   TeamEdit·TeamAutoAssign을 열고, 409·400을 가로채 실패 문구를 본다
④ 눌러서 간다  MG-08을 목록 → 상세 → 탭 경로로 다시 (캐시가 있는 경로)
```

> **도구가 끊겨도 브라우저는 띄울 수 있다** — MCP 플러그인이 사라졌을 때 「렌더 확인
> 불가」로 적었는데, `playwright-core`(npx 캐시)와 chromium(ms-playwright 캐시)이 로컬에
> 다 있었다. `executablePath`로 붙이면 그대로 돈다. ②③④가 아직 남은 것은 **도구 탓이
> 아니다.**

**이 네 줄을 여기 남기는 이유** — 미검증 목록에 섞어 두면 또 뒤로 밀린다. 문서가
「「테스트 못 했다」를 적는 것이 안 적는 것보다 낫다」고 한 것보다 한 발 더 나가,
**왜 못 했는지와 언제 할지**까지 적어 둔다.

---

## 하드닝 3차 — 팀 탭만 (2026-08-25)

> 범위를 **팀 탭 하나로** 좁혔다(사용자 지시). 그사이 33차(반 스코프)로 팀 탭이
> 통째로 다시 짜였고, 팀 해체 UNIQUE 마이그레이션도 이날 적용됐다 — 2차가 끝난
> 시점과 화면이 달라져 있었다. 2차 자체 감사에서 "다음 라운드에 먼저 할 것"으로
> 남긴 ③ 다이얼로그(409·400 가로채기) · ④ 눌러서 간다를 이번에 갚는다.
>
> 측정 — 2026-08-25 · Lambda(그린컴퍼니 dev) · `minseo@naver.com`(매니저 · 7기
> 담당 E반·G반) · 브랜치 `develop`

### ③ 갚음 — 다이얼로그를 열고 409·400을 가로챘다

`TeamEditDialog`(배정·해제)와 해체 확인 `AlertDialog` 셋 다 실제로 열고, 라우트를
가로채 실패를 만들었다.

| 다이얼로그 | 가로챈 실패 | 화면이 한 말 |
|---|---|---|
| 팀 편집 · 해제 | 404 `TEAM_MEMBERSHIP_NOT_FOUND` | 「이미 그 팀에서 빠져 있습니다 · 다른 곳에서 먼저 처리됐습니다」 |
| 팀 편집 · 배정 | 404 `PROJECT_MEMBERSHIP_NOT_FOUND` | 「그 교육생을 이 프로젝트에서 찾을 수 없습니다 · 명단에서 빠졌을 수 있습니다」 |
| 팀 해체 확인 | 409 `TEAM_SUBMISSION_LOCKED` | 아래 참고 — 처음엔 **아무 말도 안 했다** |

둘(배정·해제)은 정상이었다 — 체크가 풀리고 실패 배너가 다이얼로그 안에 뜬다.
**해체가 새로 찾은 버그였다.**

### 🔴 해체 확인 모달이 실패해도 이유를 안 보여줬다

`useDisbandTeam`은 탭 레벨 훅인데 버튼은 `AlertDialog` 안에 있다. 실패해도 모달을
안 닫는 것까지는 맞았다(규칙 H) — 그런데 실패 사유(`errorCopy`)는 **탭 본문의
공용 배너**에만 그려졌고, 그 배너는 모달 오버레이 **뒤에 깔려 안 보였다.**

```
스크린샷 확인 — 모달 밑단에 빨간 배너 끄트머리 몇 픽셀만 삐져나옴
사용자가 보는 것 — "G반 1팀을 해체할까요?" + [취소] [해체](다시 눌러도 되는 상태)
                  왜 막혔는지는 어디에도 없다
```

`TeamEditDialog`·`TeamAutoAssignDialog`는 이미 실패를 **자기 다이얼로그 안에**
그리는데 해체만 빠져 있었다 — 해체 API가 2차 이후에 생긴 기능이라 그때는 아직
없었다(1차 문서: "팀 삭제 API가 없어 화면에 지울 방법이 없다").

**고침** — `AlertDialogContent` 안, 푸터 위에 같은 `errorCopy` 배너를 추가했다.
가로채기로 재확인: `TEAM_SUBMISSION_LOCKED`를 다시 흘려보내니 모달 안에 「제출이
있는 팀은 해체할 수 없습니다」가 바로 보인다. 취소를 누르면 탭 본문 배너에도
같은 문구가 남는다(confirm·reopen·create와 같은 자리 — 다음 액션 전까지 마지막
실패를 붙들고 있는 기존 패턴 그대로).

`src/features/manager/projects/components/TeamTab.tsx` — `AlertDialogContent`에
`disbandTeam.error` 전용 `Alert` 추가. `npx tsc --noEmit` 통과.

### ④ 갚음 — 눌러서 갔다

`goto`로 상세에 바로 들어가지 않고 `/manager/projects` 목록 → 행 링크 클릭 →
탭 클릭 순서로 다녔다. `?tab=`은 실제로 탭을 눌러야만 주소에 실린다(진입 시
기본값은 URL에 안 쓴다 — `defaultTab(status)`로 계산만 하고 `replace`하지 않는다.
`?class=`도 같은 결 — 반이 하나 이상일 때 세그먼트를 눌러야 실린다). 둘 다 의도한
동작이라 버그 아님, 다만 "URL이 스코프를 갖는다"는 규칙 J와 헷갈리기 쉬워 여기
적어 둔다 — **진입 즉시 URL에 없다고 스코프가 없는 게 아니다, 아직 기본값을 쓰고
있을 뿐이다.**

### IME 조합 — 팀 탭엔 해당 입력이 없다

2차가 남긴 "② IME 조합"은 "Enter로 확정하는 입력"이 대상이다. 팀 이름 입력
(`NewTeamPrompt`)을 읽어 보니 **Enter 핸들러 자체가 없다** — `추가` 버튼을 눌러야만
제출된다. 조합 중 Enter를 무시할 필요도, `imeSetComposition`으로 잴 것도 없다 —
막을 것이 애초에 없다. 팀 탭에 검색·필터 입력은 없으므로 이 화면에서는 여기서
끝난다(다른 화면의 검색 입력은 2차가 남긴 대로 별도로 갚는다).

### 새로 확인한 것 — 33차 반 스코프 리라이트 실사용

- E반 자동 배분(25명 → 5팀) · G반 팀 이름 재사용(`7팀`) 둘 다 **UNIQUE 마이그레이션
  적용 후** 409 없이 성공(47차 요청 → 백엔드 회신으로 확인)
- 확정 ↔ 편성 다시 열기 왕복 시 배지·버튼이 새로고침 없이 즉시 갱신됨
  (`submissionKeys` 무효화 수정 — PR #350)
- `?class=` 전환 시 이전 반의 실패 배너가 안 남음(`useEffect`가 4개 뮤테이션을
  reset — 기존 코드, 이번에 회귀 없음을 재확인)

### 🔴 반을 바꾸면 표가 통째로 비었다 — StaleBlock이 없었다

사용자 지적으로 다시 봤다: "목록 같은 경우는 다른 곳들처럼 좀 막아둬야 하는 거
아냐?" `?classId=`가 쿼리 키에 실려 반을 바꿀 때마다 **새 키**가 되고, 새 키는
캐시가 없어 `teamList.data`가 `undefined`로 떨어진다. 이 화면은 그 상태를 "아직
아무것도 안 왔다"로 읽어 **방금 보던 반의 표까지 스켈레톤 6행으로 지웠다** —
`lib/listQuery.ts`가 명시한 "같은 것의 다른 조각(필터·페이지)은 유지한다" 규칙을
반 전환에는 안 걸어 둔 것이었다. 필터·검색·정렬에는 이미 있는 `StaleBlock` 패턴이
이 화면에는 아예 안 들어와 있었다(원인이 필터가 아니라 **세그먼트 버튼**이라
처음 두 차례 하드닝에서 안 걸렸다).

가로채기로 재현 — `teams?classId=` 응답을 4초 늦추고 반을 전환:

```
전(고침 전)   클릭 즉시 표·배지·버튼·경고까지 전부 사라지고 스켈레톤 6행
후(고침 후)   클릭 즉시 이전 반의 표가 40% 흐림 + "반을 불러오는 중" 알약
              — 4초 뒤 새 반의 값으로 자연스럽게 갈아 끼움
```

**고침**

1. `_/api/api.ts` — `useTeams`·`useSubmissionStatus`에 `placeholderData`를 달았다.
   **`classId`만 바뀌면 옛 값을 유지**하고(같은 프로젝트의 다른 조각), **`projectId`가
   바뀌면 버린다**(다른 프로젝트를 이 프로젝트 것처럼 보이면 사고다 — `listQuery.ts`가
   가른 경계 그대로). 쿼리 키의 `path.projectId`를 직접 비교해 판정한다.
2. `TeamTab.tsx` — 배지·액션 줄·경고·표를 `StaleBlock`으로 감쌌다(반 세그먼트
   버튼은 밖에 둔다 — 다른 화면의 툴바가 `StaleBlock` 밖에 있는 것과 같은 자리,
   전환 자체를 계속 누를 수 있어야 한다). `stale`은 `teamList.isPlaceholderData ||
   submission.isPlaceholderData` 둘 중 하나라도 옛 값이면 켠다.

`npx tsc --noEmit` 통과 · 실측 스크린샷으로 재확인(이전 반 표 흐림 + 알약 노출).

### 미검증 (3차)

```
접근성   팀 탭 컨트롤(반 세그먼트·팀 추가·해체)의 키보드 이동·포커스 순서 — 2차부터
        이어지는 항목, 아직 안 뚫음
자동 배분  skillBalanced=true(실력 섞기) 실제 배분 결과 — 이번엔 무작위만 실행
반 3개 이상  담당 반이 셋 이상인 매니저 계정이 없어 세그먼트 줄바꿈·가로 스크롤을
            못 봄
```
