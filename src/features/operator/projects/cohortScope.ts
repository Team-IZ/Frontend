/**
 * 지금 보고 있는 기수 — **화면이 아니라 여기가 갖는다.**
 *
 * 목록·상세 두 파일이 각각 `const COHORT_ID = '7'`을 들고 있었다. 기수 스위처가 붙는
 * 순간 **두 곳이 서로 다른 기수를 볼 수 있는** 상태였고, 그때 화면은 아무 경고도 안 낸다.
 *
 * ⚠ **임시다.** 실제로는 **상단 기수 스위처가 정하는 앱 상태**여야 한다(점검표 H5 —
 * 컨텍스트 스위처는 상단 고정). 그 스위처가 아직 없어서 상수로 둔다 — 붙으면
 * **이 파일만** 스토어 조회로 바뀌고 화면은 안 바뀐다.
 *
 * `features/operator/admin/_/cohortScope.ts`가 같은 것을 갖고 있다. **세 번째 도메인이
 * 필요해지면 그때 올린다**(decision-log D14) — 지금 합치면 남의 도메인 파일을 건드린다.
 */
export const COHORT_ID = '7'
