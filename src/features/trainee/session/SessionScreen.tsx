import PlaceholderScreen from '@/app/PlaceholderScreen'

/** TR-03 검증 세션 — 전체화면 모드 — 셸 없음 — 화면 이식 전 자리표시. 실제 화면이 들어오면 이 파일 내용만 바뀐다. */
export default function SessionScreen() {
  return <PlaceholderScreen code="TR-03" title="검증 세션" />
}
