/**
 * 우수 교육생 표 페이지당 행 수.
 *
 * ⚠ 기획에 없다 — 문서에 페이저를 두는 것은 화면 사정이지 기획 항목이 아니다
 * (OP-05-report.md §11). PDF 본문에는 페이지네이션 없이 전부 들어간다.
 *
 * 20으로 두면 1920×1080에서 `.content`가 넘쳐 스크롤과 페이징을 같이 써야 했다
 * (02-layout-system.md §6 — 이 화면이 실제로 어긴 사례로 기록됨). 10이면 여유
 * 있게 한 뷰포트에 들어간다(13도 들어갔지만 사용자 요청으로 더 낮춤).
 */
export const TOP_STUDENTS_PAGE_SIZE = 10
