/*
  채점 루브릭 — `docs/plan/v2/14-verification-design.md` 부록(1190~1240줄)이 정본이다.
  SOLO 분류(Prestructural~Extended abstract)를 축마다 그 축의 말로 옮긴 것이라
  **축이 다르면 같은 점수도 다른 문장**이다.

  이 문장들이 시연자가 누르는 버튼의 라벨이다. "3점"이라고만 적혀 있으면 시연자가
  무슨 답변을 흉내 내는지 모른 채 누르게 되고, 그러면 화면이 왜 그렇게 갈리는지
  설명할 수 없다.

  ⚠️ `fixture.ts`의 녹화 데이터에도 `matchedLevel` 문장이 들어 있는데 **문구가 다르다**
  (예: L1 2점이 데이터에선 "코드의 표면적 동작만 반복 진술한다"). 뜻은 같지만 여기가
  SSOT다 — 문서 쪽은 24칸이 다 차 있고 데이터엔 실제로 나온 10칸뿐이다.
*/

/** 도달 경계. 3점 이상이면 그 축을 통과한다 */
export const PASS_SCORE = 3

export type AxisCode = 'L1' | 'L2' | 'L3' | 'L4'
export type Score = 0 | 1 | 2 | 3 | 4 | 5

export const SCORES: readonly Score[] = [0, 1, 2, 3, 4, 5]

/** 리포트에만 나온다. 응시 중에는 축 이름 자체가 힌트라서 감춘다(QuestionThread 주석) */
export const AXIS_NAME: Record<AxisCode, string> = {
  L1: '코드이해',
  L2: '설계논리',
  L3: '대안비교',
  L4: '반례대응',
}

export const RUBRIC: Record<AxisCode, Record<Score, string>> = {
  L1: {
    0: '답이 질문과 무관하거나 「그냥 이렇게 했습니다」 수준',
    1: '구성 요소를 하나만 언급. 나머지는 모름',
    2: '여러 요소를 나열하나 연결 관계를 설명 못 함',
    3: '요소들이 어떻게 이어지는지 설명 (일부 불명확해도)',
    4: '구성 요소 간 관계와 데이터 흐름을 정확히 설명',
    5: '그 구조가 왜 그렇게 될 수밖에 없는지까지 (프레임워크 제약·패턴)',
  },
  L2: {
    0: '왜 그렇게 했는지 설명 자체를 못함',
    1: '「필요해서」 수준. 이유가 하나뿐이고 구체성 없음',
    2: '고려사항을 나열하나 선택과 연결되지 않음',
    3: '의도와 제약 하나를 연결',
    4: '의도와 여러 제약을 연결. 트레이드오프를 인식',
    5: '다른 상황이었다면 다르게 했을 것임을 조건부로 설명',
  },
  L3: {
    0: '대안이라는 개념 자체를 이해하지 못함',
    1: '「다른 방법이 있을 것 같다」 수준. 구체성 없음',
    2: '대안 이름은 대나 어떻게 다른지 설명 못 함',
    3: '대안 하나를 구체적으로 설명',
    4: '대안과 현재 방식을 비교. 각각의 장단',
    5: '어떤 상황에서 무엇을 골라야 하는지 조건화',
  },
  L4: {
    0: '반례를 던져도 이해 불가',
    1: '반례를 인지하나 왜 문제인지 설명 못 함',
    2: '왜 문제인지 설명하나 어떤 경우에 터지는지 모름',
    3: '언제 문제가 되는지 조건을 특정',
    4: '조건 특정 + 대응 방향 제시',
    5: '던지기 전에 스스로 한계를 언급',
  },
}

/*
  도달 단계 0~4의 문구는 **여기 두지 않는다.** 리포트 화면의 `labels.ts`가 이미
  갖고 있고 `ConceptCard`가 그것을 읽는다 — 여기에 또 쓰면 배지 문구가 두 벌이 되어
  실제 화면과 시연 화면이 다른 말을 하게 된다.
*/
