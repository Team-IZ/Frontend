/**
 * 지금 보고 있는 기수.
 *
 * 상단 스위처가 아직 하나뿐이라 상수다. 실제 세션이 붙으면 헤더 스코프(또는 기수
 * 스토어)에서 받는다 — **그때 이 한 줄만 바뀐다.** 화면 다섯 곳에 `'7'`을 흩어 두면
 * 스위처가 생길 때 다섯 곳을 고쳐야 한다.
 *
 * OP-03(`ProjectListScreen`)도 같은 방식으로 상수를 들고 있다. 스위처가 실제로 생길 때
 * 둘이 같은 곳(스토어)을 보게 합친다 — 지금 스토어를 만들면 값 하나짜리 스토어가 된다.
 */
export const COHORT_ID = '7'
