# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
# Claude Code 개인 설정 (팀 공유는 .claude/settings.json)
.claude/settings.local.json

# 도구가 만드는 로컬 캐시
.impeccable/

# 스크린샷·브라우저 도구 산출물 (루트 오염 방지)
/*.png
.playwright-mcp/

# 세션 간 인수인계 메모 (로컬 전용, 공유 안 함)
docs/dev/handoff.md

# 개인 포트폴리오용 메모 (비공개, 공유 안 함)
docs/dev/portfolio-notes.md
docs/dev/portfolio-security-notes.md

# dev 전용 테스트 계정 — 자격 증명이 들어 있다(scripts/dev-accounts.mjs가 만든다)
# .env는 같은 내용을 Vercel 환경변수에 붙여 넣을 한 줄이다
dev-accounts.json
dev-accounts.env
