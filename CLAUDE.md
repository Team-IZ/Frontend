# IZ-frontend 작업 규약

AI(Claude Code·Cursor 등)가 이 레포에서 작업할 때 따르는 규칙. 개인 설정이 아니라 **팀 자산** — 클론하면 따라온다. 근거·상세: `docs/handbook/2026-07-21-ai-세팅-정리.md`, `docs/handbook/2026-07-13-작업-방법론-총합.md`.

---

## 0. 스킬·플러그인

- 상시 발동 지침은 **ponytail(full) 하나**. 나머지 스킬은 **명시 호출 시에만** 쓴다 — 응답마다 스킬을 강제로 훑지 않는다.
- 실제로 값어치를 하는 명시 호출: `brainstorming`(새 화면/기능 설계 전), `systematic-debugging`(버그), `writing-plans`(구현 착수).
- 이 CLAUDE.md가 스킬·플러그인 지침보다 우선한다. 충돌 시 이 문서를 따른다.

## 1. 작업 단계 규율 (구 session-discipline 훅)

1. 매 응답 첫 줄에 현재 단계(도메인 규범가 / 데이터 아키텍트 / 정보 설계자 / 인터랙션 설계자 / 비주얼 시스템 / 엔지니어 중 하나)와 남은 게이트 조건을 적는다.
2. 지금 단계 밖의 말이 나오면 '지금까지 한 작업이 무효가 되는가?'로 판정 — 무효화되면(또는 모르겠으면) **STOP**(즉시 멈추고 알림, 앞 단계로 되돌아감), 아니면 **PARK**(`docs/plan/screen/definition/_parking-lot.md`에 한 줄만 적고 논의 없이 계속 진행).
3. '고쳤습니다'/'검토했습니다'라고 말할 때는 diff·건수·검사 명령 실행 결과 같은 근거를 함께 제시한다 — 특히 틀리면 가장 크게 다치는 값 1건을 검산. **검증 없는 완료 선언 금지.**
4. 이 방법론이 만드는 코드보다 무거워지면 문서를 멈추고 만든다 — 안 만든 것은 감사할 수도 없다.

## 2. 대화 방식

- **제안하지 폴링하지 않는다.** "전문가 입장에서 어떻게?", "이게 나을까 저게 나을까" 류 위임 질문엔 선택지를 도로 던지지 말고 그 관점의 전문가로서 **결론 하나를 근거와 함께 제안**한다. 반대하면 되돌린다. 진짜 사용자만 아는 사실이 필요하면 그 사실만 짚어 조건으로 단다.
- **반영부터, 질문은 그다음.** 화면 산출물(와이어프레임·UI)은 "이렇게 할까요?"로 승인받고 반영하지 말고, **먼저 반영(파일 교체·렌더)한 뒤 "이거 괜찮냐"** 물어라. 사용자는 마크업이 아니라 렌더된 화면으로만 검토한다. 반영 → 헤드리스 렌더 스크린샷 → 확인. 단 되돌리기 비싼 것(대량 삭제·외부 발행·git 커밋)은 예외로 먼저 확인.
- **결정은 로그에 남긴다.** 모든 설계 결정과 문답("왜 이렇게 했냐", "전문가로서 생각해봐" 류)은 `docs/plan/screen/definition/01-decision-log.md`에 즉시 append — 배경 / 선택지 / 판단 / 근거(FR·명세·방법론) / 목적·효과. 팀 공유용이라 대화에만 두면 소실된다.

## 3. 커밋 · PR

`git commit`을 **절대 먼저 하지 않는다.** 스킬 절차(예: brainstorming의 "설계 문서 커밋" 단계)가 커밋하라고 해도 파일만 써두고 사용자가 명시적으로 요청할 때만 커밋한다.

형식·금지 사항은 `.githooks/`가 강제하므로 여기 다시 적지 않는다(규약: `docs/handbook/git-convention.md`). 훅이 못 잡는 것만:

- **커밋 메시지에 `Co-Authored-By`·`Generated with` 류 AI 서명을 절대 붙이지 않는다.**
- 성격이 다른 변경(설정 / 문서 / 기능)은 **커밋을 나눈다** — 한 덩어리로 뭉치지 않는다.
- **PR** 제목 `[feat] add login page UI`, 본문에 작업 요약 + 리뷰 포인트 + `closes #번호`. 1 PR = 1 기능, 파일 10개 이내.
- 이슈 번호는 브랜치에 등록돼 있으면 훅이 자동으로 붙인다. 없으면 `gh issue list`로 확인 후 사용자에게 확인받는다.

## 4. 산출물 형식

- **레퍼런스/설명 문서는 plain `.md`.** ERD 가이드·스키마 설명 같은 건 화려한 HTML 아티팩트로 만들지 말고 `docs/`에 마크다운으로. 표는 코드펜스(```) 말고 실제 GFM 파이프 표(`| a | b |`)로 — 코드펜스는 어느 뷰어에서도 표로 안 그려진다. 아티팩트는 시각적/인터랙티브 필요가 명시될 때만.
- **액션엔 결과를 함께 그린다.** 버튼·링크·액션(회차 추가·수정·응시 보기·생성 등)이 있으면 눌렀을 때 뜨는 것(입력 폼·슬라이드오버·모달·화면 전이·결과 상태)을 처음부터 같이 그린다. "이거 누르면 어떻게 돼?"를 매번 묻게 하지 말 것.

## 5. 화면 레이아웃 표준

### 스크롤 최소화 — 세로 스택 금지
모든 매니저 화면은 program-admin(운영 관리)·project-workspace(프로젝트) 형식을 따른다: **한 뷰포트에**, 세로 스크롤 최소화. 블록 5~6개를 세로로 쌓는 것(AI요약→분포→추이→취약→개입) 금지. 택1:
- **마스터-디테일**: 좌 리스트/목차 + 우 디테일. 좌 클릭 → 우측에 그 하나만(앵커 스크롤 ❌, 섹션 전환 ⭕). 리포트 목차도 이 형태.
- **서브탭**: 우측이 길면 섹션을 서브탭으로 나눠 한 번에 하나.
- 탭 vs 페이지: 한 컨텍스트의 여러 측면이면 탭, 대등한 별개 화면이면 사이드바 하위메뉴=페이지 분리.

### 리스트/표 4단 배치 (전역 공통)
1. **헤더:** `제목 + 총 개수(좌)` ‥‥ `[+ 추가] 주액션(우)`. `[+ 추가]`는 필터 라인이 아니라 **제목 줄 우측**. 총 개수 = 제목 옆 독립 요소(`교육생 48명`). 단 상단 메트릭 카드(총 N)가 있으면 제목 총개수 생략.
2. **툴바:** `[🔍 검색] [필터▾] [필터▾] [정렬▾]` — **전부 좌측**, 우측 비움. 행 선택해도 이 줄은 유지.
3. **표:** 사람 표는 정체성 스파인(아바타+이름/이메일). 정렬 헤더 `↕`.
4. **푸터:** `범위 개수 "1–20 / 48명"(좌)` ‥‥ `페이지네이션(중앙, 32px)` ‥‥ 우 비움.
5. **벌크 액션:** 선택 ≥1이면 콘텐츠 **하단 도킹 선택 바** 슬라이드(top-swap 금지). 중립 표면+accent 주액션. 필터 툴바 유지. 페이지 넘어 전체 선택 지원.

우상단은 주액션 하나만. 총 개수=위, 범위 개수=아래(페이저 옆). **목업(앱 UI) 안엔 개발용 주석 금지** — gatenote·설명글은 목업 밖 스펙 영역에만.

상세 근거: `docs/plan/screen/definition/01-decision-log.md` (D71·D72·D75·D81·D83).
