# 파킹랏
- 공용 매니저 와이어프레임(dashboard 등 ~10개) 좌측 nav의 옛 "기수 구성" → 삭제, 총괄 셸에만 "운영 관리" 표기하는 nav 스윕 (D67·D68 후속). ※담당 매니저가 "기수 구성/프로젝트"를 보는지 = RBAC 미확정, 사용자 확인 필요(D77 구현 중 재확인).
- D77 후속: 프로젝트 생성 폼(FB-F5, 총괄 권한) + 팀 편성 골라담기 상세 뷰 미구현. 워크스페이스는 조회/구조까지만.
- D77 후속: 10개 매니저 화면 chrome의 준비/실행/분석 그룹핑 미반영(project-workspace 셸에만 정식 반영).
- 제품 nav 깊이 정책(검토 필요): 목업 내부 사이드바에 하위메뉴(토글) 도입 여부. 결론 방향 — 사이드바=장소(명사)지 액션(동사)이 아니므로 "프로젝트 생성/편집" 같은 액션은 nav 잎사귀 금지(버튼·배지로 게이팅). 예외는 **운영관리(총괄, 가끔 쓰는 설정)만** 장소성 하위메뉴(기수/반/명단/매니저) 허용. 데일리 영역(분석진단·교육생·개입·리포트)은 얕은 nav 유지. '한눈에 관리'는 nav 깊이가 아니라 overview 화면으로. (쇼케이스 사이드바 ≠ 제품 사이드바 — 전자는 리뷰용 전수 열거라 깊어도 됨.)
- D56 B 후속(2026-08-07): `manager/dashboard/components/InboxRow.tsx` `ItemWhy`의 RETRY 케이스 "창 마감 {deadlineLabel} · {missingCount}건 미응시" — 창이 열려 있는 상태에서 "미응시"(마감 후 확정 뉘앙스)를 쓴다. B 작업 중 발견, 범위 밖이라 미수정.
- D56 B 후속(2026-08-07): `AttendanceStatus.MISSED`(마감 후 확정 미응시)·`retryStatus` "미응시" 둘 다 **시드 사례가 없다** — mif-3는 응시 창이 전부 열려 있고(같은 팀=같은 창) t9의 `retryDueAt`도 미래라, 새 상태값의 확정 분기가 화면에서 한 번도 안 그려진다. 코드 주석(`mockData.ts` MIF3_ATTENDANCE·t9 시드)에만 적혀 있던 것을 파킹랏으로 올린다. 채우려면 결과 9명 집계가 흔들려 별도 판단 필요.
- 기수 스코프 수정(2026-08-24) 후속: `manager/dashboard/_/api/api.ts`의 `useInbox` 기본 프로젝트 선택이 `status === 'RUNNING'`만 찾는다 — 종료 기수(5·6기)는 프로젝트가 전부 CLOSED라 하나도 안 걸리고 `projectId`가 `null`로 떨어져 "진행 중인 프로젝트가 없습니다"가 뜬다. 콘텐츠는 실제로 있고(미프 1~4차) 드롭다운으로 고르면 나온다 — `RunLine.tsx`의 `<Select>`도 값이 없을 때 placeholder 문구가 없어 화살표만 빈 채로 보인다(사용자 실측, 5기 대시보드 첫 진입). 기수 스코프 이슈와 다른 파일·메커니즘이라 범위 밖으로 미룸.
