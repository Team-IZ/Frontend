# MG-02 · 히트맵

## 1. 한 줄

**반 전체가 어느 개념에서 무너졌는지**를 한 화면에서 본다.

## 2. 진입 · 이탈

| | |
|---|---|
| 진입 | 좌측 네비 |
| 이탈 | 개인 행 클릭 → **MG-06** |
| **계층** | **담당 반 → 팀 → 개인 3단**(14번 12-3 · 15번 69행). 화면이 바뀐다 |

## 3. 뷰 — v1 이해 단계 히트맵 구조를 그대로

```
히트맵 › 7기 › A반
히트맵
보기 [반별] › 팀 › 개인    프로젝트 [회차 미프 3차 ▾]    취약 ▮▮▮▮▮ 양호  ▨ 문항없음
──────────────────────────────────────────────────────────────
💡 이 반은 Graph 구성(1.9)에서 공통적으로 무너집니다. 23명 중 12명이 2단 이하.
   [반 전체 안내 보내기]  [면담 대상 보기]
──────────────────────────────────────────────────────────────
              개념 1          개념 2        개념 3
              HITL Trigger    Graph 구성    State 관리
 A반 평균 23명    2.2 주의       1.9 취약      3.4 양호
 ─ 팀별 · 행 클릭 → 그 팀 팀원
 2팀  3명 ▸       1.7           1.7           3.0
 3팀  3명 ▸       2.3           1.7           3.3
──────────────────────────────────────────────────────────────
· 열은 그 회차의 검증 개념 3건입니다 …
```

| v1에서 가져온 것 | |
|---|---|
| **줌 브레드크럼** | `보기 [반별] › 팀 › 개인` — 지금 어느 계층인지 |
| **우측 범례** | `취약 ▮▮▮▮▮ 양호` + 문항 없음 |
| **평균 행 + 구분 행** | `avgrow`(테두리 강조) → `grouprow`(설명) → 상세 행 |
| **셀 안 라벨** | `1.9` 아래 `취약` — 숫자만으로는 좋은지 나쁜지 모른다 |
| **인사이트 배너** | 한 문장 진단 + 액션 버튼 |
| **하단 각주** | 값의 의미·드릴 경로·주의사항 |

### 셀에는 값만, 라벨은 튀는 셀에만 — v1 방식

v1 히트맵은 셀이 대부분 **맨 숫자**였고 `취약 / 주의`는 평균 행과 눈에 띄는 셀에만
달렸다. 그게 맞다 — **전부 달면 라벨이 배경이 되어 아무것도 강조되지 않는다.**

| 값 | 라벨 |
|---|---|
| `< 2.5` | 취약 |
| `2.5 ~ 2.7` | 주의 |
| 그 위 | 없음 |
| **개인 셀** | **없음** — 2단 이하가 다수라 라벨이 배경이 된다. 색 + 셀 테두리로 충분 |

`무엇을 / 왜 그렇게` 같은 단계 서술도 셀에서 뺐다. **셀마다 문장을 반복하면 셀이
넓어지고 표가 가로로 늘어나 비교가 안 된다.** 히트맵은 **한눈에 훑는 표**이지 읽는
표가 아니다. 셀 폭 **92px 고정**, 열 너비에 맞춰 늘리지 않는다.

**단계 뜻은 개인 뷰 표 아래 한 줄**로만 둔다 — `1단 무엇을 · 2단 왜 그렇게 · 3단 다른
방법 · 4단 언제 깨지나`. 반·팀 뷰는 평균이라 필요 없다. 상시 세로 목록으로 띄우면
**매번 읽는 정보가 아닌데 화면 예산을 상시로 쓴다.**

### 좌상단에 축 안내 · y축 폭 150px 고정

`개념 1 / 개념 2 / 개념 3`만 있으면 **그 열이 무엇인지 모른다.** 표 좌상단(비는 칸)에
축을 쓴다.

```
세로 팀 › 팀원          개념 1        개념 2       개념 3
가로 이 회차의 검증 개념 3건  HITL Trigger  Graph 구성   State 관리
```

**y축 칸은 150px 고정**이다. 안 그러면 `담당 반`·`김민준`·`4팀`의 글자 길이가 달라
**화면마다 히트맵 시작 x가 움직인다** — 계층을 오갈 때 표가 좌우로 흔들려 시선이 매번
다시 찾는다. 범례와 단계 안내도 같은 160px 들여쓰기로 첫 셀에 맞춘다.

### 글자 크기 — 셀 밖 텍스트는 12px 이상

10~11px로 깔았다가 올렸다. 히트맵 자체가 작고 주변 텍스트가 다 회색이라 **읽히지
않는 장식**이 됐다.

| | 전 | 후 |
|---|---|---|
| x축 개념명 | 12px 보통 | **13px 굵게** |
| 행 그룹 안내 | 10px 대문자 | **12px** |
| 위험 배지 | 10px | **12px** |
| 범례 · 단계 안내 | 11px | **12px** |

**셀 안 `under` 밑줄도 지웠다** — 2단 이하 표시였는데 색이 이미 같은 말을 하고, 일부
셀에만 그어져 무슨 뜻인지 알 수 없는 장식이었다.

### 범례는 히트맵 바로 아래 — 툴바 오른쪽이 아니라

**범례는 표를 읽는 열쇠라 표에서 멀어질수록 표 → 범례 → 표 왕복이 생긴다.** 오른쪽
카드에 넣었다가 툴바 오른쪽 끝으로, 다시 **히트맵 바로 아래**로 옮겼다 — 이 화면의
히트맵은 3열이라 폭이 좁고 왼쪽에 붙어 있어서, **툴바 오른쪽 끝은 표에서 가장 먼
자리**였다.

### 판정 줄을 지웠다 — 판정은 셀 위에 남는다

세 자리(표 위·오른쪽·아래)를 다 시도하고 아래로 정했다가, **줄 자체를 지웠다.**

```
⚠ A반 · Graph 구성  23명 중 12명이 2단 이하 — 절반을 넘어 반 문제
                              [A반 자세히 보기] [오퍼레이터에게 공유]
```

이 한 줄이 하는 일이 셋인데 셋 다 문제였다.

| 하는 일 | 문제 |
|---|---|
| **화면의 숫자를 문장으로 다시 읽음** | `23명 중 12명`은 셀에 이미 있다 |
| **읽는 법을 가르침** | *"가로로 읽으면 안 된다"*(OP-02 팀 뷰) — 화면이 사용법을 가르치는 것이다 |
| **해석을 대신함** | *"개인을 부르기 전에 팀별로 먼저"* — 매니저가 할 판단이다 |

### 시스템만 아는 판정은 이미 셀 위에 있다

문장이 없으면 잃는 것이 있는지 따져봤다. **없다.**

| 시스템 판정 | 어디에 |
|---|---|
| 9-6 집단 미달 | 셀 **테두리**(`.grp2`) + `취약` 라벨 + 열 머리 `⚠` |
| 위험 유형(단계 하락·지속 저점) | 행 오른쪽 **배지** |
| 다시 보기로 오른 것 | 셀 안 `↑` |
| 문항 없음 | 해치 무늬 |

**판정 줄은 이걸 문장으로 반복한 것**이었다.

### 문장은 데이터가 다르면 거짓말을 한다

이게 결정적이다. `"네 팀 모두 절반 초과"` 같은 문장은 **실제 값이 다르면 틀린다.**
v1이 `AI 요약 일치검증`(RPT-01 case3)을 만들어야 했던 이유가 정확히 이것이고,
**15번이 그 기능을 삭제**했다. 사람이 쓴 판정 문장은 검증 없는 AI 요약이다.

**구현 비용도 반대 방향이다.**

| | |
|---|---|
| 배지·색·라벨 | 이미 계산된 플래그를 **표시만** 한다 — 추가 부담 **0** |
| 판정 문장 | 규칙을 자연어로 렌더링. 프론트에서 조건 분기를 짜면 **판정 로직이 두 곳에 복제**되고(SSOT), 서버가 문장을 내려주면 규칙이 바뀔 때마다 문구·테스트가 따라온다 |

### 남긴 것 하나 — 값이 왜 없는지

```
· 이번이 첫 회차입니다
  비교할 지난 회차가 없어 단계 하락은 판정하지 않습니다.
```

이건 해석이 아니라 **배지 열이 왜 비었는지**를 말하는 것이라 남긴다(F1·F3 —
없음과 0을 다르게). 아이콘도 액션도 없는 한 줄이다.

### 액션 버튼 5개를 전부 뺐다

| 버튼 | 왜 |
|---|---|
| `반 전체 안내 보내기` | **9-6이 *"시스템은 표시까지만 한다"* 고 명시** — 발송 기능이 기획에 없다 |
| `오퍼레이터에게 공유` | 공유 기능이 v2 어디에도 없다 |
| `면담 브리프 열기` · `보기` | **C4 위반** — *"브리프는 면담 목록에서만"* 이라 정해 놓고 여기서도 열었다 |
| `A반 자세히 보기` | 행 클릭이 이미 하는 드릴다운 |

**없는 기능을 버튼으로 만들면 그것이 곧 요구사항이 된다.**

### 판정은 세어지는 것만 — 이 원칙은 남는다

사이드에 *"개념 자체가 어려웠을 가능성이 커요"* 같은 문장을 썼다가 지웠다. **그건
원인 추정이고, 추정하려면 지금 없는 데이터가 필요하다** — 교안 난이도, 다른 기수 비교,
선수 개념 이력.

PRODUCT.md — **어디서 막혔는가는 말하고 왜 막혔는가는 말하지 않는다.** 이제 *"어디서"*
조차 **문장이 아니라 셀 위의 마크**로 말한다.

### 와이어프레임 각주는 화면 밖에

`· 열은 그 회차의 검증 개념 3건입니다…` 같은 설명을 앱 본문 안에 뒀다가 뺐다. **그건
실제 UI에 나갈 글이 아니라 리뷰어용 메모**인데, 브라우저 크롬 안에 있으면 사용자가
읽어야 할 글처럼 보인다. 목업에서는 **크롬 밖 회색 주석**으로 내린다.

### 각 계층마다 선택 필터와 정렬

`반별`은 사실상 **담당 반 전체 뷰**다. 계층을 내려갈수록 대상이 좁아지므로 **그 계층에서
필요한 선택 도구**를 툴바에 둔다.

| 화면 | 툴바 |
|---|---|
| **반별** | 프로젝트 · 계층 · **정렬**(낮은 순) · **⚠ 반 문제만** |
| **팀** | 〃 · **반 select** · 정렬 |
| **개인** | 〃 · **반 · 팀 select** · 정렬 · **위험만** |

**드릴다운으로만 내려가게 하면 매번 위에서부터 눌러야 한다.** select가 있으면 반·팀을
바로 바꾼다.

**툴바 크기는 프로젝트 목록과 같다** — 알약형 34px, 12px. 프로젝트 select만 13px 굵게로
한 단계 크다(이 화면의 변수라서).

### 회차 비교 토글 자리에 프로젝트 select

v1은 `현재 상태 / 회차 비교` 토글이었다. v2는 **축이 달라져서** 그 자리를 select가 쓴다.

| | v1 | v2 |
|---|---|---|
| 열 | L1~L5 **5축 고정** (이해 깊이 파생) | **그 회차의 검증 개념 3건** |
| 회차 | 비교 모드로 두 회차 | **하나씩** |

**축이 프로젝트마다 다르므로 select가 필수다.** 미프 1차의 `HITL`과 2차의 `Service`를
나란히 두면 비교 가능한 것처럼 보인다(6-3). 미프 8~10회면 열이 24~30개가 되는 문제도
같이 사라진다.

**회차 간 변화는 교육생 상세 타임라인**(MG-06)에서 본다.

### 값 — 개인은 그대로, 팀·반만 평균

| 행 | 값 |
|---|---|
| **개인** | 도달 단계 **그대로**(0~4). 평균이 아니다 |
| 팀 · 반 | 평균 (소수점 1자리) |

**5-3이 평균을 금지한 것은 위험 판정**이고, 히트맵은 **패턴 탐색**이라 팀·반 평균을 쓴다.
다만 개인 행까지 평균을 내면 그 사람의 판정값이 흐려지므로 **개인은 원값**이다.

**팀·반 평균은 판정에 쓰지 않는다** — 어느 열이 낮은지 눈으로 찾는 용도다.

### 색 스케일 — 여기서 확정하고 MG-06이 물려받는다

| | 값 | 색 |
|---|---|---|
| **―** | 코드에 없음 · 문항 없음 | **회색 사선.** 숫자가 아니다 |
| 0 | 응답 없음 · 무성의 | 진한 주황 |
| 1 | 무엇을 하는지까지 | 주황 |
| 2 | 왜 그렇게 했는지까지 | 옅은 주황 |

| 3 | 다른 방법까지 | 옅은 초록 |
| 4 | 언제 깨지는지까지 | 초록 |

**`―`는 숫자와 다른 기호·다른 무늬다**(F3·F4). 사선 패턴을 써서 색약이어도 갈린다 —
미실시를 0점으로 읽히게 하면 안 된다.

**색과 숫자를 둘 다 쓴다** — 색만 쓰면 정확한 값을 못 읽고, 숫자만 쓰면 패턴이 안 보인다.

**2단 이하에는 밑줄을 긋는다.** 경계가 세로선이 될 수 없다 — 값이 열이 아니라 셀에
있기 때문이다. 색 농담만으로는 "다시 보기 대상인가"가 안 잡히므로 **밑줄로 한 번 더**
표시한다(F4). 팀·반 평균 행은 옅은 밑줄이다 — 판정값이 아니라서.

### 드릴다운 — 줌 브레드크럼으로 3단

`보기 [반별] › 팀 › 개인`. 한 화면에 계층을 쌓지 않고 **화면이 바뀐다.**

| 화면 | 상단 행 | 아래 행 | 값 |
|---|---|---|---|
| **① 반별** (시작) | 담당 반 평균 | **담당 반들** | 평균 |
| **② 팀** | 그 반 평균 | 팀들 | 평균 |
| **③ 개인** | 팀 평균 | 팀원 | **도달 단계 그대로** |

**첫 화면이 반 비교인 이유:** 담당 반이 여럿이면 *"어느 반이 처지나"* 가 먼저다. 반이
하나면 ①이 생략되고 ②부터 시작한다.

**오퍼레이터는 앞에 기수가 하나 더 붙어 4단**이다 — `기수 › 반 › 팀 › 개인`.

### 다시 보기는 필터가 아니라 셀 표식

**15번 #3**: 재시험 포함/미포함 필터 폐기 → **셀에 개선 표식(↑)만**.

조직 단위 수치는 1차만 쓰지만(11절), 다시 보기 결과를 아예 안 보여주면 매니저가
*"시켰는데 어떻게 됐지"* 를 알 수 없다. **개인 셀에 `↑`** 를 붙이고, **반·팀 평균과 위험
판정은 1차 값 그대로** 둔다 — 다시 보기는 못한 사람만 보므로 섞으면 평균이 위로 밀린다.

### 집단 미달 — 배너와 셀 둘 다

**15번 #9**(미정이던 것)를 여기서 정한다.

| | |
|---|---|
| **배너** | 놓치지 않게. 상단에 인원과 조치 |
| **셀 테두리 + 열 머리 ⚠** | **어느 열인지** 알게 |

배너만 두면 표에서 그 열을 다시 찾아야 하고, 셀만 표시하면 스크롤 밖에 있을 때 놓친다.

### 위험 유형은 배지로

9-2의 **단계 하락 · 지속 저점**은 직전 회차와 비교해 나오는 값이라, 이 화면이 한 회차만
보여도 **판정 결과는 배지로 실을 수 있다.** 오른쪽 끝 열에 둔다.

전이 자체(어느 회차에서 떨어졌는지)는 **교육생 상세 타임라인**에서 본다.



## 4. 데이터

회차 × 개념 × (반·팀·개인) 도달 단계. **집단 미달**(반 절반 이상 미달)인 열은 열 머리에 표시.

## 5. 액션

셀·행 클릭 → 해당 단위. 개인 행 = MG-06.

## 6. 상태 — 왜 그렇게 정했나

> **케이스 표는 목업에 있다** — [`manager/heatmap.html#cases`](../wireframe/manager/heatmap.html#cases).

### 1회차에는 단계 하락을 판정하지 않는다

`단계 하락` 배지는 **지난 회차 대비**다. 미프 1차에는 비교 대상이 없다 — 그대로 두면 **첫 회차 학생이 전부 하락으로 보인다.**

배지를 빼고 `이번이 첫 회차입니다`로 밝힌다. **지금 낮은 개념은 있지만, 떨어진 것인지 원래 그런 것인지는 다음 회차부터 알 수 있다.**

### 없음 · 0단 · 미응시는 서로 다르다

| 표기 | 뜻 | 처방 |
|---|---|---|
| `―` | **코드에 그 개념이 없어 못 물었다** | 학생이 아니라 **개념 선택** 문제 |
| `0단` | 물었는데 못 했다 | 그 개념을 다룬다 |
| 셀 비움 + `미응시` | 아예 안 봤다 | 응시부터 |

같은 칸으로 그리면 처방이 달라진다. **평균 모집단에서도 미응시는 뺀다.**

### 인원이 적은 팀도 평균은 내되 순위를 매기지 않는다

팀은 3~4명이다. 이 표본으로 팀을 줄세우면 한 사람이 팀 순위를 뒤집는다 → **인원 수를 같이 쓰고**(`3명`) 순위는 붙이지 않는다.
## 7. 안 하는 것

| | 어디에 |
|---|---|
| 위험·우수 테이블 | **MG-03 면담 · MG-05 명부** — 매니저에게 별도 테이블은 중복 |
| 개인 이벤트 이력 | **MG-06** |
| 숫자 점수(0~5) | 없음 |


---

## v1 대조 (SC-M05) — 검수 결과

> v1은 **14 case**였다(DASH-14 이해 단계 7 + DASH-15 기술영역 7). **절반이 성립하지 않는다.**

| v1 | 판정 | |
|---|---|---|
| 14-1·3 축 점수 없음 · 근거 세션 없는 셀 | **그대로 · 이름만** | → `―`(문항 없음) |
| **14-2 회차 1건 이하 → Δ 산출 불가** | **살림** | v2에 **없었다.** 1회차 단계 하락 판정 문제 — 위 §6 |
| 14-7 재측정 값 갱신 | **그대로** | `다시 보기` 반영(`#retried`) · **위 평균은 1차 기준** |
| 15-1 표본 부족 N<5 | **바꿈** | 유보하지 않고 **평균 + 인원 수**를 쓴다. 대신 **순위를 안 붙인다** |
| 14-4·5 일부 축 결측 · 5축 파생 오해 | **버림** | **축이 화면에 없다.** 0~5 루브릭은 내부 값 |
| 14-6 기수 평균 부재 | **버림** | 매니저 스코프는 담당 반이다. 기수 평균을 쓰지 않는다 |
| **15 전체(기술영역 히트맵)** | **버림** | v2에 **그 화면이 없다.** 열은 언제나 그 회차 **검증 개념 3건** |
| 15-7 회차 간 영역 구성 변경 | **전제로 흡수** | **회차마다 개념이 다른 것이 v2의 전제**다 — 프로젝트 select로 하나씩 본다 |

### v2에서 채운 것

| | |
|---|---|
| 1회차 상태 없음 | → `#first` 추가 |
| 집계 실패 | → 표 행 추가(`HEATMAP_UNAVAILABLE`) · **0으로 그리지 않는다** |
