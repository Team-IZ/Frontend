# 개발 결정 기록

> 프론트엔드 **개발** 과정의 결정과 그 목적·근거. 팀에 "왜 이렇게 했는가"를 그대로 전달하기 위한 문서.
> 새 결정이 나올 때마다 아래에 append.
>
> **화면 설계 결정은 여기가 아니다** — `docs/plan/screen/definition/01-decision-log.md`(기획 단계, D1~D131)에 있다.
> 그쪽을 가리킬 때는 번호가 겹치므로 **"화면정의 로그 D4"**처럼 출처를 명시한다.
>
> **한 화면을 만들며 내린 결정도 여기가 아니다** — 화면별 문서가 갖는다(`op-01-dashboard.md` ·
> `op-02-analysis.md` · `op-projects-progress.md`). 여기 남는 것은 **여러 화면에 걸치는
> 것**(구조·규약·도구)뿐이다.

---

## D1 · 개발 착수: `feature/setup` 브랜치 + `docs/` 재편(plan/screen · handbook)

- **배경:** 목업(화면정의 v8) 단계가 끝나고 프론트엔드 개발이 시작됨. 목업 작업은 `planning` 브랜치에 쌓였고 기본 브랜치 `main`은 아직 Vite 스타터(hello-world). 산출물이 `docs/` 루트에 평평하게 섞여 있어(화면정의 + 방법론 + 구버전 v7 + 프로세스 문서) 개발 브랜치에 그대로 들고 가면 스펙과 잡문서가 구분되지 않음.
- **선택지:** (a) `planning`에서 그대로 개발 계속 (b) `main`에서 `feature/setup` 분기 후 필요한 문서만 이관 (c) 문서는 `planning`에만 두고 개발 브랜치는 순수 코드.
- **판단:** (b). `main`에서 `feature/setup`을 파고, **개발 레퍼런스가 되는 문서만** 이관하며 동시에 폴더를 재편한다.
  - `docs/plan/screen/definition/` — 화면정의 명세 전부(SC-\*, 번호문서, `_parking-lot`) + `_source/`(design-tokens · component-contract). **평면 유지**.
  - `docs/plan/screen/wireframe/` — 와이어프레임 HTML 23개.
  - `docs/handbook/` — 팀 협업 규약.
  - **제외:** 구버전 `docs/screens/`(v7) · `docs/superpowers/`(스펙 제작 프로세스 문서) · 루트 PNG 48장(작업 중 스크린샷) · 기획 단계 방법론·회고 문서. 전부 `planning` 브랜치에 보존.
- **근거·출처:** ① 명세서를 평면으로 유지한 이유 = SC↔번호문서↔`_source` 상호링크 34건이 전부 **같은 폴더 상대링크**라, 하위 폴더로 쪼개면 전량 깨짐(이관 전 grep 검증). ② 와이어를 별도 폴더로 뺀 것은 안전 = 마크다운 링크로 참조되는 곳이 0건(코드스팬 언급뿐). ③ 방법론·회고 문서를 뺀 이유 = 제품 스펙이 아니라 기획 단계 산출물이라 개발 브랜치에 두면 신입이 스펙으로 오독함.
- **경로 갱신 원칙:** **실제 파일을 가리키는 포인터만** 새 경로로 치환. **당시 결정의 기록은 원문 보존** — 화면정의 로그 D4의 "`docs/screen-def-v8/` 신규, 예전 `docs/screens/` 미참조"는 그대로 둔다. 히스토리를 새 경로로 덮어쓰면 결정 기록이 위조되므로.
- **목적·효과:** 개발자가 `docs/plan/screen/`만 보면 "무엇을 만드는가"가, `docs/handbook/`만 보면 "어떻게 일하는가"가 나온다.
- **후속:** 목업 배포(gh-pages, `team-iz.github.io/Frontend/`)와 `planning` 브랜치는 목업 아카이브로 유지하다 폐기. 폐기 전 태그로 박제 권장.

## D2 · 팀 Git 규약: 문서에 적을 것과 기계가 막을 것을 분리

- **배경:** 팀이 정한 Git/PR 규약 원문을 어디에 넣을지(md냐 커밋 시 실행되는 스킬이냐) 질문받음. 원문은 커밋 형식·체크리스트·브랜치 전략·PR 규칙·이슈 기준·충돌·커뮤니케이션이 한 덩어리로 섞여 있었음.
- **선택지:** (a) 원문 그대로 md 한 장 (b) 원문 그대로 AI 작업규약에 삽입 (c) **강제 수단별로 쪼개 배치**.
- **판단:** (c). 규칙을 주제가 아니라 **"무엇이 이걸 강제하는가"**로 나눈다.
  - **기계** — `.env` 금지=`.gitignore` · 커밋 메시지 형식=`commit-msg` 훅 · `console.log`=oxlint · 빌드=GitHub Actions · 직접 push=브랜치 보호 · PR 형식=템플릿
  - **핸드북 `docs/handbook/git-convention.md`** — 사람 판단 영역만(브랜치 전략·작업 흐름·이슈 등록 기준·충돌 예절·리뷰 SLA).
- **근거:** ① 원문 체크리스트 6개 중 5개가 기계로 검증 가능 — "console.log 제거" 체크박스보다 린트 룰 한 줄이 정확하고, 사람이 체크하는 항목은 바쁠 때 가장 먼저 생략된다. ② 원문이 같은 규칙을 **3번 중복** 기재했고 이미 드리프트 시작(`1인 승인` vs `1인 리뷰`, 체크박스 누락 2건) — 규약이 3개면 지켜지는 건 0개라 단일 원천으로 통합.
- **폐기:** "로컬에서 정상 동작 확인" 체크 항목 — 검증 기준이 없어 체크박스로 무의미.
- **발견한 충돌:** 규약 전체가 `develop` 통합 브랜치를 전제하는데 레포에 `develop`가 없었음 → 생성 + 브랜치 보호를 세팅 범위에 편입.
- **목적·효과:** 사람은 판단이 필요한 것만 읽고, 나머지는 커밋·PR 시점에 자동으로 막힌다.

## D3 · 규약 자동화: git 네이티브 훅 + 브랜치별 이슈 번호 자동 부착

- **배경:** [[D2]]에서 "문서에 적으면 무시된다 → 기계가 막아야 한다"고 판단한 것의 구현. 추가로 "커밋마다 이슈 번호를 사람이 타이핑해야 하나"라는 문제 제기.
- **선택지(훅 설치):** (a) husky 도입 (b) **git 네이티브 `core.hooksPath`** (c) 각자 `.git/hooks`에 수동 복사.
- **판단:** (b). `.githooks/`를 레포에 커밋하고 `core.hooksPath`로 가리킨다. husky는 이 정도 검사에 npm 의존성 하나를 더 얹는 것이라 불필요. `package.json`의 `prepare` 스크립트가 `npm install` 시 자동 설정하므로 팀원은 클론 후 별도 조치가 없다. (c)는 버전 관리가 안 되어 사람마다 규칙이 갈린다.
- **선택지(이슈 번호):** (a) 매번 손으로 입력 (b) 브랜치명에 번호를 넣어 파싱(`feature/3-setup`) (c) **`git config branch.<name>.issue`에 1회 등록 → `prepare-commit-msg`가 자동 부착**.
- **판단:** (c). (b)는 규약의 브랜치 네이밍(`feature/login-api`)을 바꿔야 하고, (a)는 누락이 반복된다. (c)는 브랜치 생성 시 한 번만 등록하면 이후 커밋 전부에 `(#N)`이 붙고, 이미 번호가 있으면 존중한다.
- **차단 범위:** 되돌리기 비싼 것만 막는다 — `main`·`develop` 직접 커밋, `.env` 스테이징(`.env.example`류는 허용), **AI 공동저자 trailer**(`Co-Authored-By`·`Generated with`·🤖 — 커밋 저자는 사람). 이슈 미연결은 **경고만** 하고 통과시킨다(오타·포맷팅은 이슈 없이 커밋하는 것이 규약 §3).
- **길이 규칙 해석:** 50자 상한은 **`(#N)`을 제외한 설명** 기준. 훅이 자동 부착하는 메타데이터까지 세면 사용자가 쓴 적 없는 글자로 커밋이 거부되기 때문.
- **검증:** `.githooks/test-hooks.sh` 자체 검사 18케이스(정상 7 · 위반 11) **전량 통과**. 이슈 자동 부착·중복 방지·`.env` 차단은 실제 실행으로 확인.
- **한계(명시):** 로컬 훅은 `--no-verify`로 우회된다. **최종 방어선은 GitHub 브랜치 보호 규칙**이며 이는 레포 설정 사항이라 훅으로 대체 불가.
- **AI 서명 소급 안 함:** 기존 히스토리에 5건 이상 유입됐으나 이미 push된 커밋의 재작성 비용이 크고 다른 브랜치·gh-pages까지 물려 있어 앞으로만 차단.

## D4 · default 브랜치를 `develop`으로 변경

- **배경:** 규약은 "모든 기능은 develop으로 머지"인데 GitHub default가 `main`이었음.
- **판단:** default = `develop`.
- **근거:** ① **`closes #N` 자동 닫힘은 default 브랜치로 머지될 때만 작동한다.** default가 main이면 feature→develop 머지로 이슈가 안 닫혀 승격 전까지 계속 열려 있음 — 규약과 GitHub 동작이 어긋남. ② New PR 기본 base가 develop이 되어 실수로 main에 PR을 넣는 사고가 준다. ③ `main`의 역할이 "프로덕션 릴리스 전용"으로 명확해진다.
- **필수 동반 조치:** default를 옮겼으므로 **`main`·`develop` 둘 다** 브랜치 보호를 건다. 안 그러면 develop이 무방비가 된다.
- **연결:** `docs/handbook/issue-and-branch-online.md` §4·부록.

## D5 · 세팅 1차: 되돌리기 비싼 것부터 (strict · 포매터 · alias · CI)

- **배경:** "팀 협업을 위해 세팅 단계에서 뭘 해야 하나" 질문. 목적은 도구를 채우는 게 아니라 **자유도를 미리 줄이는 것** — 4명이 4가지 방언으로 쓰기 시작하면 되돌릴 수 없다. 판단 기준은 하나: **"나중에 하면 비싼가"**(Type-1/Type-2, 화면정의 로그 D5와 동일 프레임).
- **한 것 (Type-1 — 코드 쌓이기 전에만 쌈):**
  - **`strict: true`**(app·node 양쪽). 지금 켜서 **에러 0건**, 코드가 쌓인 뒤면 수백 건. 실측 확인 후 적용.
  - **Prettier** — 없으면 매 PR이 공백 diff 전쟁이 되어 리뷰가 로직을 못 본다. 설정은 **기존 코드 스타일에 맞춤**(`semi:false`·`singleQuote`) — 포매터 도입으로 생기는 리포맷 diff를 최소화하기 위함.
  - **`@/` path alias**(vite `resolve.alias` + tsconfig `paths` 쌍) — `../../../` 방지. tsc·vite 양쪽에서 실제 import로 해석 검증.
  - **`.editorconfig`** — prettier를 안 돌리는 에디터·비JS 파일 대비.
- **한 것 (Type-2지만 매일 아픈 것):** CI(GitHub Actions) · Node 22 고정(`.nvmrc`+`engines`) · README를 Vite 템플릿 원문에서 팀 온보딩용으로 교체 · PR 템플릿.
- **포매터 적용 범위 — 마크다운 전면 제외:** prettier가 표 셀을 **글자 수 기준**으로 패딩하는데 한글은 표시 폭이 2배라, 스펙 문서 30여 개의 표가 오히려 어긋난다(README에서 실제로 재현·확인). 렌더 결과는 어차피 같으므로 **`**/*.md` 전체 제외**. 포매터는 코드·설정에만 건다.
- **`baseUrl` 제거:** TypeScript 6에서 deprecated(TS5101). `paths`만으로 tsconfig 위치 기준 해석되므로 `ignoreDeprecations`로 덮지 않고 제거하는 쪽을 택함.
- **CI 3단계(format:check · lint · build):** `build`가 `tsc -b && vite build`라 타입 검사를 포함 → typecheck를 따로 돌리지 않는다(중복 제거). **로컬에서 CI와 동일 명령을 전수 실행해 3단계 전부 종료코드 0 확인** 후 커밋.
- **안 한 것 (명시):**
  - **`.env.example`** — 환경변수가 아직 0개. 빈 파일은 "나중을 위한 스캐폴딩"이므로 MSW/API가 붙는 단계에서 첫 변수와 함께 만든다.
  - **Storybook** — 컴포넌트 소비자가 여러 팀이 될 때. 3~4명 규모에 와이어프레임이 이미 스펙 역할을 하므로 유지비만 남는다.
  - **Vitest·E2E** — 테스트할 로직이 아직 없다. 첫 실제 로직(RBAC 게이트·기수 스코프 전파)에서 도입.
  - **폴더 레이어 경계(ui/common/features)** — 빈 디렉터리를 미리 파는 건 스캐폴딩. 실제 컴포넌트가 생기는 디자인 시스템 작업에서 규칙과 함께 확정.
- **이슈 분리:** `#3 [Chore] Project Settings` = `src/`를 건드리지 않는 레포·협업 인프라까지. 디자인 시스템(토큰→Tailwind `@theme`·컴포넌트 3계층·variant 어휘·a11y·RBAC 프리미티브)은 **첫 실제 코드**라 리뷰 포인트가 완전히 달라 별도 이슈로 분리(규약 §4 "1 PR = 1 기능", 파일 10개 이내).
- **연결:** [[D1]] 개발 착수 · [[D2]] 규약 배치 원칙 · [[D3]] 훅 자동화 · `docs/plan/screen/definition/07-frontend-setup-plan.md` 0단계.

## D6 · 리뷰 승인을 강제하지 않는다 (의도적 이탈)

- **배경:** ruleset을 점검하다 규약(§2·§4 "최소 1인 리뷰")과 실제 설정(`required_approving_review_count: 0`)이 어긋난 것을 발견. 규약대로 1로 켤지 물음.
- **판단:** **켜지 않는다. 대신 문서에서 "1인 리뷰" 문구를 걷어내고 이탈임을 명시한다.**
- **근거:** 프론트 인원이 **2명**이라 승인 필수는 곧 "매 PR이 상대 1명에게 병목"이다. 지금은 일정이 빠듯해 그 대기 비용이 리뷰로 얻는 것보다 크다는 판단.
- **대신 하는 것:** 리뷰가 필요한 변경(공용 기반·설계 결정·남의 영역)은 **머지 전에 말로 요청**한다. 규칙이 아니라 판단으로 가져간다.
- **되돌리는 조건:** 인원이 늘거나 일정에 여유가 생기면 승인 1인을 다시 켠다.
- **왜 문구를 지웠나:** 지켜지지 않는 규약을 문서에 남겨두면 **나머지 규약의 신뢰도까지 같이 떨어진다**([[D2]] "규약이 3개면 지켜지는 건 0개"와 같은 이유). 정석이 아님을 아는 상태로 내린 결정이라는 사실 자체를 기록해 둔다 — 몰라서 빠진 것과 알고 뺀 것은 다르다.
- **함께 확인된 것:** ruleset에 `required_status_checks`가 없어 **CI가 빨개도 머지되던 상태**였다. 이건 이탈이 아니라 누락이므로 `verify` 체크를 필수로 걸었다.

## D7 · `.gitattributes`로 줄바꿈을 저장소 차원에서 고정

- **배경:** "`.gitattributes`가 없어 Windows·Mac 사용자가 같은 파일을 저장만 해도 전체가 바뀐 것처럼 보인다"는 지적을 받고 검증.
- **검증 결과:** 지적은 타당하다. 다만 현재 추적 파일 중 **CRLF는 0개**(전부 LF)이고 `core.autocrlf`는 미설정 상태였다.
- **판단:** 지금 넣는다. `* text=auto eol=lf` + 바이너리 확장자 명시.
- **근거:** ① `core.autocrlf`는 **사람마다 다른 로컬 설정**이라 신뢰할 수 없다 — Windows Git 설치 옵션에 따라 갈린다. 저장소 차원 보증은 `.gitattributes`뿐. ② 이미 넣은 `.editorconfig`·`.vscode`의 `eol=lf`는 **에디터** 설정이라 git의 체크아웃/커밋 변환은 막지 못한다. ③ **타이밍:** 지금은 정규화할 파일이 0개라 비용이 파일 1개지만, Windows 팀원이 CRLF를 커밋한 뒤에는 **전 파일을 건드리는 정규화 커밋**이 필요해진다 — 정확히 지적이 우려한 "왜 파일 전체를 고쳤지?" 상황이 된다.
- **검증:** `git add --renormalize .` 결과 변경 0건(공짜 시점 확인). `git check-attr`로 텍스트 파일은 `text=auto eol=lf`, PNG는 `binary set`으로 변환 차단됨을 확인.

## D8 · 이슈·PR 제목은 `[Tag] Title Case`, 커밋은 소문자 명령형

- **배경:** 이슈 제목 컨벤션 요청 — `[Chore] Project Settings`처럼 **의미 있는 단어는 대문자로 시작**하고, 관사·등위접속사·짧은 전치사 같은 무의미한 단어는 소문자.
- **판단:** 요청대로 Title Case. **더해서 PR 제목도 같은 규칙으로 통일**한다(기존 규약은 `[feat] add login page UI` 소문자였음).
- **근거:** 이슈와 PR은 **둘 다 GitHub UI에서 사람이 읽는 제목**이라 다른 규칙을 쓸 이유가 없었다. 반면 커밋 메시지는 소문자 명령형을 유지한다 — git 전통이고 훅이 이미 그렇게 검사하며, 로그를 기계로 훑는 대상이라 성격이 다르다. 경계를 "누가 읽는가"로 그었다.
- **어휘:** 태그는 커밋 type과 동일 — `[Feat]` `[Fix]` `[Refactor]` `[Style]` `[Docs]` `[Chore]` `[Remove]`.
- **강제 수단:** **훅으로 막을 수 없다**(제목은 GitHub 쪽). 이슈 제목을 검사하는 Action은 2명 팀에 잔소리 비용이 더 크다고 보고 만들지 않았다. 대신 **이슈 템플릿의 `title:` 기본값**(`[Fix] `·`[Feat] `)으로 형태를 미리 보여주는 방식을 택했다 — 빈칸보다 강하고 봇 코멘트보다 조용하다.
- **적용:** 템플릿 2개 기본 제목, PR 템플릿 주석, 규약 §3·§4, 이미 올라간 PR #4 제목.
- **부수 발견:** 이 레포는 옛 Projects(classic)가 붙어 있어 `gh pr edit`·`gh issue view`가 GraphQL 에러를 낸다. 제목 수정은 REST(`gh api -X PATCH`)로 우회했다.

## D9 · 디자인 토큰: 1레이어(의미론) + 가드, 컴포넌트는 미룸

- **배경:** 참고용으로 사내 다른 프로젝트(React Native 기반)의 디자인 시스템을 검토. 2레이어(원시 팔레트 → 시맨틱)를 쓸지, 컴포넌트를 먼저 만들지 판단 요청.
- **핵심 비대칭(사용자 지적):** 그 시스템은 **화면 5개를 만든 뒤 추출한 결과물**이다("값은 실화면의 실측 사용 빈도에서 도출"). 우리는 화면이 0개다. 같은 산출물을 시작점으로 복제하면 **추측을 시스템처럼 포장**하게 된다. → 베낄 것은 그들의 *산출물*이 아니라 *방법*(만들고 → 재고 → 뽑고 → 왜인지 적는다).

### 판단 기준
**"화면을 만들어봐야 알 수 있는가?"** — 그렇다면 지금 정하지 않는다. 색은 컴포넌트 구조와 독립적이므로(Button을 어떻게 쪼개든 primary는 `#3a4db8`) 토큰은 지금 확정, 컴포넌트 API는 미룸.

### 레이어 = 1개 (원시 팔레트 층 없음)
검토한 측면과 우선순위: ① 가역성 ② 테마 확장 ③ 디자인 소스 정합 ④ 강제 가능성 ⑤ 인지 부하 ⑥ SOLID ⑦ 변경 전파.

- **① 가역성이 결정적 — 한 방향 문이 아니다.** `@theme` 안에서 `--color-primary: var(--color-blue-600)` 형태의 참조가 동작함을 **빌드로 확인**했다. 나중에 원시 층을 끼워도 컴포넌트(`bg-primary`)는 변경 0건. 되돌릴 수 있으면 "지금 필요한 만큼만" 짓는 게 옳다.
- **② 테마 확장 요구 없음** — 플랫폼 콘솔 설정 탭은 상태·AI예산·보존기간·공개범위뿐이고 **기관별 브랜딩 항목이 없다**. 다크 모드도 스펙에 없다(교육생 세션의 다크 코드패널은 컴포넌트 색이지 테마가 아님). 2레이어의 최강 논거가 해당 없음.
- **③ 디자인 소스 정합** — 우리 SSOT는 역할당 값이 1개로 저작됐다. 원시 팔레트를 만들면 **디자인에 없는 구조를 발명**하는 것. 그쪽은 반대로 디자인 원본에 번호 스케일이 있어 2레이어가 원본을 반영한 것이다.
- **④ 강제 가능성** — 2레이어는 "컴포넌트는 시맨틱만 참조" 규율이 있어야 성립하는데, **참고 프로젝트조차 "가능하면 시맨틱을 먼저"라는 연성 규칙**이라 이미 새고 있다. 강제 못 하는 레이어는 무너지고, 무너진 2레이어는 1레이어보다 나쁘다(개념 2개·규율 0).
- **⑥ SOLID** — DIP(구체 대신 추상 의존) 이득은 컴포넌트가 hex 대신 `bg-primary`를 쓰는 시점에 **이미 달성**된다. 원시 층의 추가 이득은 SRP/OCP인데, 그건 **팔레트가 의미와 독립적으로 변할 때**만 값어치가 있다 — 전담 디자이너가 팔레트를 따로 관리하지 않는 우리 구조에선 발생하지 않는다.
- **전환 트리거를 `index.css` 주석에 명시:** ①다크모드·기관 브랜딩 요구 ②같은 계열 명도 단계 5개 초과(현재 최대 primary 4) ③디자이너의 독립 팔레트 관리.

### 이름 = 역할 기준 (실측)
`text-text-2` 같은 중복을 없애고 역할을 드러냄. 와이어 측정: `--c-text-3` **614회**(47%가 xs·11px 동반 → 캡션·메타), `--c-text-2` **280회**(19% → 보조 본문). → `fg` / `fg-muted` / `fg-subtle`, `bg` → `canvas`. **SSOT 문서도 같이 갱신**했다(코드와 문서가 갈리는 것보다 낫다).

### 간격 의도 레이어는 만들지 않음
참고 프로젝트의 간격 의도 레이어(화면 패딩·요소 간격에 이름을 붙인 것)는 실화면 빈도에서 도출된 것. 우리 와이어는 **지배적 패딩값이 없다**(sp-3/4/5/6 = 27·19·11·3회) → 만들면 존재하지 않는 일관성을 발명하게 된다. Tailwind 기본 스케일을 그대로 쓰고 **컨테이너 폭만** 토큰화(`--container-page`, 와이어 10회 일관).

### 가드 = 코드로 (참고 프로젝트에서 가져온 것)
그 프로젝트의 토큰 검사 스크립트는 **컴포넌트 29개에 하드코딩 hex 0건**이라는 결과로 실효성이 입증됐다. 이걸 본떠 `scripts/check-design.mjs`를 만들되 **bash가 아니라 Node**로 썼다 — `.gitattributes`로 Windows 호환을 챙겨놓고 bash 스크립트를 두면 앞뒤가 안 맞는다.
- 검사 ①하드코딩 hex(토큰 정의 파일만 예외) ②대비 AA 16쌍.
- **가드가 즉시 결함을 잡았다:** `fg-subtle #6b7280`은 흰 배경에서 4.83:1로 통과하지만 **실제 페이지 배경(`canvas` #f4f5f7)에서 4.43:1로 미달**이었다. 수동 검산이 놓친 것을 잡아 `#686f7d`(4.63:1)로 교정. 가드를 만든 이유가 그대로 증명됨.
- CI 3단계 → 4단계(design 추가). 위반 시 종료코드 1, 정상 0을 실제로 확인.

### 컴포넌트를 지금 만들지 않음
3번 반복되기 전엔 추출하지 않는다. 첫 화면에서 뽑은 Button은 두 번째 화면에서 반드시 틀린다. 순서를 이렇게 잡았다:
```
① 토큰 + 가드(이 결정)  → ② 화면 1개를 추상화 없이 끝까지 → ③ 두 번째 화면(중복 노출)
→ ④ 추출  ← 여기서부터 2명 병렬
```
②의 첫 화면은 **교육생 리스트** — 173줄로 가장 단순한데 반복 패턴(표·툴바·페이저·배지·빈상태)을 다 품어 추출 수확이 가장 크다. **병렬 시점이 "공용 컴포넌트 완성 후"가 아니라 "④ 추출 후"로 바뀐다.**

### 그 외 버린 것
TS 객체 토큰(RN은 CSS가 없어 불가피했던 것 — 우리가 하면 토큰이 두 곳에 생겨 drift), `AppText` 강제 래퍼(웹은 CSS 상속이 함), 학습용 장문 주석, 컴포넌트 29개 선제작.

## D10 · 디자인 시스템 카탈로그 페이지 — 그리고 그것이 드러낸 결함 3건

- **배경:** "만든 것을 한 화면에서 다 보고 싶다"는 요청. Storybook은 [[D9]]에서 규모에 안 맞아 제외했다.
- **형태를 두 번 바꿨다.** 처음에 앱 라우트(`/dev/design-system`)로 만들었으나, **정적 HTML이어야 한다**는 지적을 받고 생성물로 바꿨다. 이유가 타당했다 — ① 앱 코드는 검수 후 커밋해야 하는데 문서는 먼저 공유하고 싶었고, ② 팀원이 문서를 보려고 개발 서버를 띄워야 하는 것은 문턱이 높다. 지금은 `npm run doc:design`이 `docs/dev/design-system.html`을 만들고, 브라우저로 바로 열린다(외부 자원 참조 0건 확인).
- **핵심 판단 — 토큰 값을 문서에 적지 않는다.** 값을 하드코딩하면 토큰이 바뀔 때 문서가 조용히 거짓말을 하게 된다. 생성 시점에 `src/index.css`에서 읽어 이름·값 모두 `@theme` 하나에서만 나오게 했다. 가드가 하드코딩 hex를 막는 것과 같은 원칙이다.
- **드리프트를 CI가 막는다.** `--check`는 생성 결과가 커밋된 파일과 다르면 실패한다. 토큰만 고치고 문서를 다시 만들지 않는 것은 반드시 일어나므로 사람에게 맡기지 않는다(토큰 한 글자를 바꿔 종료 코드 1을 실측).
- **만들면서 결함 3건이 드러났다.** 카탈로그가 없었으면 전부 조용히 남았을 것들이다.

### ① Tailwind가 토큰을 `@layer` 안에 넣는다
최상위 `cssRules`만 훑으면 토큰이 **0개** 잡힌다. `:root, :host` 규칙이 `CSSLayerBlockRule` 안에 있어서다. 중첩 규칙까지 내려가도록 고쳤다. 빌드는 통과하므로 렌더해보기 전에는 알 수 없는 종류의 결함이었다.

### ② 문서에 적은 예시 코드가 빌드 CSS에 실렸다
카탈로그에 `--color-blue-600`이 떠서 추적하니, 값이 내가 쓴 hex가 아니라 **Tailwind 기본 팔레트의 oklch 값**이었다. 원인은 `docs/dev/decision-log.md`에 남긴 원시 팔레트 예시 한 줄 — Tailwind v4는 기본적으로 **프로젝트 전체**를 훑어 클래스 후보를 찾으므로 문서의 텍스트까지 스캔 대상이 된다.
- **조치:** 문서를 고치지 않고 **스캔 범위를 좁혔다**(`@import 'tailwindcss' source(none)` + `@source '.'`). 문서는 앞으로도 늘어나므로 원인 쪽을 막는 것이 맞다.
- **효과:** 빌드 CSS 19,666 → **17,265 bytes**. 문서에서 끌려오던 것이 그만큼 있었다.
- 주석을 고치다 같은 함정에 두 번 빠졌다("기본 팔레트 이름을 적지 말 것"이라고 쓰면서 그 이름을 적었다). 스캐너는 주석·문서를 가리지 않는다.

### ③ 정의했는데 방출되지 않는 토큰이 있었다
`success`·`neutral-soft`·`primary-border` 등 5개가 아직 어느 화면도 쓰지 않아 CSS에 실리지 않았다. 카탈로그가 "있는 토큰"이 아니라 "쓰인 토큰"만 보여주면, 새로 합류한 사람이 `success`를 못 찾고 임의 색을 만든다.
- **조치:** `@theme static`으로 전량 방출. 비용은 **175 bytes**로, 발견 가능성이 그 값어치보다 크다.

- **부수 효과:** 문서가 세는 색 개수와 가드가 세는 개수가 일치해 서로를 교차 검증한다. 분류에 안 잡힌 토큰은 "그 외"로 떨어져 눈에 띈다.
- **참고:** ①③은 실행 중 스타일시트를 파싱하던 시절의 결함이다. 정적 생성으로 바꾸면서 `@layer` 문제는 사라졌고(CSS 원본을 직접 읽는다), 미방출 토큰 문제도 사라졌다(`@theme static`은 유지 — 유틸리티 클래스 자체는 여전히 사용된 것만 생성된다). 그러나 ②(문서가 CSS를 오염시킴)는 형태와 무관하게 남는 문제였고 그대로 유효하다.

## D11 · 문서를 협업 자료로 — 정보를 세 층으로 나눈다

- **배경:** 팀원이 이 페이지를 보고 코드를 짤 것이므로 "어떤 경우에 쓰는지"와 "왜 그렇게 정했는지"가 필요하다. 다만 다 펼치면 훑어보기가 안 되고, 다 숨기면 아무도 근거를 안 읽는다.
- **판단 — 읽는 목적이 다르면 층을 나눈다.**

| 층 | 무엇 | 언제 읽나 | 수단 |
|---|---|---|---|
| 1 | 토큰 이름·값 | 무엇이 있는지 훑을 때 | 항상 보임 |
| 2 | 언제 쓰나 + 실제 예 | 코딩하다 잠깐 확인할 때 | 호버·포커스 |
| 3 | 왜 이렇게 정했나 | 합류했거나 규칙을 바꾸려 할 때 | 클릭(`<details>`) |

- **2층을 호버로 한 이유:** 코딩 중 확인은 빈도가 높고 짧다. 클릭이면 흐름이 끊긴다. 단 **호버 전용은 접근성 실패**이므로 카드를 `tabIndex=0`으로 두고 `group-focus`에도 같은 규칙을 걸었다(키보드로 Tab 이동 시 동일하게 열림을 실측 확인). 팝오버는 절대 위치로 겹쳐 띄운다 — 카드가 커지면 격자가 흔들린다.
- **3층을 `<details>`로 한 이유:** 네이티브라 키보드·스크린리더가 기본 동작하고, 접혀 있어도 내용이 문서에 있어 검색·인쇄에 걸린다. 상태 관리 코드도, JavaScript도 필요 없다 — 정적 HTML로 옮기면서 이 선택이 더 값어치를 했다.
- **설명을 `scripts/design-doc-content.mjs`로 분리:** 값은 `@theme`에서 읽고, 설명은 이 파일에서만 온다. **값을 두 곳에 적으면 한쪽이 반드시 낡는다**는 [[D10]]의 원칙을 설명에도 적용했다.
- **문서 구멍이 드러나게 했다:** 설명이 없는 토큰은 팝오버가 없어 눈에 띈다. 현재 우리가 정의한 31개는 전부 설명이 있고, 없는 것은 Tailwind 기본 `white` 하나다.
- **값이 조정된 토큰(4개)에 표시(✎)를 붙였다.** SSOT와 다른 값을 쓰는 이유를 카드에서 바로 알 수 있어야, 팀원이 SSOT 문서를 보고 "왜 다르지"라고 혼란스러워하지 않는다.

### 가드에 예외 표시를 추가했다

문서는 **"이렇게 쓰지 마라"를 보여주려고 그 나쁜 예를 적어야 한다.** 실제로 카탈로그의 금지 예시와 옛 SSOT 값 인용이 가드에 걸렸다.

- **판단:** 예시를 흐리게 쓰는 대신 줄 단위 예외 표시(`design-token-allow`)를 뒀다. 문서에서 반복될 상황이고, 예외 수단이 없으면 사람들이 **규칙 자체를 약화시키는 쪽**으로 간다.
- **표시가 눈에 보이고 grep으로 셀 수 있게** 했다 — 현재 5건(금지 예시 2, 옛 값 인용 3). 이 수가 늘어나면 규칙이 현실과 안 맞는다는 신호다.

## D12 · 셸·라우터 검토 — 참조 구현이 되기 전에 감사한다

- **배경:** 이 코드가 팀의 참조 구현이 된다(팀원이 다르게 짜오면 여기 맞추기로 함). 새 컴포넌트를 얹기 전에 기존 코드를 먼저 감사했다.

### 검토에서 잡은 결함 2건

| # | 무엇 | 왜 문제인가 |
|---|---|---|
| 1 | 미등록 경로 화면이 전역 `location.pathname`을 읽음 | 라우터 상태가 아니라 화면 안에서 경로가 바뀌어도 갱신되지 않는다. **실측 확인** — 주소는 `/second-missing`인데 화면은 `/first-missing`을 계속 보여줬다. `useLocation()`으로 교체 |
| 2 | 매니저 셸에 본문 건너뛰기 링크 없음 | 키보드 사용자가 **화면을 열 때마다 네비 8항목을 지나야** 본문에 닿는다. 참조 구현에 없으면 팀원 코드에도 없다. `sr-only` + `focus:not-sr-only`로 추가하고 `<main tabIndex={-1}>`로 포커스가 실제로 옮겨지는 것까지 확인 |

## D13 · 프리미티브를 전부 Base UI 위에 — 손수 구현 폐기

- **배경:** shadcn CLI(`base-nova` 스타일)로 뽑은 Base UI 포트와 손수 만든 버전을 나란히 두고 비교했다. 가르려던 것은 "Base UI로 우리 디자인을 그대로 재현할 수 있는가"와 "손수 만든 게 나은 지점은 어딘가".
- **1차 판단(기각):** APG 명세가 있는 위젯만 Base UI로 — Dialog·Tabs. Button·Badge·Card는 포커스·포털·키보드 관리가 필요 없어 이득이 없다고 봤다. 실제로 Base UI 프리미티브 위에 같은 cva를 얹었더니 픽셀 단위로 동일했다.
- **최종 판단(뒤집음):** **여섯 개 전부 Base UI 위에.** 손수 버전은 폐기했다.
  - 지금 안 쓰인다고 앞으로도 안 쓰이는 게 아니다 — Button에 아이콘·로딩이 붙거나 "버튼처럼 보이는 링크"가 필요해지는 건 시간 문제다. Base UI Button의 `render`(다른 요소로 렌더)·`focusableWhenDisabled`(비활성도 탭으로 닿게)는 그때 새 컴포넌트 없이 확장된다. Badge도 `useRender`로 같은 이점.
  - **"지금 필요 없다"(YAGNI)보다 "기반을 하나로 통일해 나중에 갈아끼우지 않는다"를 우선했다** — 프리미티브 레벨이라 화면 결정보다 되돌리기 비용이 크다.
  - 픽셀까지 같았다는 실측은 여전히 유효하다. "이득이 없다"가 아니라 "당장의 이득은 없지만 나중 확장 비용이 더 크다"로 결론이 바뀐 것이다.
- **CLI 결과물을 그대로 커밋하지 않는다 — 3단계를 거친다.** ① cva를 우리 토큰으로 교체(`rounded-lg/xl`은 우리 스케일에 없는 값이다) ② 안 쓰는 variant·size 전량 삭제 ③ 파일명·export 정규화. 안 거치면 "variant를 임의로 늘리지 않는다"는 자체 규율이 CLI 한 번에 무너진다.
- **export 규약:** 서브컴포넌트가 없는 단일 요소(Button·Badge)는 `export default` + named 둘 다. 컴파운드(Card·Dialog·Table·Tabs)는 named만 — 서브컴포넌트가 여럿이라 default가 애초에 불가능하다.
- **문서와 대조해 기본값에서 되돌린 것 2건.** Button의 `border border-transparent`(문서의 `.btn`은 `border: 0`이고 ghost만 1px), Badge의 `h-5` 고정 높이(`.bdg`는 높이를 정하지 않는다 — 고정하면 글자가 커질 때 잘린다). **"Base UI 위에"가 "shadcn 기본값대로"를 뜻하지는 않는다.**
- **shadcn 토큰층이 우리 토큰을 덮고 있었다.** `@theme static` 뒤에 `@theme inline`이 붙으면서 같은 이름을 뒤엣것이 이겼다. 빌드 산출물로 실측하니 `bg-primary`가 `#3a4db8`이 아니라 거의 검정, `rounded-md`가 10px이 아니라 8px, body 배경이 canvas가 아니라 흰색이었다. 겹치는 이름을 `@theme inline`에서 빼고 `:root`의 shadcn 변수를 전부 `var()`로 우리 토큰에 연결했다 — **값을 두 곳에 적으면 한쪽이 반드시 낡는다**는 [[D10]]의 원칙을 토큰 이름에도 적용한 것이다.
  > CSS 커스텀 프로퍼티 충돌은 **타입 검사도 린트도 못 잡는다.** 빌드 산출물을 열어보기 전까지 아무도 모른다.
- **미착수:** 아이콘 전용 버튼 사이즈(Dialog 닫기 버튼이 `size="sm"` + `p-1.5`로 정사각형을 흉내내는 중 — 실제 공백이지만 사용처가 한 곳뿐이라 둘째가 생길 때), 기수/프로젝트 전환 팝업(Select가 아니라 Menu Button 패턴이라 별개).

## D14 · 중복 프리미티브를 지금 통합하지 않는다 — 세 번째 등장까지

- **배경:** 인증 화면이 합류하면서 같은 역할의 조각이 두 벌이 됐다.

  | 인증 쪽 | 공용 쪽 | 겹치는 것 |
  |---|---|---|
  | `features/auth/components/PrimaryButton` | `components/ui/Button` | 주 액션 버튼 |
  | `features/auth/components/BrandPanel` + `AuthForm` | (셸 없음) | 인증 화면 2단 레이아웃 |
  | `features/auth/components/TextField` · `PasswordField` | (없음) | 입력 필드 |

- **판단: 지금 통합하지 않는다.** 사용처가 인증 화면 3개뿐이라, 지금 올리면 **인증 화면 사정에 맞춘 것이 공용이 된다.** 이건 `frontend-architecture.md`가 경계한 그 함정이다 — "처음부터 공용으로 만들면 한 곳의 사정에 맞춘 것이 공용이 된다."
- **다시 볼 시점:** 같은 조각이 **두 번째 도메인에서 필요해질 때**(세 번째 등장). 그때는 두 도메인이 실제로 공유하는 부분이 무엇인지 눈으로 보인다. 지금은 추측해야 한다.
  > 한 번은 만들고, 두 번은 기록하고, **세 번째에 올린다.** 두 번째에 올리면 공통점이 아니라 우연의 일치를 공용으로 만든다.
- **이슈로 두지 않고 여기 적는 이유:** "언제 끝난 것인가"를 판정할 수 없기 때문이다. 완료 조건을 쓸 수 없는 것은 이슈가 아니라 결정이다. 이슈로 두면 아무도 못 닫는 채 목록에 남아, 진짜 열린 작업이 안 보이게 된다.
- **중복이 생긴 진짜 원인은 규칙 부재가 아니다.** 규칙(`빈도 5회 이상만 공용으로`)은 이미 있었고 문서에도 적혀 있었다. 두 사람이 각자 만든 이유는 **만들기 전에 볼 곳을 몰랐기** 때문이다. 2인 팀에서 이걸 막는 것은 린터가 아니라 화면 착수 전 **"이 화면에 뭐가 필요한가"를 맞춰보는 5분**이다 — "이 두 파일이 같은 것"은 기계가 판정할 수 없다.

## D15 · 레이어 경계 린트가 절반만 지켜지고 있었다

- **배경:** 인증 화면을 계층 구조로 옮기며 import 방식을 정하다가, 규칙을 실제로 돌려보고 발견했다.
- **결함:** `@/features/*/*` 는 glob의 `*`가 `/`를 넘지 않아 **딱 두 단계만** 잡는다.

  | import | 고치기 전 | 후 |
  |---|---|---|
  | `@/features/{남}/AuthContext` | 막힘 | 막힘 |
  | `@/features/{남}/components/TextField` | **통과** | 막힘 |

  하필 통과하던 쪽이 **남의 도메인 컴포넌트 가져다 쓰기** — 교차 참조의 제일 흔한 형태다. 규칙은 있는데 정작 막아야 할 것을 안 막고 있었다.
- **고침:** 패턴에 `@/features/*/**` 를 더했다. feature가 늘어도 이 줄은 그대로다.
- **feature 안은 상대경로로 참조한다.** 이 규칙이 자기 자신도 막기 때문이기도 하지만, 그것과 별개로 폴더째 옮길 때 내부 import를 다시 쓰지 않아도 된다. 실제로 인증 화면을 옮기며 절대경로로 쓰인 import를 13개 파일에서 다시 썼다. `@/` 로 시작하면 그것만으로 "내 모듈 밖"이 드러나는 이점도 있다.
  > **규칙이 있다는 것과 규칙이 작동한다는 것은 다르다.** 검사 규칙도 코드다 — 통과하는 것만 보지 말고, **막아야 할 것을 넣어서 실제로 막히는지** 확인해야 한다. 이 결함은 규칙을 도입한 날부터 있었지만, 아무도 위반 코드를 넣어본 적이 없어 드러나지 않았다.

## D16 · v1 화면·셸을 지우지 않고 격리한다 — `git mv`로 보관

- **배경:** v2 화면정의서(25화면, `docs/plan/v2/`)가 확정되면서 기존 화면·셸(`src/features`, `src/shells`)이 전부 v1 구조(SC-xxx 기준) 위에 있다는 게 드러났다. 셸·URL 체계가 통째로 바뀌므로 기존 화면 위에 새 구조를 얹을 수 없다.
- **선택지:** (a) 기존 코드를 지우고 새로 짠다 (b) 별도 브랜치에 보관하고 develop에서는 지운다 (c) **같은 트리 안에서 `-v1` 접미사로 격리하고, 뼈대가 서면 화면을 하나씩 이식한다**.
- **판단:** (c). `src/features` → `src/features-v1`, `src/shells` → `src/shells-v1`(모두 `git mv`, 히스토리 보존). 이식은 화면 단위로 나중에.
- **근거:** 지우면(a) 실제 동작하는 v1 화면(로그인·커리큘럼·교육생 명부 등, 팀원이 이미 완성한 것)이 통째로 사라진다 — v2 뼈대가 서기 전까지 참고할 원본도, 이식할 원본도 없어진다. 별도 브랜치(b)는 두 브랜치를 오가며 대조해야 해서 실제로는 더 불편하다.
- **부작용과 수리:** 격리 직후 v1 화면 6개가 `@/shells/ManagerShell`(경로 별칭)을 참조하고 있었는데, 별칭은 파일이 옮겨져도 자동으로 안 따라가서 새로 만드는 v2 `ConsoleShell`에 잘못 바인딩됐다. `@/shells-v1/ManagerShell`로 전부 고쳐 원래 v1 셸을 다시 가리키게 했다.
- **목적·효과:** `src/features-v1`가 점점 비어가는 것 자체가 이식 진행률이 된다. 화면을 옮기고 나면 그 폴더만 지운다.

## D17 · 역할별 셸 4개를 `ConsoleShell(role)` 하나로 — 짜고 나서 SOLID·YAGNI 위반을 발견

- **배경:** v2는 역할 4개(슈퍼어드민·오퍼레이터·매니저·교육생)마다 메뉴·스코프가 다르다(`docs/plan/v2/17-ia-menu-tree.md`). 처음엔 v1의 `ManagerShell` 패턴을 그대로 늘려 `SuperAdminShell`·`OperatorShell`·`ManagerShell`·`TraineeShell` 4개 파일을 만들었다 — 각자 공용 프레임에 자기 역할의 사이드바 배열만 주입하는 얇은 래퍼.
- **문제 제기(사용자):** "통일된 거 하려고 ConsoleShell 만든 거 아니야? 넷 다 내용만 다를 텐데 왜 뷰를 하나하나 들어가서 고쳐야 돼?" — 스코프 라벨 하나 바꾸는 데도 파일 4개를 돌아다녀야 했다.
- **판단:** 4개 파일을 지우고 `ConsoleShell`이 `role: Role` 하나를 받아 사이드바(`SIDEBAR_BY_ROLE[role]`)·스코프(`COHORT_SCOPED_ROLES.includes(role)`) 둘 다 내부에서 결정하게 합쳤다. 역할별 래퍼는 "어떤 사이드바를 넣을까"만 다른 얇은 함수였다 — 동작이 아니라 **파라미터**만 다른데 파일을 따로 둔 것.
- **근거:** 이건 개방-폐쇄나 단일 책임과 무관한, 그냥 파라미터화 누락이었다(SOLID). "역할별로 다른 컴포넌트가 필요할 수도 있으니 미리 나눠 둔다"는 근거도 없었다(YAGNI) — 넷 다 지금도 앞으로도 프레임은 완전히 같다.
- **재발 방지:** 이 세션에서 "코드 짜기 전에 기존 구조 파악 + 계획부터"를 원칙으로 명시적으로 받았다. 여러 유사 파일을 만들기 전에 "이게 진짜 행동이 다른가, 파라미터만 다른가"를 먼저 묻는다.

## D18 · 손으로 만들지 않는다 — 있는 컴포넌트·토큰부터 확인

- **배경:** `ConsoleShell`의 상단바(`Header`)를 처음 만들 때 브랜드 로고·기수 선택기·사용자 아바타를 전부 `<div>`/`<span>`/`<button>`으로 손으로 그렸다. 나브 구분선 색도 임의 클래스(`border-nav-active`)를 새로 붙였다.
- **문제 제기(사용자):** "쉘 구분선이 너무 하얘" → 확인해보니 목업 값(`--c-nav-2`)과 다른 색을 썼다. 이어서 "만들 때는 항상 디자인 시스템·컴포넌트 있는 거 다 참고해서 해."
- **판단·조치:**

  | 손으로 만든 것 | 교체 |
  |---|---|
  | 브랜드 로고 `<div>` | `components/common/Wordmark`(인증 화면 `BrandPanel`과 같은 로고 — `features-v1/auth`에서 공용 위치로 승격) |
  | 기수 선택기 `<button>+▾` | `components/ui/Select`(`component-page-map.md`가 이미 이 자리를 select로 매핑해 둠) |
  | 사용자 이니셜 원 | `components/ui/Avatar` + `AvatarFallback` |
  | 나브 색 임의 클래스 | `index.css`에 이미 배선된 shadcn 표준 `--sidebar-*` 토큰(`bg-sidebar` · `text-sidebar-foreground` · `bg-sidebar-accent` · `border-sidebar-border`) |
  | nav 아이콘 유니코드 글리프(▦▨✎) | `lucide-react`(다른 shadcn 프리미티브가 이미 쓰는 라이브러리) |

- **예외 1건 — 토큰이 틀렸다:** `--sidebar-accent-foreground`가 `--color-nav-fg`(흐린 회색)로 매핑돼 있는데, 목업의 선택 항목 글자는 흰색이다. 토큰을 그대로 썼으면 대비가 떨어졌다 — 이 한 곳만 `text-white`를 직접 쓰고 이유를 주석에 남겼다.
  > **"있는 걸 써라"가 "토큰이 항상 맞다"는 뜻은 아니다** — 확인하고 어긋나면 근거를 남기고 벗어난다.

## D19 · 셸 프레임 버그 2건 — 스크롤과 반응형 폭

- **배경:** `ConsoleShell` 초안이 렌더는 됐지만 실사용 조건에서 두 가지가 깨져 있었다.
- **① 고정 높이(진짜 버그):** `min-h-svh`(최소 높이)를 썼더니, 콘텐츠가 길어지면 문서 전체가 늘어나 헤더·사이드바까지 스크롤에 밀려 올라갔다. 목업은 `.app{height:var(--screen-h)}` + `.content{overflow:hidden}` — 콘텐츠 영역 **안에서만** 스크롤되는 구조다. `h-svh`(고정 높이) + `overflow-hidden`으로 바꾸고, 3000px짜리 테스트 콘텐츠를 넣어 헤더·사이드바가 고정된 채 본문만 스크롤되는 것을 스크린샷으로 확인했다.
- **② 반응형 폭(재현 환경 문제였다):** 사용자가 "헤더 width가 고정 아니냐"고 반복 지적. 코드상 `<header>`는 이미 `flex`/`w-full` 계열이라 뷰포트 폭을 따라가야 했는데, 확인해보니 **재시작 전 죽은 dev 서버 포트(5174)에 열린 탭**을 보고 있었던 것으로 드러났다 — 살아있는 포트(5173)에서는 1100~1920px 전 구간에서 헤더 폭 = 뷰포트 폭이 실측(`getBoundingClientRect`)으로 일치했다. 그래도 `<header>`에 `w-full`을 명시로 박아 애매함 자체를 없앴다.
  > ②는 코드 값이 아니라 **사용자가 실제로 보고 있는 화면을 먼저 재현**하고 나서야 원인이 갈렸다. 코드만 계속 고치는 쪽으로 갔으면 시간을 낭비했을 것이다.

## D20 · 라우팅 방식을 세 번 갈아엎었다 — 도메인 파일 → 화면 파일 겸용 → 화면별 sibling 파일

- **배경:** v1 라우팅 관례(§4, 도메인마다 `routes.tsx` 하나 + `app/routes.tsx`가 손으로 import)를 v2 화면 25개로 그대로 확장하려 했다.
- **1차 — 도메인당 `routes.tsx`.** `app/routes.tsx`가 18개 도메인을 손으로 import + spread하게 됐다. "import가 너무 많다"는 지적에 `import.meta.glob('/src/features/**/routes.tsx')`로 폴더를 훑도록 바꿔, 새 도메인 추가 시 루트 파일이 안 바뀌게 했다.
- **2차 — 화면 파일이 `route`를 겸해서 export.** "폴더마다 routes.tsx를 계속 남겨야 하냐"는 재지적에, `routes.tsx` 자체를 없애고 `{X}Screen.tsx`가 컴포넌트와 `route` export를 같이 갖게 했다. 폴더 하나에 화면이 2개면(면담 목록+브리프 등) 파일을 나눠 가져야 하는 문제는 풀렸지만, 대신 `react/only-export-components` 경고가 화면 25개 전부에서 떴다 — React Fast Refresh가 그 파일에 상태 보존 핫리로드를 못 해 준다.
- **판정 질문(사용자):** "그런 상태가 뭔데, 예를 들어줘." → 폼 입력값·70분 세션 타이머·에디터 내용처럼 개발 중 저장할 때마다 날아가는 구체 사례로 답했다. 비용이 실제로 있다는 데 동의한 뒤 분리를 결정했다.
- **3차(최종) — 화면 파일 옆에 `{X}Screen.route.tsx`를 따로 둔다.** 컴포넌트 파일은 컴포넌트만 export(Fast Refresh 정상), 라우트는 화면마다 독립된 sibling 파일(공유 파일 없음 — 충돌 재발 안 함). `app/routes.tsx`는 glob 패턴만 `*.route.tsx`로 바꿔 계속 자동 수집한다.
- **기각한 대안 — 역할 폴더 단위로 한 번 더 모으기:** "그게 성능상 낫냐"는 질문에 추측으로 답하지 않고 확인했다 — `eager: true`라 glob 단계 수와 무관하게 25개 화면이 전부 같은 번들에 정적으로 들어간다(빌드 산출물의 JS 청크가 1개인 것으로 확인). 중간 집계 파일을 두면 그 파일을 여러 화면 담당자가 같이 고쳐야 해서, 애초에 화면마다 파일을 쪼갠 이유(작업 겹침 방지)가 다시 깨진다. 성능 이득 0 + 충돌 재발이라 기각했다.
  > **파일 구조를 "성능 때문"이라는 이유로 합치기 전에, 무엇이 실제로 정적 번들되는지부터 확인한다.** `eager: true` 하나가 이 질문의 답을 완전히 바꾼다 — 확인 없이 짐작으로 구조를 결정하지 않는다.

## D21 · 셸을 목업과 토큰 단위로 대조 — `--sp-N`의 N은 Tailwind의 N이 아니다

- **배경:** 오퍼레이터 화면 착수 전에 셸을 점검하다가, `01-design-checklist.md`의 「반영 완료」 표가 `ConsoleShell.tsx`에 본문 폭 상한(`mx-auto max-w-7xl`)이 있다고 적어 뒀는데 **실제 코드에는 없었다**(`grep`로 확인). 같은 표의 `TraineeShell.tsx` 행도 존재하지 않는 파일을 가리키고 있었다. 문서의 "반영 완료"를 믿고 화면을 그리기 시작했으면 25화면이 1920에서 조용히 H8을 어겼다.
- **고친 것 3건:**

  | | 목업 | 코드(전) | 코드(후) |
  |---|---|---|---|
  | 본문 폭 상한 | `.content > *{max-width:--w-app}` 1280 | **없음** | `<div class="mx-auto max-w-7xl">` |
  | 본문 좌우 여백 | `.content{padding:--sp-6}` **32px** | `p-6` **24px** | `p-8` 32px |
  | 나브 그룹 구분선 간격 | `.nav .grp{margin-top:--sp-2}` **8px** | `mt-3 pt-3` **12px** | `mt-2 pt-2` 8px |

- **★ 근본 원인 — 목업 간격 토큰의 번호가 Tailwind 번호와 다르다.**

  | 목업 | px | Tailwind 클래스 | 같은 번호를 쓰면 |
  |---|---|---|---|
  | `--sp-1` ~ `--sp-4` | 4·8·12·16 | `1`~`4` | 일치 |
  | **`--sp-5`** | **24** | **`6`** | `p-5`는 20px — **4px 부족** |
  | **`--sp-6`** | **32** | **`8`** | `p-6`은 24px — **8px 부족** |
  | **`--sp-8`** | **48** | **`12`** | `p-8`은 32px — **16px 부족** |

  **오퍼레이터 목업 5장에서만 이 구간이 78회 쓰인다**(sp-5 52 · sp-6 14 · sp-8 12). 목업의 숫자를 그대로 Tailwind 클래스 번호로 옮기는 순간 전부 좁아진다. `p-6` 사고가 정확히 이것이었다.

- **대조해서 이상 없던 것:** `--fs-*`(12·13·14·16·20·26)와 `--r-*`(6·10·14)는 `index.css`의 `--text-*`·`--radius-*`와 **값이 전부 같다.** 타이포·라운드는 목업 이름을 그대로 믿어도 된다 — **어긋나는 축은 간격 하나뿐**이다.

- **검증:** 1920에서 목업(`zoom` 환산)과 앱을 나란히 실측 — 나브 184 / 패딩 32 / 본문 1280 / 좌우 여백 226~228 대 228로 일치. 1440·1512·1728에서도 가로 스크롤 없음.

- **혼선 하나를 같이 기록한다 — 목업은 반응형이 아니다.** `.viewport{width:1920px; zoom:.6625}`라 **창을 어떻게 놓든 항상 1920 레이아웃을 축소해 보여준다.** 실제 앱은 창 폭을 따라가므로 1550px 창에서는 좌우 여백이 43px(패딩 32 + 남는 11)이 정상이고, 목업의 226px와 다른 것이 **버그가 아니다.** 목업과 눈으로 비교할 때는 브라우저를 1920으로 맞추거나 축소해서 본다.

- **재발 방지:** 목업 값을 코드로 옮길 때는 **토큰 이름이 아니라 px 값**을 기준으로 옮긴다. 그리고 문서의 「반영 완료」류 표는 **믿지 말고 `grep`으로 확인한다** — 이 표가 실제로 틀려 있었다.

## D22 · 표 컬럼 폭 — 목업 px을 베끼지 않고 측정해서 시스템으로 만든다

- **배경:** OP-03 프로젝트 목록을 그리려고 목업 컬럼 폭(`.c-nm 112` 등)을 옮기려다, 그 값들이 **실제로는 적용조차 안 되고 있다**는 것을 렌더 실측으로 발견했다.

  | | 선언 | **실제 렌더** |
  |---|---|---|
  | `operator/projects` — 6열 **전부** 폭 지정 | 112·64·180·230·124·108 (합 818) | **175·100·281·359·194·169** |
  | `manager/trainees` — 흡수 열 1개 비움 | 150·124·92·112·(없음)·124 | **선언대로** |

  `table-layout:fixed`는 **모든 열에 폭을 주면 남는 폭을 전 열에 비례 분배한다.** 체크리스트가 이미 *"마지막 열에만 폭을 주지 않는다"* 고 적어 뒀는데 이 목업이 그것을 어겼다 — 462px가 흩어져 선언값이 전부 무의미해졌다. **이미 구현된 MG-05는 규칙을 지키고 있다.**

- **또 하나 — 같은 뜻의 열이 목업마다 다르다.** `.c-nm`(주 식별자)이 **112 / 150 / 264** 세 종류였다. 체크리스트의 *"같은 뜻의 열은 어느 표에서나 같은 폭"* 이 목업에서 안 지켜지고 있었다.

- **기존 `--col-*` 11개도 시스템이 아니었다.** 8px 격자(업계 표준 8pt grid) 위에 있는 것이 **11개 중 4개**뿐이다(`chk 40 · act 96 · period 168 · name 200`). 나머지(92·108·110·116·124·132·196)는 눈대중으로 나온 값이라 근거를 댈 수 없다.

- **판단: 목업 px을 베끼지 않는다. 실제 콘텐츠를 재서 정하고, 새 CSS 토큰은 만들지 않는다.**

  실측(앱 실제 폰트, 셀 좌우 패딩 32px 포함) → 8배수 올림:

  | 뜻 | 최장 내용 | 필요폭 | 채택 | Tailwind |
  |---|---|---|---|---|
  | 짧은 라벨 | `미프` | 56 | **56** | `w-14` |
  | 상태 배지 | `준비 중` + 점 | 90 | **96** | `w-24` |
  | 날짜 | `07-21 18:00` + `5일 남음` | 104 | **128** | `w-32` |
  | 주 식별자 | `미프 5차` + 하위설명 | 160 | **160** | `w-40` |
  | 부 식별자 | `Kubernetes 기초 v1` | 136 | **160** | `w-40` |
  | **흡수 열** | 칩 3개 / `⚠ 미확정 · 후보 8건에서 3건`(176) | — | **미지정** | — |

- **왜 새 토큰을 안 만드나:** Tailwind 폭 유틸리티가 **이미 8px 격자 시스템**이다. 우리에게 없던 것은 값이 아니라 *"어떤 뜻에 어떤 폭"* 이라는 **매핑**이고, 그것은 CSS 변수가 아니라 이 표가 담는다. 토큰을 새로 정의하면 Tailwind 스케일과 `--col-*` 두 벌을 유지하게 된다.

- **경계:** **레이아웃 수치는 우리가 정하고, 문구·상태 이름·케이스 표는 목업이 계약이다.** 00-index가 *"목업이 단일 원천"* 이라고 한 것은 **케이스 표**를 가리킨 것이지 px을 가리킨 것이 아니다. 문구는 계정 열거 방지 같은 보안 규칙이 실려 있어 바꾸면 규칙이 깨진다(D21과 같은 결).

## D23 · 여러 세션이 한 폴더를 공유해 작업이 두 번 사라졌다 — 격리로 풀고, 훅은 보조로 둔다

- **배경:** 터미널 두 개(OP-04 · OP-06)가 같은 레포 폴더에서 동시에 작업하다가, 한쪽의 git 명령이 **다른 쪽의 미커밋 파일을 두 번 날렸다.** 두 번째에는 OP-06 화면 13개가 사라졌다.

### 원인 — reflog에 그대로 남아 있다

추측이 아니라 `.git`의 기록으로 갈렸다.

```
HEAD reflog   2cf9676 → 2cf9676  22:58  reset: moving to HEAD
              2cf9676 → 2cf9676  23:17  reset: moving to HEAD
stash reflog  23:10  WIP on feature/op-03-project-list          ← untracked까지 치움(부모 3개)
.git/AUTO_MERGE  23:17                                          ← 머지하다 끊은 흔적
```

**커밋 해시가 안 움직이고(`2cf9676 → 2cf9676`) 워킹트리만 버려진 것**이 reset 계열이 돌았다는 뜻이다. 그 stash 하나에는 **두 사람 작업이 같이 들어가 있었다** — `admin/` 36개 + `projects/` 6개. 한쪽이 자기 작업을 정리하려던 명령이 남의 폴더까지 쓸어 담은 것이다.

> **reflog 문구만으로 어느 명령인지는 안 갈린다.** 임시 저장소에서 확인해 보니
> `git reset --hard`와 `git merge --abort`가 **똑같이 `reset: moving to HEAD`를 남긴다.**
> 23:17 것은 `.git/AUTO_MERGE`와 시각이 같아 머지 중단 쪽이고, 22:58 것은 그 흔적이 없다 —
> **다른 파일과 대조해야 갈린다.** 처음엔 둘 다 `reset --hard`로 단정했다가 검증에서 고쳤다.

### 왜 이런 일이 가능한가 — 세션 격리가 애초에 없다

터미널·에디터·AI 세션을 여럿 띄워도 **디렉터리 한 벌, `.git` 한 벌을 물리적으로 같이 쓴다.** git에는 "이 세션이 만든 파일"이라는 개념이 없어서, 파괴적 명령은 **경로를 안 주면 워킹트리 전체**에 걸린다.

| git이 아는 저장 지점 | 보호받나 |
|---|---|
| 커밋 | ⭕ reflog가 지킨다 |
| index(`git add`) | 🔺 `clean`엔 안 지워짐 · 되돌리기엔 날아가되 **dangling blob으로 건진다** |
| stash | ⭕ (단 버리면 끝) |
| **untracked 새 파일** | ❌ **git이 존재를 모른다** |

사라진 것은 전부 마지막 칸이었다. **새 화면을 만드는 작업은 구조적으로 가장 약한 상태로 오래 머문다** — 파일을 20개 만들 때까지 커밋할 것이 없기 때문이다.

### 조치 ① 규약 — `git-convention.md` §5

**폴더 분리(`git worktree`)를 동시 작업의 기본으로 올렸다.** 원래 §5는 worktree를 *"정말 필요하면"* 이라는 최후 수단으로 적어 뒀는데 순서가 반대다 — 금지 목록은 실수를 **줄이고**, 격리는 사고를 **불가능하게** 한다.

`.git`을 공유하므로 커밋·브랜치·stash가 한 곳에 모이고, 별도 clone과 달리 서로의 브랜치가 보인다. **이 레포는 이미 목업 작업에 worktree를 쓰고 있어서**(`git worktree list`) 새로 배울 것도 없었다.

### 조치 ② 훅 — 만들었지만 🔺로 표기했다

git에는 `pre-stash`·`pre-reset` 훅이 없다. 그래서 Claude Code의 `PreToolUse`로 명령 실행 **전에** 문자열을 검사하는 `.claude/guard-worktree.sh`를 뒀다.

**만들고 나서 실제로 돌려 봤고, 그 결과를 표에 정직하게 적었다**([[D15]] — *"규칙이 있다는 것과 규칙이 작동한다는 것은 다르다"*).

| 검증 결과 | |
|---|---|
| 의도한 7개 | **전부 차단됨** ⭕ |
| **같은 파괴력인데 통과** | stash 버리기(`drop`·`clear`) · 강제 체크아웃(`-f`) · `checkout <브랜치> -- <경로>` · `reset --merge` · `rm -rf <소스폴더>` · `git -C <경로>` 우회 |
| **우회가 쉽다** | 공백을 둘 넣거나 따옴표를 끼우면 통과 |
| **오탐** | 명령문에 **단어가 들어가기만 해도** 막는다 — 문서를 `grep`하는 것, 금지 문구를 파일에 쓰는 것이 전부 걸렸다. **이 D23을 작성하는 명령 자체가 세 번 막혀서 다른 도구로 우회해야 했다** |

특히 stash 버리기가 안 막히는 것이 아프다 — **이번 사고에서 stash가 유일한 복구처였다.**

- **미커밋 결함(발견 즉시 기록):** `settings.json`은 추적되는데 `guard-worktree.sh`는 **untracked였다.** 둘이 따라온다 — ① 가드 자신이 자기가 막으려는 `clean` 명령의 삭제 대상이다 ② settings.json만 push되면 팀원 쪽엔 스크립트가 없어 `sh`가 **127**을 반환하는데, 차단은 **2**일 때만 걸리므로 **조용히 전부 통과한다.** 훅이 있다고 믿는 상태가 훅이 없는 상태보다 위험하다.

### 결론 — 계층을 뒤집지 않는다

```
1  폴더 분리(worktree)   사고를 불가능하게 한다        ← 진짜 방어선
2  규약(§5)              사람이 판단한다
3  훅                    AI 세션의 실수를 알린다        ← 방어선이 아니다
4  백업 · git add        사고가 나도 복구된다
```

**훅을 정교하게 만들어도 "폴더를 공유한다"는 전제는 안 바뀐다.** 이번에 작업이 살아난 것도 훅이 아니라 4번이었다 — 세션 스크래치패드 백업과, stash에 36개가 남아 있었던 것.

- **연결:** `docs/handbook/git-convention.md` §5·§6 · [[D2]] 규약을 강제 수단별로 배치 · [[D15]] 검사 규칙도 코드다.

## D24 · `반 · 명단`을 두 탭으로 나눴다 — 정의서와 다르게 정한 것

- **배경:** OP-06 정의서 §3이 *"**`반`과 `명단`을 한 탭으로 합친다.** 반을 만드는 것과 명단을 넣는 것은 한 흐름인데 탭이 갈리면 계속 오간다 — 반 하나를 추가하고 바로 그 반에 사람을 넣는 것이 실제 동선이다"* 라고 정해 뒀고, 그대로 구현했다.
- **판단:** **나눈다.** `/operator/admin/classes`(반) · `/operator/admin/roster`(명단).

### 왜 — 구현이 그 근거를 먼저 깨뜨렸다

정의서의 근거는 **"같이 보인다"** 였다. 그런데 만들어 보니 두 표가 한 뷰포트에 안 들어갔다 — 반 10행 + 명단 25행. 그래서 합친 상태를 유지하려고 **반 표에 내부 스크롤**(`maxHeight="19rem"`, 6행만 보임)을 넣었다.

**그 순간 합친 이유가 사라졌다.** 6행만 보이는 표는 "같이 보이는" 것이 아니고, 명단을 보다 반을 확인하려면 위로 올라가 스크롤을 또 해야 한다. 탭을 누르는 것보다 비싸다.

| | 한 탭 | 두 탭 |
|---|---|---|
| 반 표 | **6행만**(내부 스크롤) | **10반 전부** — 스크롤 0 |
| 반 → 명단 이동 | 페이지 스크롤 | 탭 클릭 1회 |
| 명단에서 체크 하나 | 반 표까지 리렌더 | 무관 |

**정의서가 틀렸다기보다, 데이터 규모가 정의 시점의 가정을 넘었다.** 10반 250명은 정의서 자신이 정한 스코프(17번)인데, 두 표를 한 화면에 담는다는 판단은 그 수를 실제로 그려 보기 전에 내려진 것이다.

- **동선은 잃지 않는다.** 탭이 나란히 있어서 반을 만든 뒤 명단으로 가는 것은 클릭 한 번이다. 정의서가 걱정한 *"계속 오간다"* 는 **탭이 서로 먼 곳에 있을 때** 생기는 비용이고, 인접 탭에는 해당하지 않는다.
- **배정 모드의 복귀 지점을 `roster`로 바꿨다.** 배정은 명단에서 시작하는 작업이라 끝나고 반 탭으로 돌아가면 방금 한 일의 결과가 안 보인다.
- **뒤늦게 메운 것 — 반 → 명단 링크.** 목업 케이스 표는 `GET /admin/classes`가 *"반별 인원 · 담당 매니저 · **명단**"* 을 준다고 했는데, 탭을 가르면서 **반에서 그 반 명단으로 가는 길이 사라졌다**(탭을 눌러도 250명 전체가 나온다). 인원 수를 링크로 만들어 `?class=`로 좁혀 연다 — 그 수가 곧 그 명단이라 누를 것이 이미 거기 있었다.
- **부수 효과 — 조회가 하나 늘었다.** 한 탭일 때는 부모가 반 목록을 한 번 조회해 두 섹션이 같이 썼는데, 탭이 갈리면서 명단 탭도 필터 드롭다운용으로 따로 부른다. **탭을 둘 다 여는 경우에만** 늘고, 각 탭은 자기가 그릴 것만 부른다 — 화면이 독립이면 조회도 독립인 것이 맞다.
- **함께 걷어낸 것:** 반 표의 `maxHeight`. 자를 이유가 없어졌다(02-layout §6은 *진짜 길어지는* 목록에만 내부 스크롤을 준다).
- **정의서를 고치지 않았다.** 화면 정의는 기획 산출물이고 이 결정은 구현 판단이라, 근거를 여기 남기고 코드 주석이 이 항목을 가리킨다 — 정의서를 조용히 고치면 *"원래 그렇게 정했다"* 가 되어 판단한 사실이 사라진다([[D1]] 경로 갱신 원칙과 같은 결).

## D25 · 감사 값(누가·언제)을 표 열로 세우지 않고 **바꾸는 자리**에 뒀다

- **배경:** 탭별 검토 중 *"기수나 반이나 편집자·편집일자 같은 게 보여야 하지 않나"* 라는 질문이 나왔다. 확인해 보니 `_/api/types.ts`에 **감사 필드가 하나도 없었다** — `createdAt`·`updatedBy` 어느 것도 없다. 그런데 OP-06 정의서 §3·§4는 *"담당 반 배정이 **기간형 이력**이라 6기 A반 담당이 누구였는지가 그 계정에 붙어 있다"* 고 이미 요구하고 있었다. **기획이 요구한 것을 계약이 표현하지 못하는 상태**였다.
- **판단:** 필드는 더한다(`ClassRoom.assignedAt` · `assignedBy`). 다만 **모든 표에 `편집자`·`편집일자` 두 열을 붙이지는 않는다.**

### 왜 — 감사 값은 "언제나 보이는 값"이 아니라 "물을 때 보는 값"이다

탭 검토 기준 ②는 *"그 열을 지우면 이 탭의 질문에 답을 못 하나"* 로 열을 고른다. 반 탭의 질문은 **어느 반에 담당이 없나 · 정원 여유가 있나**이고, `누가 마지막에 고쳤나`는 그 질문에 답하지 않는다. 여섯 탭 전부에 두 열을 붙이면 **정작 스캔해야 하는 값이 밀려난다.**

그래서 셋으로 갈랐다.

| 값 | 어디에 | 왜 |
|---|---|---|
| **담당 시작일**(`assignedAt`) | 반 표의 열 하나 | 담당은 **기수 중간에도 바뀐다** — 목록에서 실제로 눈에 걸리는 것이 *"D반이 6월에 손이 바뀌었네"* 다 |
| **배정한 사람**(`assignedBy`) | 담당 배정 모달 | 한 칸에 값 하나(E11). 이름은 **바꾸려고 연 자리**에서 필요하다 |
| 생성자·생성일 전반 | **안 만들었다** | 기획에 없고, 지금 화면 어디도 그것으로 판단하지 않는다(YAGNI). 이력 엔드포인트가 생기면 그때 그쪽으로 |

- **`⚠ 백엔드와 확인` 을 타입에 박아 뒀다**(mock-first §4) — 기획 문서에 필드로 적힌 적이 없다. 조용히 넣지도, 조용히 빼지도 않는다.

### 같이 찾은 결함 — 모달이 담당을 **지우고 있었다**

`담당 변경`을 눌러 열면 아무것도 선택돼 있지 않았다(`useState(null)`). 그 상태로 `저장`을 누르면 `managerId = null`이 나가 **담당이 해제된다.** 바꾸러 들어온 사람이 지우게 되는 자리였고, 확인 모달도 없다.

- 현재 담당을 **처음 선택으로** 넣었다. `저장`은 `picked === current`면 눌리지 않는다 — 안 바꾼 것은 저장할 것이 없다.
- `담당 비우기`는 **담당이 있을 때만** 나온다. 이제 그 버튼을 눌러야만 해제된다.
- 모달 제목 아래에 `현재 박지현 · 2026-03-02부터 · 김오퍼레이터 배정`을 적는다 — 검토 기준 ⑩의 *"지금 값이 보이나"*.
- **매니저 쪽 문은 그대로 비워 둔다.** 한 사람이 여러 반을 맡을 수 있어(`classNames: string[]`) "지금 고른 것"이 하나로 정해지지 않는다.

- **연결:** [[D24]] 반·명단 분리 · [[D22]] 목업과 우리 것의 경계 · `scratchpad/tab-review-criteria.md` ⑨·⑩

## D26 · 명단 탭 — 헤더 수가 필터를 따라 움직이고 있었다

탭 ③ 검토에서 찾은 것 여섯. 넷은 결함이고 둘은 빠진 것이다.

### 결함 — 필터 결과를 모집단인 척 썼다

`RosterPage.total`은 **필터를 적용한 수**인데 헤더와 빈 화면이 그것을 모집단으로 썼다. 검색어가 안 걸리면 이렇게 나온다.

```
명단 0명   7기 · 미배정 3
"zzz"와 맞는 사람이 없습니다
7기 0명에서 찾았습니다.      ← 0명 중에서 찾을 수는 없다
```

**반 탭은 같은 문제를 이미 풀어 뒀다** — 헤더용 수를 `getAdminCounts`로 따로 받는다. 명단은 그 규칙을 안 따르고 있었다. `RosterPage.cohortTotal`(필터 전 기수 전체)을 더해 헤더와 빈 화면이 그것을 쓰게 했다. **푸터만 `total`을 쓴다** — 거기가 필터 결과를 말하는 자리다.

### 결함 — 직접 입력이 CSV보다 무르게 검사하고 있었다

CSV는 `parseRosterCsv`가 줄 번호와 파일 안 중복을 잡는데, 직접 입력은 `checkEmail`을 한 줄씩 돌리고 있었다. 그래서 둘을 놓쳤다.

| | 쳤을 때 | 화면이 한 말 | 사실 |
|---|---|---|---|
| 1행 비움 + 2행 오타 | | `1번째 줄 오류` | 1번째 칸은 멀쩡한 빈 칸 |
| 같은 주소 두 줄 | | `✓ 유효 2명` | 서버는 1명만 등록 |

`rules.ts`에 `checkRosterRows(rows, domain)` 하나를 두고 **빈 행을 포함한 입력칸 전체**를 받게 했다 — 번호가 칸과 맞아야 해서 거르지 않고 받는다. `npm run check:admin`에 세 줄을 더해 두 경우가 다시 생기면 잡힌다.

- **라벨도 같이 고쳤다** — `파일 안에서 중복` → `같은 주소가 두 번 있음`. 직접 입력에는 파일이 없다.

### 결함 — 취소하고 다시 열면 지난 판정이 떠 있었다

`reset()`이 **성공했을 때만** 돌았다. 특히 `이미 등록된 이메일 n명`은 **서버가 그 시점에 센 수**라, 그 사이 명단이 바뀌면 틀린 수를 보여준다. 닫는 길 하나(`close`)가 비우는 일까지 하게 묶었다 — 성공도 취소도 같은 문으로 나간다.

> 마지막에 쓴 입력 방식(CSV ↔ 직접)은 **안 비운다.** 그건 지난 판정이 아니라 취향이고, 두 번째 등록도 대개 같은 방식이다.

### 빠진 것 — `미배정`으로 좁힐 수 없었다

헤더가 `미배정 3`으로 경고하는데 **그것으로 거르는 길이 없었다.** 배정 모드에 가면 볼 수 있지만 그것은 *작업*이지 *조회*가 아니다. 같은 축(어느 반이냐)이라 필터를 새로 만들지 않고 `소속 반` 드롭다운에 값으로 넣었다 — 쿼리에서만 갈린다(`scope` ↔ `classId`).

### 빠진 것 — 정렬 키가 행에 없었다

`최근 등록순`으로 정렬해 놓고 **등록일이 어디에도 없었다**(E2 — 기수 탭의 `기간` → `시작 ~ 종료`와 같은 자리). `Trainee.registeredAt`을 더해 열로 세웠다. 중도 합류를 가려내는 값이기도 하다 — 개강일이 아닌 사람이 곧 그 사람이다.

### 안 고친 것

| | 왜 |
|---|---|
| **직전 기수 열** | 재수강생을 어느 반에 넣을지 판단하는 값인데, 그 판단은 **배정 모드**에서 한다. 거기 이미 있다 |
| **교육생 제외·삭제** | 정의서 §5 액션에 없다(`생성·초대·배정·정지`의 정지는 매니저 것) |
| **쪽당 25명** | [[D24]]로 *"반 표와 같이 담는다"* 는 근거가 사라져 실측을 다시 했다 — 1512×900에 9행이라 맞추면 **28쪽**이 되어 페이저가 목록보다 길어진다. 250은 페이지로 훑을 수가 아니라 검색·필터로 좁힐 수다. 죽은 근거를 그대로 두지 않고 주석을 고쳐 적었다 |

- **연결:** [[D25]] 감사 값은 바꾸는 자리에 · [[D24]] 반·명단 분리

## D27 · 배정 모드 — 고를 때마다 표가 밀리고 있었다

### 결함 — 선택 바가 표를 58px 아래로 밀었다

벌크바를 **표 바로 위**에 조건부로 그렸다(목업 `#bulk`). 첫 체크에 표 전체가 아래로 밀린다 — 실측 58px.

**여러 명을 연달아 고르는 화면에서 이건 오조작이다.** 첫 사람을 체크하는 순간 아래 행이 전부 한 줄 높이만큼 내려오므로, 커서를 안 옮기고 두 번째를 누르면 **조준한 사람이 아니라 그 윗사람**이 체크된다. 250명을 스무 번 나눠 넣는 화면에서 스무 번 일어난다.

**위에서 자라는 요소는 아래 있는 것을 전부 민다.** 자리를 잡는 방식이 둘로 갈렸다.

| | 어디에 | 왜 |
|---|---|---|
| **배정 모드** | 모드 **바닥 고정 바** | `h-svh flex-col`의 마지막 칸이라 줄어드는 것은 `main` 높이뿐이고 내용은 위에 붙어 있다. 커밋 액션이 늘 같은 자리에 있는 것이 반복 작업에서 이득이다 |
| **명단 탭** | 툴바와 **같은 칸에 겹침** | 셸 안이라 바닥에 붙일 자리가 없다. 자리를 늘 비워 두면(`invisible`) 표가 44px 내려가 열 번째 줄과 푸터가 접힌 아래로 넘어갔다 |

명단 탭은 둘을 같은 그리드 칸(`col/row-start-1`)에 넣어 **칸 높이를 둘 중 큰 쪽으로 고정**했다 — 실측 두 층 모두 36px, 같은 top, 이동 0px. **필터가 가려지는 것은 손해가 아니다**: 필터를 바꾸면 `narrow()`가 선택을 비우므로 고른 상태에서는 어차피 못 쓰는 컨트롤이었고, `선택 해제`가 같은 줄에 있다.

### 결함 — 버튼 이름이 사용법 문장이었다

`왼쪽에서 반을 고르세요`가 **버튼 위에** 적혀 있었다. 누를 수 있는 일이 아니라 안내다(E10). 게다가 좌 레일 머리글이 이미 같은 말을 하고 있었다(D1). 버튼은 `반에 넣기` → `A반에 넣기`로 **할 일 이름**만 두고, 안내는 레일 한 곳에 남겼다.

### 정의서와 다르게 정한 것 — 순서 강제를 뺐다

§3의 상태 표는 `① 진입(0명 선택) → 레일 **흐림** + 먼저 교육생을 고르세요`다. 즉 **교육생 → 반** 순서를 강제한다.

**반대 순서로 일하는 사람이 있다.** 모드 헤더가 스스로 `A반 채우는 중`이라고 말하는 것이 그 머릿속이다 — *"A반을 채운다"* 를 먼저 정하고 사람을 담는다. 흐림은 그 사람에게 **아무 이유 없이 막힌 벽**이다.

- 순서를 강제해서 막을 사고가 없다 — 넣기 버튼은 **둘 다 정해져야** 켜지고, 그 전까지 잘못될 수 있는 것이 없다.
- 흐림을 걷어내니 상태 하나가 사라졌다(`idle`). 레일 머리글도 `채울 반을 고르세요` 하나로 고정된다.
- C1(*못 하는 일을 흐리게 두지 않는다*)과도 이제 어긋나지 않는다. 예전 주석은 *"권한이 아니라 순서라 다르다"* 고 변호했는데, **막을 이유가 없으면 그 변호도 필요 없다.**

### 쪽당 인원 25 → 10

[[D26]]에서 25의 근거가 [[D24]]로 이미 죽어 있었음을 적었고, 여기서 실제로 바꿨다. 1512×900 실측으로 9~10행이 들어가 02-layout §6(*한 쪽이 뷰포트에 들어가는 행 수로 자른다*)에 맞는다.

> 처음엔 페이저가 36px 모자라 접힌 아래로 넘어갔는데, [[D28]]에서 이메일을 이름 밑에서 제 열로 빼면서 **행이 57px → 53px**이 되어 해결됐다 — 열 줄과 페이저가 다 들어온다(마지막 행 바닥 846px · 페이저 바닥 891px).

- **연결:** [[D26]] 명단 탭 검토 · [[D24]] 반·명단 분리

## D28 · 배정 모드 — 좌우를 뒤집고, 넣기 버튼을 반 옆으로 옮겼다

### 이메일을 이름 밑에서 제 열로 뺐다 (명단·배정 모드·앞으로 매니저도)

한 칸에 이름과 이메일을 쌓고 있었다(`이름` / 작은 글씨 `이메일`). 셋이 걸린다.

| | |
|---|---|
| **한 칸에 값 하나**(E11) | 두 값이 한 칸에 있으면 정렬·복사·훑기 어느 것도 열 단위로 안 된다 |
| **이메일로 훑을 수가 없다** | 세로로 안 맞으니 눈이 매 줄마다 이름을 건너뛰어야 한다. 계정 문제를 볼 때 실제로 훑는 것이 그 열이다 |
| **행이 두 줄이 된다** | 한 화면에 들어가는 사람이 절반이 된다 |

**부수 효과가 컸다** — 행 높이가 57px → 53px이 되면서 명단 탭의 `10행 + 페이저`가 한 뷰포트에 다 들어왔다([[D26]]의 미해결 항목이 여기서 풀렸다). 열 폭은 실측: `교육생 128 · 이메일 224`, 잘림 없음.

### 좌우를 뒤집었다 — 정의서 §3의 `좌 반 레일 · 우 명단`을 바꿨다

**일의 순서와 눈의 순서가 반대였다.** 하는 일은 `누구를(명단) → 어디에(반) → 넣기`인데 명단이 오른쪽, 반이 왼쪽이라 **오른쪽에서 고르고 왼쪽으로 되돌아왔다가** 다시 화면 바닥으로 내려가야 했다. 지금은 **왼쪽에서 오른쪽으로 한 번**에 끝난다.

LTR에서 **내용은 왼쪽, 조작 패널은 오른쪽**이 일반적인 배치이기도 하다(편집기의 인스펙터, 메일의 수신함↔작성 패널). 명단은 훑는 내용이고 반 패널은 조작이다.

### 넣기 버튼을 화면 바닥에서 반 패널 안으로

[[D27]]에서 표가 밀리는 것을 고치려고 버튼을 **모드 바닥 가로 바**로 내렸는데, 실사용 평가가 *"너무 아래라 잘 안 보인다 · 그냥 빈 것 같다"* 였다. 재보니 근거가 있다.

- 폭 1200px짜리 바에 컨트롤이 셋뿐 — **대부분이 빈 공간**이라 바 자체가 구분선처럼 읽혔다.
- 버튼이 **y=880**. 눈이 머무는 곳(표 중앙·반 목록)에서 300px 넘게 떨어져 있었다.

**버튼은 그것이 무엇에 대한 것인지 바로 옆에 있어야 한다.** `A반에 넣기`는 A반 밑에 있을 때 읽힌다. 반 패널 안, 목록 바로 아래로 옮기니 **y=508**이 됐다.

| | 전 | 후 |
|---|---|---|
| 넣기 버튼 | y=880 (바닥 가로 바) | **y=508** (반 패널 안) |
| 고른 수 · 선택 해제 | 바닥 바 | **명단 툴바** — 고른 대상이 거기 있으므로 |
| 표 밀림 | 0px | 0px (유지) |

- **반 패널을 `sticky`로 붙였다** — 명단을 훑어도 넣을 곳과 넣기가 따라온다. 반 열 개라 패널이 뷰포트보다 짧아 자체 스크롤이 필요 없다.
- **미리보기를 한 번만 그린다.** 목록 행의 `22 → 24 / 25`와 버튼 위 문구가 같은 값이었다(D1). 목록은 **고를 때 필요한 것**(지금 인원·정원)만 말하고, `22 → 24 / 25 · 1자리 남음`은 **누르기 직전 한 자리**에 둔다.
- **못 누를 때 무엇이 빠졌는지 버튼 밑에 적는다** — `왼쪽에서 교육생을 고르세요` / `넣을 반을 고르세요`. 버튼 이름에 사용법을 쓰지 않는 대신(E10) 이 줄이 그 일을 한다. 비활성 버튼만 두면 왜 안 눌리는지 알 수 없다.
- **`채울 반을 고르세요`가 안 보인다는 지적도 여기서 풀린다** — 흐린 본문 한 줄이 버튼들 위에 떠 있었다. 지금은 테두리·배경이 있는 **패널의 제목**이라 덩어리로 먼저 읽힌다.

- **연결:** [[D27]] 표 밀림 · [[D26]] 명단 탭 검토

## D29 · 배정 플로우를 도메인부터 다시 봤다 — 목업을 베낀 자리 둘, 계약이 못 하던 일 하나

*"로직이 겉핥기"* 라는 지적을 받고 명단 → 배정 모드 전체를 다시 훑었다. 화면 문제가 아니라 **도메인 문제**가 셋 나왔다.

### ① 되돌리기가 되돌리지 않았다 — 계약이 그럴 수 없었다

`undoAssign`은 **무조건 `미배정`으로** 보냈다.

```
B반 사람을 A반으로 옮김 → 되돌리기 → 미배정   ← B반이 아니다
```

계약이 원인이다. `AssignResult`가 `traineeIds`만 들고 있어서 **옮기기 전 소속이 어디에도 없었다** — 되돌릴 정보 자체가 없는데 버튼은 `되돌리기`라고 쓰여 있었다. `즉시 실행 + 되돌리기`(§3)가 확인 모달을 안 세우는 근거인데, 그 되돌리기가 데이터를 지우고 있었다.

`AssignResult.moved: MovedTrainee[]`로 바꿔 **사람마다 `fromClassId`·`fromClassName`을 들고 온다.** 되돌리기는 그 자리로 돌려놓는다. 배너도 어디서 왔는지 적는다(`옮겨온 사람 B반 → 강건우`) — 어디로 돌아가는지 모르면 되돌리기를 못 누른다.

> 이름에 조사를 붙이지 않는다. `강건우은`처럼 받침 유무를 틀리게 되고(은/는·이/가), 이름은 규칙으로 처리할 값이 아니다.

### ② 옮기면 반 인원 합이 부풀었다

`assignClass`가 **받는 반만 더하고 떠나온 반에서 빼지 않았다.** 배정 모드는 `범위=전체`로 옮기기를 지원하는데(반 통폐합, §3) 짝이 안 맞았다.

| | 전 | 후 |
|---|---|---|
| B반 사람 1명을 F반으로 | B 25 · F 23 → **합 248** | B **24** · F 23 → 합 247 |

같은 반에 다시 넣는 것은 **아무 일도 아닌 것으로** 처리한다 — 세면 인원이 부풀고 되돌릴 것도 없는 되돌리기가 남는다.

### ③ 전원 배정이 끝나면 사람을 옮길 수 없었다

`finished = scope === 'UNASSIGNED' && unassigned === 0`이 **표 전체를 완료 화면으로 대체**한다. 그런데 명단의 `반 변경`도 이 모드로 온다 — 미배정이 0인 기수에서 누르면 **`모두 반에 들어갔습니다`만 뜨고 아무것도 못 했다.** 기수 중반 이후에는 반 이동이 불가능했다는 뜻이다.

**문이 둘이고 하는 일이 다르다는 것을 화면이 몰랐다.** 이제 안다.

| | 채우기 | 옮기기 |
|---|---|---|
| 들어오는 길 | `미배정 배정하기 →` | 명단에서 고르고 `반 배정 →` · 행의 `반 변경` |
| 초기 범위 | `미배정만` | **`전체`** — 골라 온 사람이 이미 반에 있을 수 있다 |
| 완료 상태 | 미배정 0이면 끝 | **없다** — 옮기러 왔지 채우러 온 게 아니다 |

범위가 `미배정만`으로 고정이던 동안, 배정된 사람을 골라 오면 `1명 선택`이라고 써 있는데 **그 사람이 표에 없었다.**

### ④ 넣은 뒤 고른 반이 풀렸다

이 화면이 전제하는 일이 *"A반을 25명까지 채운다"* 인데(헤더도 `A반 채우는 중`) 넣을 때마다 반 선택이 초기화됐다. 스무 번 넣으면 스무 번 더 누른다. **고른 반을 남긴다** — 바꿀 때는 다른 반을 누르면 한 번에 바뀐다.

### ⑤ 목업에 있지만 뺀 열 둘

| | 왜 뺐나 |
|---|---|
| 명단 `소속 반`의 **`· 3팀`** | 팀은 **프로젝트마다 재편성되는 회차의 속성**이다(00-index `학생 → 팀`, 01-design-checklist). **매니저용 명단(MG-05)은 같은 이유로 팀 열을 이미 뺐다** — OP-06만 목업을 베껴 규칙을 어기고 있었다. 한 기수에 회차가 여럿이면 회차 이름 없는 `3팀`은 무엇도 안 가리킨다. 게다가 `반`과 한 칸에 있어 두 축이 한 값처럼 읽혔다(E11) |
| 배정 모드 **`직전 기수`** | **이 부트캠프에 재수강생이 없다**(기획 확인 2026-08-04). 열의 존재 이유 자체가 없었다 — 목업에 있다고 그대로 옮긴 자리다. 값이 있는 척하려고 목 데이터도 31명에 한 명씩 지어내고 있었다 |

`Trainee`에서 `team`·`previousCohort` 필드도 지웠다 — 화면이 안 읽는 필드를 계약에 두면 백엔드가 만들어야 할 것으로 읽힌다.

- **연결:** [[D28]] 좌우 배치 · [[D27]] 표 밀림 · [[D26]] 명단 탭

## D30 · 기획 확인 8건 (2026-08-04) — 지어내던 자리를 답으로 바꿨다

탭별 검토에서 *"기획에 없어서 화면이 정하고 있던 것"* 을 모아 한 번에 물었다. **답이 곧 규칙이고, 아래를 근거 없이 되돌리지 않는다.**

| | 물은 것 | 답 |
|---|---|---|
| ① | 중도 이탈자 | **비활성 처리 + 사유·일자** — 명단에는 남고 계정만 막힌다 |
| ② | 반 수정·삭제 | **기수 시작일이 지나면 잠근다.** 그 전에는 정원·이름 수정과 삭제 가능 |
| ③ | 담당 이력 노출 | **지금 구간만** — `assignedAt`/`assignedBy` 두 필드로 확정 |
| ④ | 기수 스위처 | **나중에** — 탭별 검토를 먼저 끝낸다 |
| ⑤ | 한 반에 매니저 여럿 | **아니다. 1반 1매니저** |
| ⑥ | 명단 오등록 수정 | **이름만.** 이메일은 계정의 신분이라 못 고친다 |
| ⑦ | 비용 `계약 예산` 출처 | **계약에 박힌 값** — 서버가 내려주고 화면은 읽기만 |
| ⑧ | 재수강생 | **없다** — `previousCohort`는 존재 이유가 없었다([[D29]]) |

### 이 답들이 코드에 남기는 것

- **`Trainee.statusNote`가 생긴다**(①). 매니저의 `정지 · 퇴사 2026-05-02`와 같은 모양이다 — MG-05가 *"초대 대기·비활성 행은 흐리게 + **사유·일자**를 쓰되 액션을 두지 않는다. 초대·정지는 **오퍼레이터 소관**"* 이라고 이미 요구하고 있었는데, 그 액션이 어느 오퍼레이터 화면에도 없었다.
- **반 편집이 날짜에 걸린다**(②). `오늘 < 기수.startAt`이면 열리고 아니면 닫힌다 — **날짜 하나로 갈려 예외가 없다.** 잠긴 뒤에도 담당 매니저 변경과 반 배정은 그대로 된다(그건 운영 중에 계속 일어나는 일이다).
- **이메일은 못 고친다**(⑥). 오타 주소는 **비활성 처리 후 재등록**이 정해진 길이다 — 초대가 이미 그 주소로 나갔으므로 주소를 바꾸면 다른 사람이 된다.
- **⑤가 `needsManager`의 전제를 확정한다.** `managerId === null` 하나로 판정하는 것이 맞다([[D26]] rules.ts).

> ④는 **미룬 것이지 아닌 것이 아니다.** `_/cohortScope.ts`의 `COHORT_ID = '7'` 상수가 그 자리를 표시하고 있다.

- **연결:** [[D29]] 배정 플로우 · [[D25]] 감사 값

## D31 · D30의 답 셋을 구현했다 (①·②·⑥)

### ① 중도 이탈 — 비활성 처리

`Trainee.statusNote` + `setTraineeStatus(id, status, reason)`. 매니저의 `정지 · 퇴사 2026-05-02`와 같은 모양이다.

- **사유가 필수다.** 없으면 저장이 안 되고 서버도 같은 규칙을 검증한다 — MG-05가 *"사유·일자를 쓴다"* 고 요구하는데 사유를 안 받으면 매니저 화면에 쓸 것이 없다.
- **일자는 서버가 찍는다.** 화면이 만들면 사용자 시계에 따라 달라진다.
- **사유를 목록으로 만들지 않았다.** 기획에 이탈 사유 분류가 없다(mock-first §5). 정해지면 그때 드롭다운이 된다.
- **상태가 다르면 버튼이 다르다**(C1) — 비활성 행에는 `반 변경`이 사라지고 `다시 활성`만 남는다. 되살리면 사유는 지운다(지난 기록은 지금 상태가 아니다).

> ⚠ **선택지 설명에 쓴 것을 하나 정정한다.** 물을 때 *"반 인원에서 빠진다"* 고 적었는데 **틀렸다.** MG-05 헤더가 `25명 · 활성 23 · 초대 대기 1 · 비활성 1`이라 **비활성이 반 인원 안에 세어져 있다** — 반에서 빼면 담당 매니저가 그만둔 학생을 아예 못 본다. **반을 유지하는 쪽으로 구현했다.**

### ② 반 수정·삭제 — 개강 전에만

`rules.canEditClasses(cohortStartAt, today)` **한 줄이 전부다.** 날짜 하나로 갈려 예외가 없고, `npm run check:admin`이 경계 셋을 지킨다(전날 열림 · **당일 잠김** · 이후 잠김).

- 반 탭이 `getCohort(COHORT_ID)`로 시작일을 받는다. 목록을 통째로 받아 찾지 않는다.
- **잠긴 뒤에도 담당 변경은 남는다** — 매니저 퇴사·교체는 운영 중에 계속 일어나는 일이라 잠금 규칙이 다르다.
- **왜 버튼이 없는지 헤더 한 줄이 답한다**(`· 개강 후에는 반 구성을 바꿀 수 없습니다`). 버튼을 흐리게 두지 않으니(C1) 그 자리를 설명이 대신한다 — 안 그러면 *"고치는 데가 어디지"* 를 찾아 헤맨다.
- **이름을 바꾸면 명단의 `소속 반`도 같이 바뀐다**(D1 — 한 사실은 한 곳에서).
- **삭제하면 그 반 사람들은 미배정으로 돌아간다.** 명단에서 지우는 것이 아니다 — 실측: F반 22명 삭제 → 미배정 3 → **25**, 명단 250명 그대로.

### ⑥ 이름 수정 — 이메일은 못 고친다

모달에 **입력칸이 하나뿐이고**(실측 확인) 이메일은 읽기로만 보여준다. 고칠 수 있는 것처럼 보이면 안 되지만, 무엇을 고치는 중인지 알려면 화면에 있어야 한다.

> 오타 주소의 정해진 길은 **비활성 처리 후 재등록**이다 — ①이 없었으면 ⑥의 답이 성립하지 않는다. 두 답이 짝이다.

### 검증에서 얻은 것

- **`browser_navigate`는 전체 새로고침이라 목 상태가 초기화된다.** 삭제 결과를 다른 탭에서 확인하려다 값이 되돌아가 잠깐 헷갈렸다 — **탭 클릭(클라이언트 이동)으로 확인해야** 같은 세션이다.
- 목의 모든 기수가 이미 시작해서 **개강 전 상태에 닿을 수 없었다.** `MOCK_NOW`를 잠깐 `2026-02-20`으로 돌려 열린 상태를 확인하고 되돌렸다 — 데이터를 지어내 UI를 억지로 띄우지 않는다.
- ⚠ **행 액션이 셋이 됐다**(반 변경 · 이름 수정 · 비활성). 매니저 탭은 더 많다 — **넘침 메뉴로 접을지는 거기서 정한다.** `DropdownMenu`가 디자인 시스템에 있는데 레포에서 아직 아무도 안 쓰므로, 첫 사용처는 액션이 가장 많은 자리에서 고르는 것이 맞다.

- **연결:** [[D30]] 기획 확인 8건

## D32 · 표에서 두 줄 쌓기를 걷어내고 `비고` 열로 (+ 정지가 반을 안 놓던 버그)

### 규칙 — **한 칸에 두 줄을 쌓지 않는다. 값이 더 있으면 열을 만든다**

`이름`/작은 글씨 `이메일`, `상태 배지`/작은 글씨 `사유·일자`처럼 쌓아 두고 있었다. 셋이 걸린다.

| | |
|---|---|
| **행 높이가 갈린다** | 값이 있는 행만 두 줄이 되어 표가 들쭉날쭉했다 — 매니저 탭 실측 **57 ↔ 60px** |
| **그 값으로 훑을 수가 없다** | 세로로 안 맞으니 눈이 매 줄마다 위 줄을 건너뛰어야 한다 |
| **한 칸에 값 하나**(E11) | 정렬·복사·잘림 처리가 열 단위로 안 걸린다 |

명단·매니저 둘 다 **이메일 열**과 **비고 열**로 갈랐다. 비고는 대부분 비지만 — **드문 값이야말로 제자리가 있어야 눈에 걸린다.**

> **버튼이 없는 행도 같은 높이여야 한다.** 액션을 지우고 나니 그 행만 43px이 됐다(다른 행 53px). 높이를 버튼이 아니라 **칸이** 정하게 `h-[30px]` 상자를 뒀다 — 두 탭 모두 실측 53px 균일.

### 명단에서 걷어낸 것

| | 왜 |
|---|---|
| **행마다 `반 변경`** | 반 이동은 여러 명을 한 번에 하는 일이라 벌크바의 `반 배정 →`이 맡는다 — 행마다 두면 250행에 버튼이 250개 생기고 정작 훑을 값이 밀린다 |
| **`이름 수정`** | 필요 없다는 판단(D30-⑥ 철회). `updateTraineeName`과 모달 파일까지 지웠다 — 화면이 안 쓰는 API를 계약에 두면 백엔드가 만들어야 할 것으로 읽는다 |
| **`다시 활성`** | **비활성은 되돌리지 않는다**(기획 확인). `setTraineeStatus(id, status, reason)`를 **`deactivateTrainee(id, reason)`** 으로 좁혔다 — 되돌릴 수 없으면 인자로도 그것을 표현하지 않는다. 모달 문구도 *"되돌릴 수 있습니다"* → **"다시 활성으로 되돌릴 수 없습니다"** 로 바꿨다(빨간 글씨) |
| `미배정 배정하기 →` | → **`반 배정하기 →`** |

### 매니저 탭 — 정지가 담당 반을 놓지 않고 있었다

**확인 모달은 *"담당 반에서 빠집니다"* 라고 약속하는데 코드는 상태만 바꿨다.** 실측으로 재현했다.

```
박지현(7기 A반) 정지 →  매니저 행: 7기 · A반 · 담당 인원 25  (그대로)
                      반 탭:   A반 · 박지현              (그대로)
                      담당 없음 경고: 1                  (안 늘어남)
```

**퇴사한 사람이 반을 붙들고 있는데 아무 데도 신호가 없다.** `담당 없음` 경고와 OP-01 `조치 필요`가 막으려던 바로 그 상황(*"그 반 학생의 면담·독촉을 아무도 처리하지 않는다"*)을 **정지 기능이 만들고 있었다.**

- `releaseClasses(managerId)`를 두고 **정지·초대 취소가 같이 쓴다.** 고친 뒤 실측: `담당 없음 1 → 2`, A반 액션이 `담당 변경 → 담당 배정`.
- **초대 취소도 같은 병이었다.** 초대할 때 반을 맡길 수 있는데(선택) 계정만 지워서 **반에 없는 사람의 id가 남았다.**
- **초대 대기 매니저가 반을 맡으면 한 행이 서로 다른 말을 했다.** `managerName`이 null이라 화면은 빨간 `담당 없음`을 띄우는데 `needsManager`는 id가 있어 false다(D1). 이름이 생길 때까지 **주소가 그 자리를 대신한다.**

## D33 · MG-08 "다시 보기 대상" 판정 기준 정정 — `reach < 4` → `reach < 2`

- **배경:** 사용자가 결과 탭을 실제로 훑어보다 다시 보기 대상이 과다 선정된 것을 발견 — "2단 도달 시엔 대상이 아니고, 2단 도달에 실패했을 때만 대상"이라고 정정 지시. 코드는 `concept()`에서 `retryTarget: inCode && reach < 4`로, **4단(전부 자력 도달)이 아니면 전부 대상**이 되고 있었다.
- **검산:** mif-3 mock 데이터(9명) 기준 이전 규칙으론 8/9명이 대상이었다. `reach < 2`로 고치니 실제로 2단 미만인 3명(hitl 0~1단 그룹)만 남는다 — "너무 많이 걸린다"던 증상과 정확히 맞물린다.
- **판단:** `concept()`의 `retryTarget` 계산식을 `inCode && reach < 2`로 수정(`mockData.ts`). 기존에 수동으로 `retryTarget: true`를 박아둔 항목(t1 hitl, reachLevel 1)은 새 기준에서도 그대로 참이라 변경 불필요.
- **⚠ 발견한 문서 충돌:** `docs/plan/v1/definition/12-scoring-rules.md:361` "3문제 중 **2단 이하**인 학습목표만 재시험"은 경계값이 다르다(2단 자체도 포함). 이번 지시는 2단 도달을 제외하므로 v1 문서와 어긋난다 — v1은 구버전 스펙이라 이번 사용자 지시를 최신 기준으로 보고 반영했다. v1 문서 자체는 손대지 않았다(범위 밖).
- **목적·효과:** 매니저가 결과 탭에서 실제로 도움이 필요한 인원만 다시 보기 대상으로 보게 된다 — 과다 선정으로 "누구를 챙겨야 하는지"가 묻히는 문제를 없앤다.

## D34 · 다시 보기 발송 — 기한 수동 지정 + 발송 현황 표

- **배경:** 두 건을 사용자가 연달아 지시. ① "다시 보기 날짜 시간을 매니저가 직접 정할수 있게 해줘" — 전엔 `+3일 18:00` 계산값을 문구로만 보여줬다. ② 발송 직후 "다시보기를 보내도 이전 창에 변화가 없어" — 발송해도 좌측 요약이 카운트 문구 하나(`N명 · 아직 보내지 않음`)로만 바뀌어 **누구에게 보냈는지 · 기한이 언제인지 · 응시했는지**가 전혀 안 보였다.
- **판단:**
  - `RetrySendDialog.tsx`에 날짜(Popover+Calendar) + `<input type="time">` 필드를 추가했다. 기본값은 그대로 +3일 18:00, 매니저가 자유롭게 바꿀 수 있다. 지난 날짜는 고를 수 없게 막았다(`disabled: [{ before: TODAY }]`).
  - 이 조합은 `SchedulePicker.tsx`(operator/projects, 팀장님 소유)가 이미 쓰는 패턴과 같다 — 교차 import가 막혀 있고(oxlint) 파일 소유권도 달라(CLAUDE.md §7) 가져다 쓸 수 없어 필요한 만큼만 복제했다.
  - `mockData.ts` `PersonResult`에 `retryDueAt`(발송 시 고른 기한)·`retryTakenAt`(다시 응시를 실제로 마친 시각) 두 필드를 추가. `sendRetry(id, traineeIds, dueAt)`가 발송 대상 전원에 `retrySentAt`과 함께 `retryDueAt`을 저장한다.
  - `ResultTab.tsx` "다시 보기" 영역을 카운트 문구 하나에서 **표**로 바꿨다 — 발송된 사람만 이름·기한·응시 여부(배지) 3열로, 미발송 인원은 그 아래 발송 버튼과 함께 남는다.
  - mif-3 mock에 시연용 상태 3종을 시드했다(임채원=발송+응시완료, 한소율=발송+미응시, 김민준=미발송) — 브라우저로 세 상태 전부 렌더 확인.
- **검산:** 브라우저에서 직접 미발송 대상(김민준)에게 발송 → 모달이 닫히자마자 표에 "김민준 · 2026-07-21(화) 18:00 · 미응시"가 추가되고 상단 카운트가 "발송 3명 · 미발송 0명"으로 바뀌며 발송 버튼이 사라짐(더 보낼 대상 없음)을 확인 — "화면이 안 바뀐다"던 증상이 재현되지 않는다.
- **목적·효과:** 매니저가 기한을 상황에 맞게 조정할 수 있고, 발송 후에도 누가 언제까지 무엇을 해야 하는지·했는지를 그 자리에서 바로 확인한다.

## D35 · 결과 탭 이름·카드 정정 + 제출 현황 표 실측 오버플로 수정

- **배경:** 사용자가 결과 탭 스크린샷을 보고 4건 지적. ①"반 종합"이라 적혀 있는데 반 관련 통계가 없다(이 프로젝트는 A·B·C반이 섞여 있어 원래 "반" 단위가 아니었다). ②"막힌 개념 2개 이상"이 뭘 뜻하는지, "다시 보기 대상"과 무슨 관계인지 불분명. ③"D-19h" 표기가 다른 항목("D-2일" 등)과 단위가 안 맞고 실제로 안 쓰는 말투다. ④ 팀 행의 "✓2·✗1"(요구사항 충족)이 그 아래 개인 행에도 토씨 하나 안 틀리고 반복돼 있는데 뭘 가리키는지 안 밝혀져 있었다.
- **판단:**
  - `ResultTab.tsx`: "반 종합" → **"프로젝트 종합"**(사이드바 탭·헤더 전부). 반별 통계를 새로 넣는 대신 이름을 실제 내용에 맞췄다 — 반 × 개념 분포는 이미 MG-02 히트맵 소관으로 분리돼 있어 여기 넣으면 책임이 겹친다.
  - "막힌 개념 2개 이상" 카드 삭제 → **"불합격 인원"**(`retryTargetCount`, 개념 1개 이상 2단 미달)으로 교체. **"다시 보기 대상"**은 그 값에 **"마감이 지나도록 응시를 시작도 안 한 인원"**(`notStartedCount`, `AttendanceStatus.NOT_STARTED` 집계)을 더한 값으로 재정의(사용자 지시) — `mockData.ts`의 `ProjectResult`에 `notStartedCount` 필드 신규, `stuckTwoPlusCount`는 이제 아무 데서도 안 써서 제거. `BLOCKED`(코드 분석이 안 끝나 응시 자체가 안 열린 상태)는 여기 안 센다 — 그건 독촉 대상이지 다시 보기 대상이 아니다(타입 주석에 이미 있던 구분).
  - `mockData.ts` `MIF3_ATTENDANCE`의 `'D-19h'` → **`'19시간 남음'`**. 다른 항목은 전부 "D-N일"(일 단위)인데 이것만 시간 단위를 "D-"에 억지로 붙인 비표준 표기였다.
  - `SubmissionTab.tsx` 개인 행의 요구사항(✓·✗) 셀을 비웠다 — 요구사항은 "같은 코드를 쓰는 팀 단위" 값이라 팀 행 한 곳에만 있으면 충분하다(정의서 §3에도 이미 그렇게 정의돼 있었는데 구현이 개인 행에도 중복 출력하고 있었다).
  - **부수 발견·수정:** "19시간 남음"으로 바꾸자 제출 현황 표가 실측 712px vs 컨테이너 669px로 가로 오버플로가 났다(주범은 사실 레포 URL 열 — 이미 전부터 넘치고 있었는데 스크롤로 가려져 있었다). "레포·제출자" 셀을 URL만 말줄임(`truncate`, `title` 툴팁)되게 하고 제출자 이름은 항상 끝까지 보이도록 flex로 분리해 669px 안에 맞췄다(D22 기준 — 실측 후 조정, 지어내지 않음).
- **검산:** 브라우저에서 `container.scrollWidth > clientWidth`를 직접 측정 — 수정 전 712/669(오버플로), 수정 후 669/669(오버플로 없음). "프로젝트 종합"·"불합격 인원 3명" 렌더 확인, 리포트 발행 후 "다시 보기 대상 6명(불합격 3+미응시 3)"·캡션 "불합격 + 미응시" 확인. 제출 현황 표에서 이서준 행 "응시 가능 19시간 남음" 전체 노출, 팀 행에만 "✓·✗"가 있고 개인 행엔 빈칸인 것 확인.
- **목적·효과:** 카드 이름과 값이 실제로 보여주는 내용과 일치하고, 다시 보기 대상이 "응시했지만 떨어진 사람 + 아예 안 본 사람"을 모두 담아 매니저가 누락 없이 챙길 수 있다.

## D36 · 제출 현황 컬럼 재구성 + 발행 게이팅 2건 (마감 전 경고·다시 보기 발행 후 전용)

- **배경:** 사용자가 6건을 한 번에 지적. ①"D-2일"이 다른 표기와 안 맞고 "응시가능 D-2일"이 라벨·값 구분이 안 됨. ②다시 보기 발송이 리포트 발행 전에도 가능해서 "다시 보기 대상" 카드가 "발행 후 정해짐"이라 숨겨둔 숫자로 보낼 수 있는 모순. ③리포트 발행에 마감 전 클릭을 막는 확인이 없음. ④"제출 · 분석"·"응시" 헤더가 서로 다른 두 축(제출 시각/분석 결과, 응시 상태)을 한 헤더에 욱여넣고 있었음. ⑤분석완료 배지가 팀 이름 칸에 얹혀 있어 어느 열 소속인지 불분명. ⑥제출 열의 날짜·시간이 이름 칸에 텍스트로 붙어 있어 왼쪽으로 쏠려 보이고, 팀 행만 있고 개인 행엔 그 사람이 실제로 응시를 마친 시각이 없었음.
- **판단:**
  - `mockData.ts` `MIF3_ATTENDANCE`의 `'D-2일'`류 → `'D-2'`(일 접미사 제거), `SubmissionTab.tsx`의 `AttendanceCell`이 라벨과 기한을 `"/"`로 갈라 그린다("응시 가능 / D-2", "응시 가능 / 19시간 남음").
  - `ResultTab.tsx`의 "다시 보기" 섹션(표+발송 버튼) 전체를 `result.reportPublished`로 게이팅. 발행 전엔 "다시 보기는 리포트 발행 후에 보낼 수 있어요" 안내만 보인다.
  - `mockData.ts`에 `deadlinePassed(dueAt, now)` 헬퍼 신규(마감 없으면 항상 true). `ProjectDetailScreen.tsx`가 `project.dueAt`을 `ResultTab`에 내려주고, "리포트 발행" 클릭 시 마감이 안 지났으면 바로 발행하지 않고 `AlertDialog`로 확인("그래도 발행"을 눌러야 `publishReport` 호출)받는다. 마감이 지났으면(대부분) 평소처럼 즉시 발행.
  - `SubmissionTab.tsx` 헤더 "제출 · 분석"→**"제출"**, "응시"→**"분석 · 응시"**. 팀 행의 이름 칸에 colSpan으로 얹혀 있던 제출 시각·분석 배지를 각자 칸으로 뺐다 — "제출" 열은 팀 행=제출 시각·개인 행=그 사람의 응시 완료 시각(`PersonAttendance.completedAt` 신규 필드), "분석 · 응시" 열은 팀 행=분석 배지·개인 행=응시 상태.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier·vite build 전부 통과. 브라우저에서 ①"응시 가능 / D-2"·"응시 가능 / 19시간 남음" 렌더 ②발행 전엔 다시 보기 섹션이 안내 문구로만 보이고 발행 버튼/표가 없음 ③mif-3 dueAt을 임시로 미래로 바꿔(`2026-07-25`) "제출 마감 전인데 발행할까요?" 경고 팝업 확인 → "그래도 발행" 눌러 정상 발행 → dueAt 원복(`2026-07-16`) ④제출 현황 표에서 팀 행=제출 시각, 개인 행=응시 완료 시각(예: 강지우 `2026-07-14 10:20`), 분석완료 배지가 "분석 · 응시" 열로 이동 확인.
- **목적·효과:** 표기가 일관되고, 발행이라는 확정 행위 앞뒤로 되돌리기 힘든 실수(마감 전 발행·미공개 숫자로 발송)를 한 번 더 막는다.
- **확인 문구가 이제 사실과 맞는다** — 어느 반이 비는지 이름을 대고(`A반이(가) 담당 없음이 됩니다`), *"다시 활성화해도 담당 반은 자동으로 돌아오지 않습니다"* 를 적는다. 그 사이 다른 사람이 맡았을 수 있어 되돌리는 것은 배정이지 상태가 아니다.

- **연결:** [[D31]] D30 구현 · [[D29]] 이메일 열 분리 · [[D26]] 명단 탭

## D37 · "다시 보기" → "재응시" 용어 통일 + 발송 버튼 상시 노출 + `retryStatus()` 단일 원천

- **배경:** 사용자가 결과 탭·재응시 발송 모달 스크린샷을 보고 3건 지적. ①"다시 보기"라는 말을 전부 "재응시"로 바꿔달라(카드 라벨 "다시 보기 대상"→"재응시 대상", 섹션·버튼 "다시 보기 발송"→"재응시 발송"). ②전원에게 발송을 마치면 `ResultTab.tsx`의 발송 버튼 자체가 사라져서 모달을 다시 열 방법이 없다 — 없어지지 말고 눌러서 열 수 있어야 한다. ③`RetrySendDialog.tsx`에서 발송된 사람에게 하드코딩해 붙이던 "이미 다시 봤어요"가 사실과 다르다 — 그건 **발송됐다**는 뜻이지 **실제로 다시 봤다**는 뜻이 아니다. 이전 화면(`ResultTab`)의 응시 여부 3분류(재응시 가능/재응시 발송 완료/재응시 완료)와 같게 맞춰달라.
- **판단:**
  - 용어 치환은 **사용자에게 보이는 문구만** 대상으로 했다 — `retryTarget`·`retrySentAt`·`retryTakenAt`·`sendRetry`·`retryConceptNames` 같은 코드 식별자는 바꾸지 않았다(의미가 같고 전면 리네임은 diff만 커진다). `mockData.ts`의 관련 주석, `PersonResultPanel.tsx`의 배지("재응시 대상"), `SubmissionTab.tsx` docblock 한 줄, `RetrySendDialog.tsx`의 제목·빈 상태 문구, `ResultTab.tsx`의 카드 라벨·안내문·섹션 헤더·버튼 문구까지 전부 갈았다.
  - `mockData.ts`에 **`retryStatus(p: PersonResult): RetryStatus`** 함수를 새로 추가했다 — `!p.retrySentAt` → `'재응시 가능'`, `retrySentAt`은 있는데 `!retryTakenAt` → `'재응시 발송 완료'`, 둘 다 있으면 → `'재응시 완료'`. `ResultTab.tsx`의 발송 현황 표와 `RetrySendDialog.tsx`의 체크리스트가 **이 함수 하나로 같은 값을 계산**한다 — 전에는 다이얼로그가 "이미 다시 봤어요"를 독자적으로 하드코딩해서 두 화면이 서로 다른 기준으로 "봤다/안 봤다"를 판단했던 것이 오표기의 원인이었다.
  - `ResultTab.tsx`의 발송 버튼 렌더 조건을 `unsentCandidates.length > 0 && !locked` → **`!locked`**로 바꿨다(사용자 지시) — 전원 발송을 마쳐도 버튼이 남아 눌러서 모달을 다시 열 수 있다. 미발송 인원이 0명이면 모달을 열어도 체크 가능한 대상이 없을 뿐, 버튼 자체가 사라지지는 않는다.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier·vite build 전부 통과(1건 prettier 포맷 자동 수정 후 통과). 브라우저에서 ①"재응시 대상"·"재응시는 리포트 발행 후에 보낼 수 있어요"·"재응시 발송 N명 · 미발송 N명" 렌더 확인 ②발송 현황 표에서 임채원="재응시 완료"(success), 한소율="재응시 발송 완료"(neutral) 배지 확인 ③마지막 미발송자(김민준)에게 발송 → "재응시 발송 3명 · 미발송 0명"으로 바뀐 뒤에도 **"재응시 발송" 버튼이 그대로 남아** 다시 눌러 모달이 열림을 실측 ④`RetrySendDialog`에서 김민준="재응시 가능"(warning, 체크 가능)·임채원="재응시 완료"(success, 체크 비활성)·한소율="재응시 발송 완료"(neutral, 체크 비활성) — `ResultTab` 표와 동일한 배지·색으로 확인.
- **목적·효과:** 화면 전체에서 같은 개념을 같은 말로 부르고, 발송 완료 여부와 실제 응시 여부를 혼동하게 만드는 문구가 사라졌으며, 두 화면이 하나의 계산식을 공유해 앞으로도 어긋날 일이 없다.

## D38 · 독촉 모달 — "미응시" 그룹을 "기한 임박"으로 교체 + 범위 확대

- **배경:** 사용자 지시 — "여기서 미응시 대신에 기간이 얼마 남지 않은 인원들에게 독촉을 보내는걸로 교체하자. 범위는 응시 완료한 개인을 제외한 모든 개인들에게 '응시 가능한 날짜가 N일 남았습니다.'" 전엔 `NudgeDialog.tsx`의 세 번째 그룹이 `NOT_STARTED`(응시를 아예 시작 안 한 사람)만 대상이었고, 이미 응시 창이 열려 카운트다운 중인 `AVAILABLE` 인원은 독촉 대상에 없었다.
- **판단:**
  - 대상을 `status === 'NOT_STARTED'` 단독에서 **`AVAILABLE` ∪ `NOT_STARTED`**로 넓혔다(`dueSoon`) — "기간이 얼마 남지 않았다"는 말이 실제로 맞으려면 이미 카운트다운 중인 `AVAILABLE`도 포함해야 한다.
  - **`BLOCKED`는 사용자 지시("응시 완료한 개인을 제외한 모든 개인")를 문자 그대로 따르면 포함되지만, 판단으로 뺐다.** `BLOCKED`는 타입 주석 그대로 "코드가 없거나 분석이 실패해 응시 자체가 열리지 않은 상태"라, "응시 가능한 날짜가 N일 남았습니다"를 보내면 응시가 가능한 것처럼 사실과 다르게 말하는 꼴이 된다 — [[D37]]의 "이미 다시 봤어요" 오표기와 같은 종류의 문제라 같은 원칙(사실과 다른 문구는 안 보낸다)으로 제외했다. 필요하면 확인받고 되돌린다.
  - `NOT_STARTED`는 `mockData.ts`에서 애초에 `deadlineLabel: null`이었다(독촉 문구에 넣을 "N일"이 없었다). **같은 팀의 `AVAILABLE` 팀원과 같은 값을 채워 넣었다** — 응시 창은 "팀의 코드 분석 완료 시점"에 한 번에 열리므로(`docs/plan/v2/14-verification-design.md` "응시 창의 기산점은 제출이 아니라 코드 분석 완료") 같은 팀이면 마감도 같다. 2팀(t9·t10·t11) → t11=D-2(t10과 동일), 3팀(t1·t2·t3) → t3=19시간 남음(t2와 동일), 7팀(t18·t19·t20) → t20=D-1(t19와 동일). 추측이 아니라 팀 배정 데이터에서 역산한 값이다.
  - 그룹 제목 "미응시 N명" → **"기한 임박 N명"**, `names`에 각자의 `deadlineLabel`을 괄호로 붙여 보여준다("오태양(D-2) · 신다은(D-2) · …") — 문구 자체("응시 가능한 날짜가 N일 남았습니다")는 상태별 고정 템플릿(`mockData.ts` "독촉 — 상태별 고정 문구" 원칙)이라 그대로 두고, 매니저가 누가 얼마나 급한지는 이름 옆 라벨로 확인한다.
  - **부수 발견·수정:** `groupCount` 계산식이 `checkUnsub`·`checkFailed`만 세고 세 번째 그룹(`checkNotStarted`)을 애초부터 빠뜨리고 있었다 — 3그룹을 전부 체크해도 하단 요약이 "2그룹"으로 떴다(사용자가 준 스크린샷에서 "2그룹 · 11명"으로 실제로 나타나 있었음). `checkDueSoon && dueSoon.length > 0`을 배열에 추가해 고쳤다.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier·vite build 전부 통과. 브라우저에서 ①제출 현황 표의 신다은·정하늘이 "미응시 / D-2"·"미응시 / 19시간 남음"으로 팀원과 같은 기한을 보여줌 ②독촉 모달에서 "기한 임박 7명 — 오태양(D-2)·신다은(D-2)·이서준(19시간 남음)·정하늘(19시간 남음)·노은채(D-1)·백승민(D-1)·유리안(D-3) 개인에게 — '응시 가능한 날짜가 N일 남았습니다'" 렌더 ③세 그룹 전부 체크 시 "3그룹 · 15명에게 발송"으로 정확히 집계(수정 전이었다면 "2그룹") 확인.
- **목적·효과:** 독촉이 "아예 안 한 사람"뿐 아니라 "곧 기한을 놓칠 사람"까지 챙기게 됐고, 사실과 다른 문구(응시 자체가 안 열린 사람에게 "응시 가능"이라 말하는 것)를 보내지 않으며, 그룹 카운트 표시도 실제 발송 범위와 일치한다.

## D39 · 발송 완료 체크박스를 항상 잠긴 채 체크 표시 + 독촉 문구를 남은 시간 단위로 유동화

- **배경:** 사용자가 두 화면 스크린샷을 보고 지적. ①`RetrySendDialog.tsx`에서 이미 발송된 사람(예: 김민준)의 체크박스가 **체크된 채 잠기지 않고 빈 채로 잠겨** 있었다 — "포함돼 있어서 못 뺌"과 "안 담겨서 못 넣음"은 다른 뜻인데 후자로 보였다. ②[[D38]]에서 만든 "기한 임박" 독촉 문구가 "응시 가능한 날짜가 N일 남았습니다"로 고정이라, 실제로는 "응시 가능한 기간이 N일, 24시간 미만일 경우 N시간 남았습니다"처럼 **남은 시간에 따라 단위(일/시간)가 유동적으로 바뀌어야** 한다.
- **판단:**
  - `RetrySendDialog.tsx`의 체크박스 `checked` prop을 `checked.has(id)` 단독에서 **`p.retrySentAt ? true : checked.has(id)`**로 바꿨다. 원인은 `checked` state가 다이얼로그를 처음 연 시점의 미발송자 목록으로만 초기화되고(useState 초기화 함수), 발송 후 다이얼로그가 리마운트 없이 갱신되면 그 사람의 체크 여부가 "발송 당시 체크돼 있었는지"에 따라 달라 세션마다 다르게 보였다 — 이제 `retrySentAt`이 있으면 내부 Set 값과 무관하게 항상 체크된 채로 그린다.
  - `mockData.ts`에 **`dueSoonMessage(deadlineLabel)`** 함수를 신규 — `"D-N"`은 `"응시 가능한 기간이 N일 남았습니다"`, `"N시간 남음"`은 `"응시 가능한 기간이 N시간 남았습니다"`로 변환(정규식 매칭). `NudgeDialog.tsx`의 "기한 임박" 그룹은 이제 이 함수로 계산한 문구가 **같은 사람끼리 묶어 문구별로 한 줄씩** 보여준다(`groupByDueSoonMessage`) — 기존 `GroupRow`는 그룹 전체가 문구 하나를 공유하는 구조라 사람마다 단위가 다른 이 그룹엔 안 맞아, 이 그룹만 별도로 렌더링했다. 문구 자체는 여전히 `dueSoonMessage(deadlineLabel)` 하나로 결정되는 상태별 고정값이라 "독촉 — 상태별 고정 문구, 커스텀 없음" 원칙은 안 깨졌다.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier·vite build 전부 통과. 브라우저에서 ①`RetrySendDialog`를 다시 열어 김민준·임채원·한소율 **셋 다** 체크된 채 표시(임채원·한소율은 비활성) 확인 ②독촉 모달에서 "기한 임박 7명" 아래 "오태양·신다은 개인에게 — '응시 가능한 기간이 2일 남았습니다'" / "이서준·정하늘 개인에게 — '…19시간 남았습니다'" / "노은채·백승민 개인에게 — '…1일 남았습니다'" / "유리안 개인에게 — '…3일 남았습니다'" 4줄로 정확히 갈려 렌더됨을 확인.
- **목적·효과:** 체크박스 표시가 "발송 여부"라는 사실 하나로만 정해지고, 독촉 문구가 실제 남은 시간과 항상 맞게 나간다.

## D40 · "기한 임박" 독촉 문구 — 개인별 일/시간 유동에서 단일 문장으로 되돌림

- **배경:** [[D39]]에서 만든 "문구별로 줄을 나눠 보여주는" 렌더(오태양·신다은/이서준·정하늘/노은채·백승민/유리안 4줄)를 사용자가 렌더 스크린샷으로 보고 "이렇게 일일히 응시가능한 기간을 개인별로 나열하니까 보기 별로다"고 지적, "그냥 문장을 하나로 통일하자. '응시 가능한 기간이 얼마 남지 않았습니다.'"로 지시.
- **판단:** `NudgeDialog.tsx`의 "기한 임박" 그룹을 **문구 하나 + `GroupRow` 재사용**으로 되돌렸다. 대상 범위(응시 완료 제외 전원, `BLOCKED` 제외 — [[D38]])는 그대로 두고, 문구 계산 로직만 걷어냈다. 그 결과 더는 안 쓰는 `mockData.ts`의 `dueSoonMessage()`와 `NudgeDialog.tsx`의 `groupByDueSoonMessage`·`FALLBACK_DUE_SOON_MESSAGE`를 삭제했다 — 죽은 코드를 남기지 않는다.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier(1건 자동 포맷)·vite build 전부 통과. 브라우저에서 "기한 임박 7명 — 오태양·신다은·이서준·정하늘·노은채·백승민·유리안 개인에게 — '응시 가능한 기간이 얼마 남지 않았습니다.'"로 한 줄에 렌더됨을 확인.
- **목적·효과:** 목록이 다시 다른 두 그룹과 같은 한 줄 구조로 통일돼 읽기 쉬워졌다. 정확한 개인별 남은 시간이 필요하면 제출 현황 표(`SubmissionTab`)의 "미응시 / D-2"류 표기를 보면 된다 — 이 모달은 "누구에게 보내는가"만 확인하는 자리로 역할이 좁아졌다.

## D41 · 잠긴 체크박스 회색 표시(공용 Checkbox의 `disabled:` 무동작 발견) + 재응시 발송 취소 기능

- **배경:** 사용자가 두 가지를 요청. ①[[D39]]에서 발송완료 체크박스를 "항상 체크된 채로 잠근다"로 고쳤지만 색은 활성 체크와 똑같은 파랑이라 "클릭했을 때와 다른, 회색배경의 비활성화된 체크로 바꿔달라"고 지시. ②"재응시 발송을 했을 때, 재응시 발송 취소기능도 넣어줘, 재응시 완료한 경우는 제외"로 지시 — 발송만 되고 아직 안 본 사람(재응시 발송 완료)은 취소할 수 있어야 하고, 이미 다시 응시한 사람(재응시 완료)은 대상에서 뺀다.
- **판단(체크박스 색):**
  - 처음에 `disabled:data-checked:bg-neutral-soft` 같은 Tailwind `disabled:` 변형으로 덮어썼는데 **브라우저에서 전혀 안 먹혔다.** `getComputedStyle`로 직접 재보니 원인은 Base UI `Checkbox.Root`가 **`<span role="checkbox">`로 렌더돼 네이티브 `disabled` 속성 자체가 없고**, 비활성은 `data-disabled` 어트리뷰트로만 표시한다는 것 — Tailwind `disabled:`는 `:disabled` 가상 클래스라 `<span>`엔 애초에 매칭될 수 없다. **공용 `components/ui/Checkbox.tsx`의 `disabled:opacity-50`·`disabled:cursor-not-allowed`도 같은 이유로 처음부터 한 번도 작동한 적이 없었다**(실측 확인 — 비활성 체크박스가 opacity 1로 항상 완전 불투명이었다).
  - `RetrySendDialog.tsx`에서 선택자를 **`data-disabled:data-checked:*`**(어트리뷰트 선택자, 항상 매칭)로 고쳐 `neutral-soft` 배경·`border-strong` 테두리·`fg-subtle` 체크 아이콘으로 덮었다. `[data-disabled][data-checked]`는 2속성 선택자라 원래 `data-checked:bg-primary`(1속성)보다 스타일시트 순서와 무관하게 항상 우선한다.
  - **`components/ui/Checkbox.tsx` 자체는 고치지 않았다** — 팀장님 소유 파일이라(CLAUDE.md §7) 이 화면(`className` 오버라이드)에서만 고쳤고, 공용 컴포넌트의 `disabled:*`가 무동작이라는 사실은 여기 기록만 하고 팀장님 확인을 거쳐야 한다(다른 화면의 비활성 체크박스도 같은 증상일 가능성이 높다).
- **판단(취소 기능):**
  - `mockData.ts`에 `cancelRetry(id, traineeId)` 신규 — `retrySentAt`·`retryDueAt`을 지워 "재응시 가능" 상태로 되돌린다. **`retryTakenAt`이 있으면(이미 재응시함) 아무것도 안 한다**(사용자 지시 "재응시 완료한 경우는 제외") — 함수 안에서도 한 번 더 막아 호출부가 실수로 버튼을 숨기지 못한 경우를 방어한다.
  - 취소 버튼은 `RetrySendDialog`(발송 대상을 고르는 화면)가 아니라 **`ResultTab.tsx`의 발송 현황 표**(관리 화면)에 뒀다 — "재응시 발송 완료" 행에만 뜨고 "재응시 완료" 행엔 안 뜬다. 누르면 바로 취소되지 않고 `AlertDialog`로 한 번 더 확인받는다("리포트 발행 마감 전 경고"와 같은 패턴) — 학생에게 이미 안내된 기한을 지우는 되돌리기 있는 행동이지만 다시 보내려면 새로 발송해야 해서 가볍게 넘길 액션은 아니다.
- **검산:** `/tmp` 격리 tsc·oxlint·prettier·vite build 전부 통과. 브라우저에서 ①`getComputedStyle`로 활성 체크(`rgb(58,77,184)` 파랑) vs 잠긴 체크(`rgb(224,227,234)` 회색) 배경색이 다름을 직접 측정해 확인 ②`ResultTab`에서 한소율(재응시 발송 완료)에만 "취소" 버튼이 뜨고 임채원(재응시 완료)엔 안 뜸 확인 ③취소 확인 다이얼로그 "한소율에게 안내된 기한(2026-07-19 (일) 18:00)이 사라지고 '재응시 가능' 상태로 돌아갑니다" 확인 → 확정 클릭 → "재응시 발송 1명 · 미발송 2명"으로 정확히 갱신, 한소율이 발송 현황 표에서 빠짐을 확인.
- **목적·효과:** 잠긴 체크박스가 눈으로도 "지금 누른 것"과 구분되고, 매니저가 실수로 보낸 재응시를 되돌릴 수 있다 — 단 이미 벌어진 재응시는 그대로 기록에 남는다.

## D42 · `develop` 머지 충돌 3건 — `mockData.ts`·`ProjectRowCells.tsx`를 HEAD(우리) 쪽으로 해소

- **배경:** `feature/mg-08-project-detail`에 `develop`(팀장님이 28커밋 앞서 있던)을 머지하는 중 `ProjectFilters.tsx`(1건) · `ProjectRowCells.tsx`(2건) · `mockData.ts`(3건) 충돌 발생. 셋 다 "빅프(구조적으로 교안이 없는 회차) 개념"이 겹치는 자리 — 우리 브랜치는 `kind==='BIG'`을 없애며 `hasNoCurriculum(project)`(커밋 `a936ebb`, `concepts !== null && concepts.length === 0`로 판정)로 치환했고, `develop`의 팀장님은 같은 시기에 독립적으로 "빅프 자체를 제품에서 제거"하며 `Project.note` 필드·`PROJECTS`의 `bigp` 항목·`hasNoCurriculum` 분기를 통째로 지웠다.
- **선택지:** (a) `develop` 쪽(팀장님)을 그대로 채택 — 문서상 더 최근·더 완전한 제거처럼 보임 (b) HEAD(우리) 쪽을 유지.
- **1차 판단(오판, 되돌림):** 처음엔 (a)로 갔다 — `mockData.ts`의 `PROJECTS`에서 `bigp` 항목·`note` 필드·`hasNoCurriculum` 함수를 지우고 `ProjectRowCells.tsx`의 두 분기도 `develop` 쪽으로 맞췄다. **곧바로 오류를 발견:** `mockData.ts` 내 `PROJECT_DETAILS['bigp']`(MG-08 팀 편성 화면이 "5국면 중 편성 전" 데모로 쓰는 항목, 판단 기록 "bigp=편성 전")가 `bigp`를 그대로 참조하고 있어, `PROJECTS`에서 `bigp`를 지우면 `ProjectDetailScreen.tsx`의 `PROJECTS.find(p => p.id === id)`가 `undefined`가 되어 헤더가 깨진다 — MG-08 자체 기능을 부순다.
- **최종 판단:** (b). `git show --stat a936ebb`로 확인한 결과 그 커밋은 `mockData.ts`를 건드리지 않았다 — 즉 `hasNoCurriculum`·`bigp`·`note`는 이미 그 이전부터 있던, MG-08 설계 초기의 **의도된** 데이터다(`git show a936ebb -- ProjectRowCells.tsx`로 `project.kind === 'BIG'` → `hasNoCurriculum(project)` 치환이 목적을 갖고 이뤄졌음을 재확인). `develop`의 "빅프 제거"는 MG-07/OP-03 **목록 화면** 범위의 정리였고, 우리 MG-08(팀 편성·제출·결과 탭)은 아직 `develop`에 없어 팀장님이 그 의존을 몰랐을 뿐 — 상충하는 두 의도가 아니라 **정보 비대칭**이었다. 세 파일 모두 HEAD(우리) 쪽 내용을 복원해 확정.
- **경계 판단(CLAUDE.md §7과의 관계):** `components/ui/Checkbox.tsx`([[D41]])처럼 팀장님이 **단독 작성**한 파일은 임의 수정 금지 대상이지만, `mockData.ts`·`ProjectRowCells.tsx`는 **양쪽이 각자 커밋한 공유 파일**이라 3-way 머지 충돌 해소는 "임의 수정"이 아니라 이 작업의 정당한 범위다 — 다만 되돌린 판단 자체([[D42]] 이 항목)는 팀장님이 병합 후 리뷰에서 바로 확인할 수 있게 기록해 둔다.
- **검산:** `/tmp` 격리 복사(`node_modules`는 심볼릭 링크로 재사용) 후 `tsc --noEmit` 통과, `prettier --check` 통과. `oxlint`·`vite build`는 알려진 네이티브 바인딩 깨짐(`docs/dev/handoff.md`)으로 이 샌드박스에서 실행 불가 — 타입·포맷 검증으로 대체. `grep`으로 3개 파일 전체에 충돌 마커(`<<<<<<<`/`=======`/`>>>>>>>`) 잔존 0건, `hasNoCurriculum` 참조가 정의(`mockData.ts`)·사용처(`ProjectRowCells.tsx` 2곳) 모두 일관됨을 확인.
  - ⚠ **이 검산은 틀렸었다** — 상세는 [[D43]].
- **목적·효과:** `develop` 머지 후에도 MG-08 팀 편성 화면의 "편성 전" 데모(`bigp`)가 그대로 동작한다. 다음 세션/리뷰에서 팀장님이 "왜 `develop`의 빅프 제거가 여기선 되돌아갔냐"고 물으면 이 항목을 근거로 답한다 — MG-08이 `develop`에 합류하면(팀장님이 목록 화면을 다시 정리할 때) `bigp` 데모 데이터를 다른 프로젝트로 이전하거나 `PROJECT_DETAILS`의 5국면 구성을 바꾸는 후속 정리가 필요하다는 점도 팀장님께 별도로 알려야 한다.

## D43 · PR 첫 CI 실패 2건 — (1) `tsc --noEmit -p .` 검산이 사실 아무것도 안 검사했음 (2) `bigp` 되돌림이 팀장님의 제품 결정을 되돌리는 셈이라는 것 확인

- **배경:** [[D42]] 병합 후 push했더니 Vercel 배포·GitHub Actions가 **둘 다 실패**. Vercel 로그: `RetrySendDialog.tsx(223,69)` — `defaultDeadline()`이 `{ date: Date; ... }`로 선언됐는데 `DueDateField`의 `onDate: (d: Date | undefined) => void`가 `undefined`를 돌려줄 수 있어 타입 불일치. 하지만 [[D13]]·[[D42]]에서 여러 번 "`tsc --noEmit` 통과"라고 적었던 항목이라 왜 못 잡았는지부터 확인이 필요했다.
- **원인 1(검산 도구 자체가 허당):** 루트 `tsconfig.json`은 `"files": []`로 선언돼 있다(shadcn CLI 등 외부 툴의 alias 오작동을 막는 진입점 전용, 주석에 명시돼 있었는데 못 보고 지나침). `tsc --noEmit -p .`는 이 루트 설정을 읽어 **파일을 0개 검사하고 그대로 종료**한다 — 여태 이 명령으로 "통과"라고 적은 검산은 전부 아무 파일도 안 본 거짓 통과였다. 실제 빌드(`npm run build` = `tsc -b && vite build`)는 `tsc -b`가 `tsconfig.json`의 `references`(`tsconfig.app.json`·`tsconfig.node.json`)를 따라가는데, `-b`(프로젝트 참조 증분 빌드)는 이 샌드박스에서 45초 타임아웃 안에 못 끝남 — 그래서 이번엔 `tsc --noEmit -p tsconfig.app.json`처럼 **실제 참조되는 서브 설정을 직접 지정**해서 우회했다. 이후 검산은 전부 이 방식으로 교체.
- **원인 2(타입만이 아니라 실제 버그였음):** `defaultDeadline()`의 반환 타입은 실수가 아니라 **당시 로직과 어긋난 선언**이었다 — 151행 주석("지워도(달력 재선택 취소) 보내기를 막는다")과 `deadlineValid = !!dueDate`·`if (!dueDate || !dueTime) return`가 전부 "날짜가 지워질 수 있다"를 전제로 짜여 있었다. `{ date: Date | undefined; time: string }`로 타입만 넓혀 실제 로직과 선언을 맞췄다(동작 변경 없음, 커밋 `09a9dfe`).
- **원인 2 확인 중 발견한 별도 문제(제품 결정 되돌림):** [[D42]]에서 되돌린 `bigp` 항목의 성격을 다시 확인했다 — 팀장님의 커밋 `3a56828`("remove: drop big project concept from screens", PR #93)은 단순 UI 정리가 아니라 **"회차 유형이 미프 하나만 남는다"는 제품 결정**이었다(커밋 본문: "요구사항 0건의 근거가 '빅프는 요구사항 없는 회차가 실제로 있다'였다. 근거가 사라지므로..."). 그 커밋이 MG-07의 `ProjectFilters.tsx`·`ProjectRowCells.tsx`·`mockData.ts`에서 유형 열·필터·`해당 없음` 분기·`kind`·`note`를 전부 지운 게 바로 이 결정의 실행분이다. [[D42]]에서 HEAD 쪽을 되살리며 `PROJECTS` 배열에 `bigp` 항목도 함께 복원했는데 — 이건 MG-08 데모 데이터 의존성 해결을 넘어서, **매니저 프로젝트 목록(MG-07) 실제 화면에 "빅프" 행이 다시 나타난다는 뜻**이다. 팀장님이 제품 차원에서 죽인 회차 유형이 이 브랜치를 통해 목록에 되살아나는 셈 — 이건 파일 소유권 문제가 아니라 **제품 결정 충돌**이라 병합 전에 팀장님 확인이 필요하다고 판단, 사용자에게 그대로 전달했다. **사용자가 "팀장님 결정대로 유지"로 확정** — 실제 제거는 [[D44]].
- **검산:** `/tmp` 새로 격리 복사, `tsc --noEmit -p tsconfig.app.json`·`-p tsconfig.node.json` 둘 다 통과, `prettier --check` 통과. 사용자가 push 후 GitHub Actions CI 통과 확인.
- **목적·효과:** 앞으로 이 레포에서 "tsc 통과"라고 적을 땐 항상 `tsconfig.app.json`/`tsconfig.node.json`을 직접 지정한다 — 루트 설정으로는 아무 것도 검증되지 않는다.

## D44 · `bigp`·`note`·`hasNoCurriculum` 최종 제거 — 팀장님 결정대로, "편성 전" 데모는 `recomputeTeamPhase`로 대체

- **배경:** [[D43]]에서 발견한 제품 결정 충돌(`bigp` 부활 = 팀장님이 죽인 "빅프" 회차 유형이 MG-07 목록에 되살아남)을 사용자에게 전달했고, 사용자가 "팀장님 결정대로 유지하자"(develop 쪽 제거를 최종 채택)로 확정.
- **문제:** 그냥 `bigp`를 지우면 [[D42]]에서 막았던 문제(MG-08 팀 편성 화면의 "편성 전"(①) 국면 데모가 없어져 화면이 깨지는가)가 다시 열린다. 새 미프를 지어내 그 자리를 메우는 방법도 있었지만, 그러면 `PROJECTS` 개수·"예정" 상태 필터 개수(`docs` 상단 판단 기록이 "2건"으로 명시)가 바뀌어 이미 튜닝된 다른 수치들과 어긋난다.
- **판단:** `TeamTab.tsx`·`mockData.ts`의 `recomputeTeamPhase(d)`를 다시 읽어보니 `d.teams.length === 0`이면 자동으로 `teamPhase`를 `'BEFORE'`로 되돌리는 로직이 **이미 있었다**(팀 삭제 기능 — 확정 전 프로젝트에서만 팀을 삭제할 수 있게
  만든 이전 세션 작업에서 함께 넣었다). 즉 `mif-4`(편성 중)나 `mif-5`(전원 배정)에서 팀을 전부 삭제하면 그 프로젝트가 그 자리에서 "편성 전" 상태로 바뀌고, 멤버는 전부 `unassigned`로 돌아간다(`deleteTeam`이 `team.memberIds`를 `d.unassigned`에 되돌려 놓는 것도 확인). **정적 시드를 하나 늘리는 대신 이미 있는 동작을 그대로 쓰기로 했다** — 새 프로젝트를 안 만들어도 "편성 전" 화면은 여전히 100% 재현 가능하고, 팀장님의 제품 결정도 그대로 존중된다.
- **반영:**
  - `mockData.ts`: `Project.note` 필드 삭제, `PROJECTS`의 `bigp` 항목 삭제, `hasNoCurriculum()` 함수 삭제, `PROJECT_DETAILS['bigp']` 항목 삭제. 판단 기록 주석("6개 프로젝트에 5국면...")을 "5개 프로젝트..."로 고치고 "편성 전은 정적으로 seed하지 않는다 — 팀 삭제로 재현한다"는 설명을 추가(원문 훼손이 아니라 **현재 구현 상태를 설명하는 인라인 주석**이라 갱신 — decision-log.md의 "원문 보존" 원칙은 이 파일이 아니라 이 문서에만 적용된다).
  - `ProjectRowCells.tsx`: `hasNoCurriculum` import·`CurriculumCell`/`ConceptCell`의 "해당 없음"/"본인 커밋 영역" 분기 삭제, 팀장님 `3a56828`의 문구로 복원.
- **검산:** `grep`으로 `bigp`·`hasNoCurriculum`·`note?: string` 코드 참조 0건(설명 주석 1건 제외) 확인. `/tmp` 새 격리 복사 후 `tsc --noEmit -p tsconfig.app.json` 통과. `TeamTab.tsx`가 `detail.teamPhase`/`teams`/`unassigned`만 읽고 프로젝트 id를 하드코딩하지 않는 것, `deleteTeam()`이 `recomputeTeamPhase`를 호출해 `teams.length === 0 → BEFORE` 전이를 실제로 만드는 것, `ProjectDetailScreen.tsx`가 `bigp`나 프로젝트 개수를 하드코딩하지 않는 것을 코드로 확인.
- **목적·효과:** 팀장님의 "회차 유형이 미프 하나만 남는다" 결정이 이 브랜치에도 그대로 반영된다. MG-08 "편성 전" 화면은 데모용 프로젝트가 사라져도 코드는 그대로라 `mif-4`/`mif-5`에서 팀을 전부 지우면 언제든 재현·확인할 수 있다.

## D45 · 공용 Checkbox.tsx `disabled:*` 무동작 버그 수정(이슈 #113) — 원인 파일을 직접 고침

- **배경:** D41에서 발견한 것 — 공용 `components/ui/Checkbox.tsx`의 `disabled:cursor-not-allowed disabled:opacity-50`·`group-has-disabled/field:opacity-50`가 처음부터 한 번도 작동한 적이 없었다. Base UI `Checkbox.Root`가 `<span role="checkbox">`로 렌더돼 네이티브 `disabled` 속성 자체가 없고 `data-disabled` 어트리뷰트로만 비활성을 표시하는데, `disabled:`·`group-has-disabled:`는 둘 다 `:disabled` 가상 클래스를 전제로 하는 선택자라 `<span>`엔 애초에 매칭될 수 없었다. D41 당시엔 팀장님 소유 파일이라 그 화면(`RetrySendDialog.tsx`)에서만 우회하고 원인 파일은 안 건드렸는데, 이번에 사용자(진용)가 "내가 고쳐보자"며 직접 승인 — CLAUDE.md §7 "팀장이 만든 파일은 임의로 수정하지 않는다 · 수정이 필요해 보이면 먼저 알리고 확인받는다"의 확인 절차를 사용자 본인이 그 자리에서 충족했다.
- **판단:**
  - `disabled:*` → `data-disabled:*`, `group-has-disabled/field:` → `group-has-data-disabled/field:`로 교체. 같은 파일군의 `Switch.tsx`·`Select.tsx`·`DropdownMenu.tsx`가 이미 `data-disabled:*`를 올바르게 쓰고 있어 그 표기를 그대로 옮겼다 — 새 규칙을 만들지 않았다. `group-has-data-*`도 `Field.tsx`(`group-has-data-horizontal/field:`)·`Avatar.tsx`(`group-has-data-[size=lg]/avatar-group:`)에 이미 있는 표기와 동일하다.
  - `RetrySendDialog.tsx`의 `DISABLED_CHECKED_CLASS`(D41에서 만든 로컬 우회)는 **지우지 않았다.** 공용 컴포넌트가 고쳐져도 주는 건 "흐릿한 파랑"(`data-disabled:opacity-50`, 불투명도만 낮춤)이고, 이 화면이 사용자 지시로 이미 만들어 둔 건 "뚜렷한 회색"(다른 배경·테두리·글자색)이라 서로 다른 요구다. 두 클래스가 건드리는 CSS 속성이 겹치지 않아(opacity·cursor vs border·background·text-color) 공용 클래스 위에 로컬 오버라이드가 그대로 얹힌다 — 충돌도 중복도 아니라 그대로 뒀다.
- **검산:** `/tmp` 격리 복사(레포 전체 — Checkbox가 14개 화면에서 쓰여 부분 복사로는 회귀를 못 잡는다) 후 `tsc --noEmit -p tsconfig.app.json`·`-p tsconfig.node.json` 둘 다 통과, `prettier --check` 통과. `oxlint`·`vite build`는 이 샌드박스의 알려진 네이티브 바인딩 문제로 실행 불가 — 브라우저 실측(`getComputedStyle`로 비활성 체크박스 opacity·cursor 확인)은 다음 렌더 확인 때 사용자가 직접.
- **목적·효과:** 비활성 체크박스가 이 레포 전역(14개 화면)에서 처음으로 실제로 흐려지고 커서가 `not-allowed`로 바뀐다. `RetrySendDialog.tsx`처럼 이미 우회해 둔 화면은 그대로 두되, 다른 13개 화면도 별도 조치 없이 자동으로 정상화된다.

## D46 · MG-04 면담 브리프 구현 — 정의서·와이어에 없는 빈틈 5건 판단

- **배경:** MG-03 다음 로드맵인 MG-04(면담 브리프)를 정의서(`docs/plan/v2/definition/MG-04-interview-brief.md`)·와이어(`manager/interview-brief.html`)만 보고 신규 구현. 정의서·와이어가 예시로 든 케이스(김민준 DECLINE·한지우 첫 면담·오세림 무효 응시)는 그대로 옮겼지만, 이 화면을 실제로 열 수 있는 MG-03 mock 케이스 7개(PLANNED 상태 전부) 중 나머지 4개(이서준·최유나 1차·최유나 3차)는 두 문서 어디에도 없어 이 세션이 직접 채웠다.
- **판단:**
  1. **"첫 면담"은 회차 번호가 아니라 "이 학생에게 DONE 면담 기록이 있는가"로 재정의했다.** 와이어 #first 장면은 회차 라벨이 "미프 2차"인데 이 저장소엔 그 회차가 없다 — 라벨을 억지로 맞추는 대신 `findPriorInterview`(현재 회차보다 이전 회차 중 같은 학생의 DONE 케이스 탐색)로 판정을 일반화했다. 그 결과 한지우(`iv-3-5`, 3차에서 처음 등장)가 자연히 "첫 면담" 조건을 만족한다.
  2. **①의 여는 말 문장(비교형/비비교형)은 위험 유형이 아니라 위 "첫 면담" 판정으로 갈린다.** 한지우는 DECLINE(0→2, from이 있음)인데도 비교 문장을 안 쓴다 — 헤더 배지의 "0→2" 숫자는 원본 그대로 두고, ①의 문장만 이전 면담 유무로 고른다. 지속 저점(LOW_PERSISTENT)은 `from`이 없어 비교 문장을 쓸 때 지난·이번 모두 같은 `lowCount`를 넣는다(이서준 `iv-3-3`).
  3. **막힌 개념 목록(`CASE_CONCEPTS`)을 이 파일에 새로 만들었다** — 정의서 §4는 "세션·교안 섹션"에서 읽는다고 했지만 이 레포에 세션-면담 연동이 없다. "Graph 구성"을 김민준 말고 최유나(`iv-3-2`)에게도 겹쳐 반 문제(9-6, "반 절반 이상") 서술이 앞뒤가 맞게 했다. 교안 위치("N장 P–Q쪽")는 `curriculum/mockData.ts`를 참조하지 않았다 — `interviews/mockData.ts` 머리말이 이미 정한 "역할 간 mock cross-import 전례 없음" 원칙을 그대로 따라, 표시용 문자열을 이 파일에 독립적으로 붙였다.
  4. **"설명·표현 어려움"의 ④ 목적지는 정의서·와이어 어디에도 예시가 없다.** 나머지 6개 원인은 표나 장면에 하나씩 있는데 이것만 없어, "개념은 아는데 표현이 안 되는" 경우로 보고 매니저가 다음 면담에서 직접 다시 설명해 보게 하는 것(매니저 소관, 전달만이 아님)으로 채웠다. **팀장님 확인이 필요하면 알려달라고 다음 세션 인계에 남긴다.**
  5. **③ 원인을 하나도 안 골랐을 때 ④ 자리는 와이어 원문("③에서 고르면 보낼 곳이 나타납니다")을 그대로 쓰고, 정의서 6절의 별도 문구("고르지 않아도 저장할 수 있어요…")는 따로 안 만들었다** — 저장 자체를 막지 않는 것으로 그 뜻이 이미 충족된다고 판단.
  6. **"코드 매칭 0" 개념 제외·각주(정의서 6절)는 구현하지 않았다** — 이번 7개 케이스 어디에도 해당 예시가 없어 검증 방법이 없다. `ConceptRef.zeroMatch?` 필드만 남겨 뒀다.
  7. **"저장 실패"(#savefail) 모달은 화면·핸들러를 정의서·와이어대로 만들되, mock이 강제로 실패시키지는 않는다** — 이 파일의 다른 mock API(`excludeCase`·`undoExclude`·`resolveVoid`)도 전부 항상 성공하는 이 레포 기존 관례를 그대로 따랐다.
  8. **MG-03 mock 데이터 하나를 이 세션에서 고쳤다** — `iv-2-2`(김민준 2차 DONE) `interview.nextAction`을 `null`→`'담당 기능 흐름 그려오기'`로. 와이어 #brief 장면의 "지난 면담(6/22)에서 정한 것" 예시가 이 케이스를 인용하는데 원래 값이 `null`이라 인용할 게 없었다. 부수 효과로 MG-03 목록의 2차 "마지막 활동" 열도 "없음"→해당 문구로 바뀐다(회귀가 아니라 실제 있어야 할 값이 채워진 것).
- **검산:** `/tmp` 격리 복사(레포 전체) → `tsc --noEmit -p tsconfig.app.json`·`-p tsconfig.node.json` 둘 다 통과, `prettier --check` 통과(1차 포맷 오류 2개 파일 → `--write` 반영 후 재검증 통과), `node scripts/check-design.mjs` 통과. `oxlint`·`vite build`는 이 샌드박스의 알려진 네이티브 바인딩 문제로 실행 불가. **브라우저 렌더 확인은 아직 안 함** — 다음으로 미룬다.
- **목적·효과:** MG-04가 placeholder에서 실제 화면으로 바뀐다. `InterviewListScreen`의 `[브리프 열기]`가 실제 데이터를 채운 브리프로 연결되고, 저장하면 케이스 상태가 예정→종결로 바뀌어 MG-03 목록에도 반영된다.

**같은 세션 이어서(렌더 전 사용자 피드백) — 용어·표기 6건 반영:**

- 블록 번호를 원문자(①②③④)에서 일반 숫자(1·2·3·4)로 — 파란 원 배지 안에서 크기·정렬이 안 맞는다는 지적. `size-5` 원 안에서 `text-xs leading-none tabular-nums`로 다시 맞췄다.
- **③ "들은 것"→"원인", ④ "보낼 곳"→"조치"로 개명.** 사용자가 "애매하다"며 추천을 요청 — 정의서 원문이 이 두 블록을 이미 "원인 체크박스"·"라우팅 스위치"로 부르고 있어(§3), 그 어휘를 그대로 화면 라벨로 올렸다. "조치"는 각 목적지 줄의 "매니저/전달만" 오너 표시와도 자연스럽게 맞는다(누가 조치하는지).
- 기록 카드 "어디로 보냈나" 힌트 오탈자 수정 — `&#9314;`(③)로 잘못 옮겨져 있던 걸 원문 `&#9315;`(④)로. 실제로도 이 값은 ④(조치) 선택 결과를 합친 것이라 ④가 맞다.
- 기록 필드 라벨 변경 — "무엇 때문이라고 했나"→"원인"(③ 블록 이름과 짝을 맞춤, 칩은 분류·텍스트는 서술), "다음에 할 것"→"추후 계획"(저장 확인 모달 문구도 같이 바꿈).
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 통과(포맷 수정 없이 1차 통과). 브라우저 렌더 확인은 여전히 다음으로 미룬다.

**같은 세션 이어서 — 기록 카드 라벨 겹침·시각 구분 3건:**

- **③ 블록 제목 "원인"과 기록 카드 필드 라벨 "원인"이 겹친다는 지적 — 기록 카드 쪽을 "상세 사유"로 바꿨다.** ③은 정의서 어휘를 그대로 쓴 구조적 분류(칩)이고, 기록 카드 쪽은 매니저가 직접 타이핑하는 서술(학생이 뭐라고 설명했는지)이라 내용 성격이 다르다 — 같은 이름을 쓰면 "칩 선택이 자동으로 여기 들어가나?"는 오해가 생긴다. ④ "조치"는 기록 카드 필드도 같은 이름으로 남겼다 — 이쪽은 실제로 ④의 내용을 그대로 미러링하는 자동 필드라 이름이 같아도 오해가 없다(구분은 "4에서 자동 반영" 힌트가 한다).
- 기록 카드 "어디로 보냈나" → **"조치"**로, 힌트 "4에서 자동" → **"4에서 자동 반영"**으로(사용자 지시, XX 자리 추천).
- **직접 입력 칸과 자동 채움 칸의 시각 구분이 없다는 지적** — 자동 채움 칸(조치)만 배경을 `bg-surface-2`(입력칸과 동일)에서 `bg-input/60`(공용 `Textarea.tsx`가 비활성 상태에 이미 쓰는 `disabled:bg-input/50`과 같은 토큰)로 바꾸고 `cursor-not-allowed`·`aria-readonly`를 추가했다 — 새 색을 만들지 않고 이 레포가 이미 "못 건드리는 입력"에 쓰는 관례를 그대로 가져왔다.

## D47 · 종결(DONE) 후에도 브리프를 다시 열 수 있게 — 정의서·와이어 밖 흐름을 사용자 지시로 추가

- **배경:** MG-03 목록에서 상태가 종결이면 `RowActions`가 `null`을 반환해 액션 열이 통째로 비었다(브리프 열기·제외 둘 다 안 보임). 사용자가 스크린샷을 보고 "특별한 이유가 없으면 종결 후에도 브리프를 열거나 제외할 수 있게 하자, 다시 열면 이전에 쓴 내용이 그대로 있어야 한다"고 지시. 정의서 7절은 "약속 이행 추적은 하지 않는다"만 말할 뿐 재오픈 자체를 막지 않는다 — 막아야 할 근거를 찾지 못해 그대로 열었다.
- **판단:**
  - **브리프 재오픈은 허용.** `InterviewListScreen`의 `RowActions`가 DONE에도 `[브리프 수정]` 버튼(ghost, PLANNED의 primary "브리프 열기"와 구분)을 그린다. `InterviewBriefScreen`은 `mockData.ts`의 새 필드 `InterviewCase.interview.causes`·`.why`(기존엔 `date`·`nextAction`만 저장했다)를 읽어 `BriefData.savedRecord`로 내려주고, `BriefSheet`가 `useState` 초깃값으로 그대로 복원한다. 저장 버튼 문구도 `savedRecord` 유무로 "저장하고 종결"/"저장"을 가른다(이미 종결된 걸 또 "종결"이라고 하면 거짓 신호가 된다).
  - **제외는 DONE에 추가하지 않았다 — 이건 사용자 지시를 그대로 안 따르고 되돌린 지점이다.** "제외"는 정의서 §6에서 "예정" 케이스를 이번 회차 대상 큐에서 빼는 동작이다(9-4, 되돌릴 수 있다). 이미 면담이 끝난(DONE) 케이스를 "제외"한다는 게 상태 모델상 의미가 없다 — 종결과 제외가 동시에 성립하는 상태를 만들 이유도 없고, `excludeCase`가 그 전이를 처리하지도 않는다. "특별한 이유가 없으면"이라는 사용자의 단서를 이 판단으로 충족했다고 보고, 반대하면 되돌린다.
  - `saveInterviewBrief`의 상태 가드를 `status === 'PLANNED'`에서 `status !== 'EXCLUDED'`로 넓혔다 — DONE 케이스를 다시 저장해도 상태는 그대로 DONE, 제외된 케이스만 저장 자체가 막힌다(제외 후 브리프로 못 돌아오는 것과 같은 이유).
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과. 브라우저 렌더 확인은 다음으로 미룬다.
- **목적·효과:** 종결된 면담도 다시 열어 원인·상세 사유·추후 계획을 고칠 수 있다. 잘못 적었거나 면담 후 새로 알게 된 내용을 반영할 방법이 생긴다.

## D48 · "전체 이력"·이름 클릭이 D-1(조회 권한 없음)으로 막히던 문제 — `trainees/mockData.ts`에 9명 추가(사용자 승인)

- **배경:** MG-04 브리프의 "전체 이력 ↗", MG-03 목록의 이름 클릭이 전부 `TraineeDetailScreen`의 D-1 안내("조회 권한 없는 교육생입니다")로 막힌다는 스크린샷 제보. 원인 확인: `features/manager/interviews/mockData.ts`가 쓰는 `traineeId`(`t-kim-minjun` 등)가 `features/manager/trainees/mockData.ts`의 실제 `TRAINEES` id(`t-1`~`t-12`, 완전히 다른 12명)와 처음부터 안 맞았다 — D-1 자체는 MG-06이 의도한 대로 정상 동작 중이었고, **이번 세션이 만든 버그가 아니라 MG-03 때부터 있던 두 mock 데이터 간극**이다.
- **판단:** `trainees/mockData.ts`는 최근 주요 커밋이 팀장님(jxxnixx) 것이라(`git log --follow`) CLAUDE.md §7에 따라 확인 없이 고치지 않고 먼저 사용자에게 3가지 선택지를 제시 — ① 9명 새로 추가(기존 12명은 안 건드림) ② 그대로 둠(알려진 한계로 안내만) ③ 팀장님께 먼저 확인. **사용자가 ①을 선택.** `TRAINEES` 배열 끝에 면담 화면이 실제로 참조하는 9명(이서준·최유나·김민준·오세림·한지우·박도현·강윤서·정하늘·서지훈)을 인터뷰 mock과 **같은 id·이름**으로 새로 추가했다. 기존 `t-1`~`t-12` 12명은 이름·값 전부 그대로 — diff는 순수 추가만(삭제 0줄).
- **범위를 좁힌 지점:** `rounds`(회차 격자)·`TRAINEE_DETAIL_OVERLAY`(MG-06 타임라인)는 채우지 않았다. `getTraineeDetailOverlay`가 오버레이 없는 id를 `{ timeline: [] }`로 이미 정상 처리하므로(§6 "아직 응시한 회차가 없습니다"), 9명은 상세 화면에 진입은 되지만 회차 격자·타임라인은 빈 상태로 보인다 — 면담 화면의 죽은 링크를 살리는 목적엔 이 정도로 충분하고, 9명분 회차·타임라인을 새로 지어내는 건 이번 요청 범위 밖이라 판단했다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과. 브라우저 렌더 확인은 다음으로 미룬다.
- **목적·효과:** MG-03·MG-04의 이름·"전체 이력" 링크 9명 전부가 이제 실제 상세 화면으로 연결된다(회차·타임라인은 빈 상태).

## D49 · 무효 응시 "시스템이 본 것"이 브리프 재오픈 후 사라지던 문제 — `voidEvidence`·`voidConfirmed` 분리

- **배경:** 사용자가 오세림(무효 응시) 브리프를 열었더니 목업엔 있는 "시스템이 본 것"(무응답·복사·응답 시간) 박스가 안 보인다고 지적. 원인 확인: MG-03의 `resolveVoid('KEEP')`이 "그대로 두기"를 고르면 `c.voidEvidence`를 지워버렸다(MG-03 당시엔 "재확인 대상에서 뺀다"는 뜻으로 지운 것) — 그 값을 MG-04 브리프도 그대로 읽고 있어서, 이미 확인된 무효 응시 케이스는 증거 자체가 없어 박스가 렌더되지 않았다.
- **판단:** 하나의 필드가 "증거 데이터"와 "확인 여부 플래그" 두 역할을 겸하던 게 근본 원인이라 판단, `InterviewCase`에 `voidConfirmed?: boolean`을 신설해 역할을 분리했다. `voidEvidence`는 이제 절대 안 지운다(브리프가 몇 번을 다시 열어도 "시스템이 본 것"을 보여줄 수 있다). `resolveVoid('KEEP')`은 `voidEvidence = undefined` 대신 `voidConfirmed = true`. MG-03 쪽에서 `voidEvidence` 유무로 판단하던 자리 3곳(`RowActions`의 "무효 확인" 게이트, `LastActivityCell`의 "확인 완료" 표시, `RiskReason`에 넘기는 `voidResolved`)을 전부 `voidConfirmed` 기준으로 바꿨다 — 동작(무효 확인 버튼이 한 번만 뜨는 것 등)은 그대로다, 데이터 모델만 정리됐다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.
- **목적·효과:** 무효 응시 브리프는 "그대로 두기"로 확인된 뒤에도 다시 열 때마다 시스템이 관찰한 근거를 그대로 보여준다. MG-03의 기존 동작(확인 게이트·완료 표시)은 회귀 없이 유지.

## D50 · MG-04 브리프 전체화면(사이드바 가림) 설계를 번복 — `ConsoleShell`로 통일(정의서 §2 오버라이드)

- **배경:** MG-04 정의서 §2는 "형태: 전체화면 · 사이드바 가림"을 명시적으로 규정했고, TR-03 검증 세션과 같은 논리("화면이 다르게 생긴 것 자체가 지금 작업 중이라는 신호")로 이 세션이 그대로 구현했다(D46). 렌더 스크린샷을 본 사용자가 "다른 매니저 화면은 다 셸이 있는데 브리프만 앱 자체가 사라지는 느낌이라 어색하다"고 지적.
- **판단:** 이건 구현 디테일이 아니라 **정의서가 명시한 설계를 뒤집는 것**이라 바로 고치지 않고 3가지 선택지(① 절충 — 상단바만 남기고 사이드바만 가림 ② 통일 — ConsoleShell로 감싼다 ③ 정의서대로 유지)를 제시했다. **사용자가 ②를 선택.**
- **반영:** `InterviewBriefScreen.tsx`를 `ConsoleShell role="manager"`로 감쌌다. 기존 전체화면 모드바(52px 고정 바, 사이드바 없이 앱 배경을 직접 칠하던 구조)를 걷어내고, 다른 상세 화면(교안 상세 등)이 쓰는 패턴 그대로 `PageHeader`(sr-only 제목·breadcrumb) + `← 목록으로 (저장 안 함)` 링크 + 이름·반·위험배지·회차 정보 줄로 바꿨다. "나가는 길"(목록으로·취소 버튼)은 그대로 유지 — 사이드바를 가리는 것과 별개로 "매니저 도구라 잘못 열 수 있다"는 정의서 §2의 취지 자체는 유효하다고 판단해서다. 4블록·기록 카드·저장/취소·다이얼로그 등 콘텐츠 쪽은 손대지 않았다(폭 상한 820px만 유지).
- **범위:** `docs/plan/v2/definition/MG-04-interview-brief.md` 원문은 고치지 않았다 — 이 레포는 정의서 번복을 decision-log로 기록하고 정의서 자체는 최초 설계 근거로 남겨두는 관례다(D33·D40 등 기존 사례와 동일).
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과. 브라우저 렌더 확인은 다음으로 미룬다.
- **목적·효과:** 브리프도 다른 매니저 화면과 같은 셸(사이드바·헤더)을 유지한 채 열린다 — 앱을 벗어난 느낌 없이, 그래도 "지금 이 사람 면담 중"이라는 신호는 이름·위험배지·나가는 링크로 계속 준다.

## D51 · 상단 정보 줄 축소 + 목록 필터 세션 유지 + 여는 말 위험 유형별 재작성

세 지적을 한 세션에서 반영, 근거를 한 항목에 묶는다(서로 이어지는 지적이라).

- **① 상단이 지저분하다** — breadcrumb("면담 › 7기 › A반")과 우측 회차 요약
  ("미프 3차 · 2단 이하 1 → 2")을 뺐다. 남긴 건 뒤로가기(아이콘 버튼으로 교체,
  기존 "← 목록으로 (저장 안 함)" 텍스트 링크 대신 `Curriculum` 상세 화면과 같은
  `ArrowLeft` 아이콘 버튼 — `aria-label`로 "저장 안 함" 경고는 스크린리더용으로
  남겼다) · 이름 · 반·기수 · 위험 배지뿐이다. 회차·판정 숫자를 뺀 이유는 ①
  여는 말이 이미 그 숫자를 자연스러운 문장으로 말해줘서 중복이었다고 판단해서다.
- **② 뒤로가기 시 필터 유지, 새로고침하면 초기화, 기본 회차는 마지막 회차** —
  `filterState.ts`에 모듈 전역 변수(`sessionFilters`)를 신설해 `InterviewListScreen`
  이 마운트될 때 거기서 초깃값을 읽고 변경마다 다시 쓴다. `localStorage`가 아니라
  모듈 전역을 쓴 이유는 요구 자체가 "SPA 안에서는 남고 새로고침하면 사라진다"라
  `localStorage`(새로고침에도 남음)·URL 쿼리(둘 다 남음) 둘 다 안 맞아서다. 기본
  회차 `'3'` 하드코딩은 `ROUND_OPTIONS[ROUND_OPTIONS.length - 1].value`(가장 마지막
  회차)로 바꿨다 — 회차가 늘어도 다시 안 고쳐도 된다.
- **③ 여는 말이 다 비슷하고 어색하다** — "점수 얘기를 하려는 게 아니라, ~"류
  정형화된 변명 문구를 전부 뺐다. 전엔 "이전 면담 유무"로 비교/비비교 두 템플릿만
  썼는데, **위험 유형 4종(무효 응시·단계 하락·지속 저점·관찰) 각각 고정 문장**으로
  다시 썼다(사용자 지시 — 무효 응시·단계 하락·지속 저점 3종을 직접 요청, 관찰은
  같은 설계 원칙을 적용해 이번 세션이 같이 채웠다). `mockData.ts`의 `openingLine()`
  이 숫자만 계산해 내려주고, 문장 자체는 `InterviewBriefScreen.tsx`의
  `renderOpeningLine()`이 고정 템플릿으로 갖는다(LLM 0회 원칙 유지). "지난 면담
  유무"는 이제 여는 말과 무관하다 — 그 정보는 원래도 별도 줄("⚠ 지난 면담(날짜)에서
  정한 것")이 담당하고 있어서, 여는 말 자체는 위험 유형만 보고 결정해도 정보
  손실이 없다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·
  `check-design.mjs` 전부 1차 통과.
- **목적·효과:** 상단이 이름·위험 배지 중심으로 단순해지고, 목록 화면을 오가도
  작업 맥락(회차·필터)을 잃지 않는다. 여는 말은 케이스마다 실제로 다르게 들린다.

## D52 · 회차를 바꾸면 나머지 필터(검색·상태·위험 유형·반)를 초기화한다

- **배경:** D51에서 필터를 세션 동안 기억하게 만든 뒤, 사용자가 "회차를 옮길 때도
  다른 필터가 그대로인데, 초기화하는 게 좋을지 그대로 두는 게 좋을지" 판단을
  요청.
- **판단(전문가 제안, 반대하면 되돌림):** **회차를 바꾸면 초기화한다.** 근거—
  ① 위험 유형은 회차마다 구성이 아예 다르다(1차는 `관찰`뿐, 2차 이상만 `단계
  하락`·`지속 저점`이 나온다) — `단계 하락` 필터를 걸어 둔 채 1차로 넘어가면
  대상이 하나도 없는 빈 목록을 만나고 왜인지 화면에 단서가 없다. ② 상태(예정·
  종결·제외) 분포도 회차마다 다르다 — 예: 3차만 보고 있던 "종결" 필터를 켠 채
  4차(결과 전)로 넘어가면 혼란만 커진다. ③ 이미 이 함수가 회차 전환을 "새
  맥락"으로 취급하고 있었다(되돌리기 배너를 지우는 기존 로직) — 나머지 필터도
  같은 원칙으로 맞추는 게 일관적이다. 반대로 **브리프를 열었다 같은 회차로
  돌아오는 것**은 초기화 대상이 아니다 — 그건 "새 맥락"이 아니라 "하던 일
  이어서"라 D51의 세션 유지가 그대로 적용된다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·
  `check-design.mjs` 전부 1차 통과.
- **목적·효과:** 회차를 옮기면 항상 그 회차의 전체 목록에서 다시 시작한다 —
  이전 회차 필터가 새 회차에서 뜻밖의 빈 화면을 만드는 일이 없어진다.

## D53 · MG-07·MG-08(프로젝트) 헤더 정리 — MG-04 뒤로가기 스타일로 통일

- **배경:** 사용자가 MG-04 브리프에서 정리한 헤더(아이콘 전용 뒤로가기)를 보고, 같은 세션에서 프로젝트 목록·상세 화면도 맞춰 달라고 지시.
- **반영:**
  1. `ProjectListScreen.tsx` — `PageHeader`의 `breakdown`("담당 A반·B반·C반 · 75명")을 뺐다. 그 값만 쓰던 `CLASS_TOTAL`·`scopeLabel` import도 같이 정리(죽은 코드 안 남김). `scopeLabel` 자체는 `DetailHeader.tsx`가 여전히 쓰므로 `mockData.ts`에서 안 지웠다.
  2. `components/DetailHeader.tsx`(MG-08 프로젝트 상세) — 뒤로가기 버튼의 "프로젝트" 텍스트 라벨을 뺐다. MG-04와 같은 아이콘 전용 형태(`ArrowLeft` size-5, `aria-label`로 목적지는 유지)로 통일. 레이아웃(세로 스택 — 버튼 한 줄, 그 아래 제목)은 안 건드렸다 — 그건 MG-04(가로 한 줄)와 애초에 다른 화면 구조라 지시 범위 밖으로 판단.
  3. 두 파일 다 최근 주요 커밋이 이 세션(사용자)이 만든 MG-07/MG-08 자체 파일이라(`git log --follow`) CLAUDE.md §7 확인 절차 없이 바로 진행했다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.
- **목적·효과:** 프로젝트 목록·상세 헤더가 MG-04와 같은 미니멀 톤으로 맞춰졌다.

**같은 세션 이어서** — 뒤로가기 버튼이 제목 줄과 **별도 줄**(위)에 있던 걸, 사용자가 "다른 화면들처럼 제목 왼쪽에 오게" 지적해 `DetailHeader.tsx`를 MG-04와 완전히 같은 구조로 바꿨다: 버튼 + `flex items-center` 한 줄 안에 제목·배지가 같이 들어간다(제목·배지끼리는 `items-baseline`으로 이전과 동일). 메타 줄(교안·검증개념·마감)은 그 아래 그대로. `/tmp` 재검증 통과.

## D54 · 목록 필터 세션 유지(D51 패턴)를 교육생(MG-05)·프로젝트(MG-07)에도 확대

- **배경:** 사용자가 면담(MG-04/03)에 적용한 "목록 필터 세션 유지"를 교육생·프로젝트 목록에도 적용해 달라고 지시(교육생·프로젝트 화면 스크린샷 첨부).
- **반영:**
  1. `projects/filterState.ts` — `getSessionFilters`/`setSessionFilters` 추가(D51과 동일한 모듈 전역 패턴). `ProjectListScreen.tsx`가 `INITIAL_FILTERS` 대신 `getSessionFilters`로 초기화하고 `useEffect`로 매 변경을 세션에 반영.
  2. `trainees/` — 이 화면은 애초에 `filterState.ts`가 없어 검색·반·계정·정렬·회차를 useState 5개로 따로 관리하고 있었다. 면담·프로젝트와 같은 구조로 맞추려고 `trainees/filterState.ts`를 새로 만들어 `FilterValues` 하나로 합치고, `TraineeListScreen.tsx`는 `filters` 하나 + `changeFilters(patch)`로 리팩터링했다.
  3. 기본 회차는 이미 `ROUND_OPTIONS[length-1]`(마지막 회차)로 동적이었다 — 그대로 `DEFAULT_ROUND`로 옮겨 이름만 붙였다.
  4. **회차를 바꿔도 다른 필터는 초기화하지 않는다** — D52(면담)와 다른 판단. 면담은 회차마다 케이스 구성 자체가 바뀌지만, 교육생 명부는 회차를 바꿔도 모집단(같은 반 전원)이 그대로고 그 회차의 도달·응시 열만 바뀐다. 검색·반·계정 필터를 지울 근거가 없어 사용자에게 묻지 않고 "유지"로 판단했다(CLAUDE.md §3 — 전문가 판단 제안, 반대 시 되돌림).
  5. 프로젝트는 애초에 회차(round) 개념이 목록 필터에 없어(각 행 자체가 회차) 이 항목은 해당 없음.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node) 통과, `prettier --check`에서 `TraineeListScreen.tsx` 포맷 어긋남 1건 발견 → `--write`로 고쳐 원본에 반영 후 재검증 전부 통과, `check-design.mjs` 통과(디자인 토큰 41개·대비 21쌍 그대로).
- **목적·효과:** 세 목록 화면(면담·교육생·프로젝트) 모두 상세→목록 왕복 시 필터가 안 사라지는 동작으로 통일. 회차 전환 시 필터 초기화 여부만 화면 성격에 따라 갈린다(면담: 초기화 / 교육생: 유지 / 프로젝트: 해당 없음).

## D55 · MG-02 히트맵 신규 구현 — mock 설계·정의서 밖 빈틈 판단 기록

- **배경:** 로드맵대로 MG-04 다음 **MG-02(히트맵)**. 정의서(`docs/plan/v2/definition/MG-02-heatmap.md`)·목업(`docs/plan/v2/wireframe/manager/heatmap.html`)이 다 있고 검증을 빌드(tsc·prettier·check-design)로 닫을 수 있어 CLAUDE.md §1 기준대로 직접 구현. 신규 파일 5개: `mockData.ts`·`HeatmapScreen.tsx`·`components/HeatmapToolbar.tsx`·`components/HeatmapTable.tsx`·`components/HeatmapLegend.tsx`(전부 이번이 첫 커밋).
- **mock 데이터 — cross-import 없이, 표시 로직만 공유:**
  1. `trainees/mockData.ts`·`projects/mockData.ts`를 import하지 않는다 — `interviews/mockData.ts` 머리말이 이미 밝힌 "이 레포에 역할 간 mock cross-import 전례가 없다"는 원칙을 그대로 따랐다. 대신 **데이터가 아니라 표시 로직**인 `trainees/lib/reach.ts`의 `REACH_STYLE`·`NA_PATTERN`(도달 단계 색·문항없음 해치)은 그대로 가져다 썼다 — 그 파일 자체가 "두 화면이 각자 베끼면 어긋난다"는 이유로 이미 공용으로 분리돼 있었다(`RoundReachGrid.tsx` 주석이 "MG-02가 아직 없어 링크를 뺐다"고 남겨 둔 자리이기도 하다).
  2. **인원 21명을 `trainees/mockData.ts`의 실제 id·이름·반을 손으로 옮겨 다시 심었다**(import 아님, 값만 복제) — 개인 행 클릭이 MG-06(교육생 상세)으로 가는데, MG-04 세션에서 겪은 것과 같은 사고(가리키는 id가 실제 목록에 없어 D-1로 막힘, D48)를 처음부터 피하려는 목적이다. 기존 12명(각 반 1팀)과 MG-04가 추가한 9명(각 반 2팀)을 반마다 7명·팀 2개(4명·3명)로 나눴다.
  3. **회차 4개**(`interviews/mockData.ts`가 미프 1~5차를 쓴 것과 같은 폭 판단) — 1차(비교 대상 없음, 배지 없음) · 2차(평이한 진행, 배지 없음 — 3차의 "단계 하락"이 성립하려면 그 직전인 2차가 상대적으로 나아야 자연스럽다) · 3차(집단 미달·다시 보기·문항 없음·미응시·위험 배지 전부 갖춘 기본값) · 4차(아직 결과 없음). 배지(단계 하락·지속 저점)는 회차별로 **사실을 직접 심어 뒀다**(연산으로 비교하지 않는다) — `interviews/mockData.ts`와 같은 방식.
  4. 평균·집단 미달(9-6, 절반 이상 2단 이하)·정렬 값은 전부 **손으로 계산해 넣지 않고 조회 시점에 함수로 계산**한다(`aggregateCell`·`buildAggRow` 등) — 손 계산은 사람이 틀리기 쉽고, `projects/mockData.ts`의 `buildProjectResult`류 계산형 mock과 같은 방향이다.
- **정의서·목업 밖에서 이번 세션이 직접 채운 빈틈 4건:**
  1. **툴바 `반별/팀/개인` 버튼으로 반을 아직 안 고른 채 팀·개인으로 바로 넘어갈 때** — OP-02 `RoundToolbar`는 팝오버를 강제로 열어 사용자가 직접 고르게 하지만, MG-02의 반 select는 항상 값이 있어야 하는 일반 `Select`라 그 패턴을 그대로 가져오면 오히려 복잡해진다. **첫 반(A반)을 기본값으로 둔다**로 판단 — 정의서가 이 갈림길을 명시하지 않았다.
  2. **정렬 "낮은 순"의 기준** — 정의서는 존재만 말하고 무엇 기준인지는 안 정했다. **그 행의 3개 개념 평균의 평균**(class/team) 또는 **그 사람의 응시한 개념 평균**(person) 오름차순으로 판단 — "문제 있는 쪽이 위로 온다"는 툴바 라벨의 뜻을 가장 직접적으로 구현하는 값이다. 미응시·결석은 정렬에서 맨 뒤로 보낸다(값이 없는 걸 "낮다"로 취급하지 않는다).
  3. **개인 뷰 "위험만" 필터의 뜻** — 오른쪽 배지 열(단계 하락·지속 저점)이 있는 사람만으로 판단(정의서가 별도 정의를 안 줬다). 반별 "⚠ 반 문제만"과 대구를 이루게, "그 행에 시스템 판정이 실제로 붙어 있는가"로 두 필터의 기준을 통일했다.
  4. **범례(색 스케일 + 개인 뷰 단계 뜻) 위치** — 정의서 본문은 "표 아래 한 줄"이라고 적었는데, 같은 문서가 원천이라 명시한 목업 HTML(`heatmap.html`)은 실제로 **표 위**(우측 정렬)에 그렸다. 케이스 계약은 목업이 원천이라는 §6 원칙대로 **목업을 따랐다** — 본문 표현은 "세로로 안 쌓고 한 줄로 압축한다"는 취지를 말한 것으로 읽었다.
- **검산:** `/tmp` 격리 복사(레포 전체, `node_modules` 심볼릭 링크 재사용) → `tsc --noEmit -p tsconfig.app.json`·`-p tsconfig.node.json` 둘 다 통과(1차 타입 오류 2건 발견해 수정 — `Array.prototype.reduce` 좁은 리터럴 타입 추론 문제, `number[]` 명시로 해결). `prettier --check` 1차 2개 파일 어긋남 → `--write` 반영 후 재검증 통과. `node scripts/check-design.mjs` 통과(토큰 41개·대비 21쌍 그대로, 새 색 없음 — 전부 기존 `bg-reach-*` 토큰 재사용). **브라우저 렌더 확인은 아직 안 함** — 다음으로 넘긴다.
- **목적·효과:** 반→팀→개인 3단 드릴다운, 집단 미달 표시, 1차 무판정, 다시 보기 반영까지 정의서 케이스 표 전량을 구현했다. `RoundReachGrid.tsx`가 남겨 둔 "MG-02로 가는 링크" TODO는 이번 범위 밖으로 남겨 뒀다 — 렌더 확인 뒤 붙일지 판단.

**같은 세션 이어서 — 스크린샷 4건 반영:**
1. **최상단 "담당 반" 평균 행에 인원수가 두 번 찍힘**(`담당 반 21명 21명`) — `label`에 이미 인원수를 넣어 두고 `countLabel`이 또 붙인 중복. `label`은 `'담당 반'`만 남겼다.
2. **프로젝트 select 목록에서 글자와 체크 표시가 겹침** — `SelectContent` 폭이 트리거 폭(`--anchor-width`)을 그대로 물려받는데 트리거를 `w-36`(144px)으로 좁게 잡아 `프로젝트 · 미프 4차` 같은 긴 라벨이 체크 아이콘 자리(`pr-8`)를 침범했다. `w-48`로 넓혔다.
3. **반·팀·개인 계층을 오갈 때 히트맵 크기·위치가 흔들림** — 두 가지가 겹친 원인이었다. ① `<table>`에 `w-full`을 줘서 `colgroup` 픽셀 값이 컨테이너 폭 기준 비율로만 반영됐다(정의서 "셀 폭 92px 고정" 위반) → `w-full` 제거, 표는 `colgroup` 합만큼만 차지. ② 개인 뷰만 배지용 4번째 열(110px)이 더 있어 반·팀 뷰보다 표가 넓었다 → 배지를 별도 열이 아니라 이름 칸 안(이름 아래)으로 옮겨 반·팀·개인 셋 다 4열(이름/팀명 + 개념 3)로 통일했다.
4. **테두리·밑줄이 언제 붙는지 질문** — 코드를 다시 짚어 확인: **테두리(주황 outline)** 는 9-6 집단 미달(그 행 자체 인원의 절반 이상이 2단 이하)일 때, **밑줄**은 그 칸 값(평균이든 개인 단계든) 자체가 2단 이하일 때 각각 독립적으로 붙는다 — 정의서 §3 "2단 이하에는 밑줄을 긋는다"·9-6 그대로이고, 실제 계산도 이 규칙과 일치함을 재확인했다(코드 변경 없음, 사용자에게 판정 근거를 설명).
- **검산:** `/tmp` 격리 재검증(레포 전체 재복사, 세션 리셋으로 이전 `/tmp` 격리본이 사라져 새로 만듦) — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 통과.

**같은 세션 이어서 — 스크린샷 3건 반영(폭 2배·가운데 정렬·테두리/밑줄 예시):**
1. **표가 카드 한쪽에 몰려 좁아 보인다** — y축 300px·개념 셀 184px(둘 다 정의서 §3 제안값의 2배, 사용자 지시)로 키우고, 표를 감싸는 `<div className="flex justify-center">`로 카드 안에서 표 블록만 가운데로 옮겼다. y축 칸 안 텍스트("A반"·"1팀" 등) 자체의 정렬은 그대로 왼쪽이다 — 사용자가 "반·팀 글자는 제외"라고 짚은 부분.
2. **테두리·밑줄 규칙을 설명 대신 예시로** — `HeatmapLegend.tsx`에 작은 예시 스와치 2개(테두리 있는 칸 = 집단 미달, 밑줄 있는 칸 = 그 칸 값이 2단 이하)를 색 범례 줄 왼쪽에 추가했다. 표 카드 밖(범례는 원래 표 위에 있다)이라 실제 셀과 안 헷갈린다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node) 통과, `prettier --check` 1차 2개 파일 어긋남(새 마크업 들여쓰기) → `--write` 반영 후 재검증 통과, `check-design.mjs` 통과.

**같은 세션 이어서 — 취약·주의 라벨 있는 행만 2줄이 돼 행 높이가 들쭉날쭉:**
사용자가 "취약/주의를 없애도 되냐"고 물어 색맹 접근성 이유로 유지를 권했고(`Badge.tsx`와 같은 원칙), 대신 "시각적으로만 맞춰 달라"는 답을 받아 반영. `AggCellView`가 라벨 없으면 둘째 줄을 아예 안 그려서(`{cell.label && <small>...}`)라벨 있는 셀만 2줄이 되고, 그 셀이 속한 행 전체가 다른 행보다 커졌다. 라벨 유무와 무관하게 둘째 줄을 항상 그리되 없을 때는 `invisible`로 숨겨 자리만 차지하게 바꿨다(`―` 문항없음 칸도 동일). 셀 높이도 `h-11`(44px)→`h-14`(56px)로 키워 두 줄이 여유 있게 들어가게 했다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — 스크린샷 반영(숫자 크기·정렬·문구·뒤로가기 5건):**
1. **셀 숫자가 작아 보인다** — 평균·개인 단계 숫자를 `text-[21px]`로 키웠다(테이블 기본 `text-sm`의 약 1.5배). 라벨(취약·주의)·↑ 표시는 원래 크기 그대로 둬 숫자만 도드라지게 했다.
2. **가운데 정렬 → 다시 왼쪽 정렬로 번복** — 지난 라운드에 넣은 `flex justify-center`를 뺐다. "왼쪽이 비어 보인다"는 지적이라, 정렬을 바꾸는 대신 y축 폭(300→220)을 줄여 표 자체를 카드에 더 붙였다(아래 4번과 같은 조치).
3. **축 표기 "세로 반" → "세로 › 반"** — `세로`/`가로` 라벨과 값 사이에 `›`를 넣어 구분을 분명히 했다.
4. **y축 폭 축소(300→220)** — "A반"·"1팀" 같은 행 이름이 히트맵 셀에서 너무 멀어 보인다는 지적. `담당 반`·그룹 안내 행도 같은 열을 쓰므로 같이 가까워지지만, 그 행들의 **문구는 바꾸지 않았다** — 사용자가 지적한 건 간격이지 텍스트가 아니라고 판단.
5. **그룹 안내 문구 교체** — 반별 "반별 · 행 클릭 → 그 반 팀"을 한 문장 "반을 클릭하여 팀 이동"으로, 개인 뷰 "팀원 · 이름 클릭 → 그 사람 이력 · ↑는 다시 보기로 오른 것"을 "팀원 · 이름 클릭 → 교육생 이력 · ↑ 재응시 후 상승"으로 바꿨다. 팀 뷰 문구는 지시받지 않아 그대로 뒀다. 타입도 `groupLabel`+`groupHint`(계층별 리터럴 유니언) → `groupText: string` 하나로 합쳐 렌더 쪽 분기를 줄였다.
6. **개인 히트맵에서 교육생 클릭 → 상세 → 뒤로가기가 명부로 가버림** — 원인은 `trainees/components/DetailHeader.tsx`(MG-06)가 뒤로가기를 `/manager/trainees`(명부) 고정 링크로 하드코딩해서였다. 이 화면은 명부·면담 목록·히트맵 등 여러 입구에서 들어오는데 고정 링크면 항상 명부로만 돌아간다 — `navigate(-1)`(브라우저 히스토리)로 바꿔 **들어온 곳으로 그대로 돌아가게** 했다. 다만 히트맵은 화면 상태(회차·계층·반·팀·정렬·필터)가 URL이 아니라 React state에만 있어서(정의서 "3장면이 같은 주소") `navigate(-1)`만으로는 주소는 돌아와도 어떤 반·팀을 보고 있었는지는 리셋된다 — `heatmap/viewState.ts`를 새로 만들어(`interviews/filterState.ts`와 같은 세션 전역 패턴) 마운트 시 마지막 상태를 복원하게 했다. `DetailHeader.tsx`는 `git log --follow` 확인 결과 이 세션(사용자, vneed154) 단독 저작이라 CLAUDE.md §7 확인 없이 바로 진행.
- **검산:** `/tmp` 격리 재검증(레포 전체 재복사) — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과(재작업 없음).

**같은 세션 이어서 — 스크린샷 반영(배지 위치·미응시 배경·문구 재조정·밑줄 제거 5건):**
1. **"지속 저점" 배지가 이름 아래 2번째 줄** — `block` → 이름과 같은 줄(`ml-1.5 align-middle`)로 옮겼다.
2. **미응시 칸이 카드 배경과 안 갈림** — `bg-surface-2`(거의 흰색)가 원인. `Badge` neutral 변형과 같은 `bg-neutral-soft`로 바꿨다.
3. **축 표기 "세로 › 반" → "세로: 반"** — 지난 라운드에 `›`로 바꿨던 걸 다시 콜론으로.
4. **"↑ 재응시 후 상승" → "↑(재응시 후 상승)"** — 화살표에 괄호로 붙이는 형태로.
5. **평균 셀 밑줄 제거** — 취약(<2.5)이면 이미 라벨이 붙어 있어 밑줄이 같은 말을 반복하는 신호였다(사용자 지적). `AggCellView`에서 밑줄 분기를 뺐고, 그 값만 쓰던 `subtleUnderline` prop도 같이 정리했다. 개인 셀(`PersonCellView`)은 라벨이 아예 없어 밑줄이 유일한 문자 신호라 그대로 뒀다 — 즉 밑줄은 이제 개인 히트맵에서만 의미를 갖는다.
- **부수 발견:** 미응시 배경색 주석에 `#fafbfc`를 그대로 적었다가 `check-design.mjs`가 "하드코딩 색"으로 오탐(이슈 번호 `#113` 오탐과 같은 근본 원인 — 3~8자리 hex처럼 보이는 문자열은 주석이라도 걸린다). 값을 말로 풀어 썼다(`docs/dev/decision-log.md` D45와 같은 회피).
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check` 1차 통과, `check-design.mjs`는 위 주석 오탐 1건 수정 후 통과.

**같은 세션 이어서 — 취약·주의 라벨·밑줄 전면 제거 + 그룹 문구 2건 최종 교체:**
1. **개인 셀(`PersonCellView`) 밑줄도 제거, 취약·주의 라벨도 제거** — 지난 라운드에서 평균 셀 밑줄만 뺀 뒤 "밑줄은 개인 히트맵에서만 의미를 갖는다"고 정리했는데, 이번 라운드는 그 마지막 신호까지 접었다. `AggCellView`의 취약·주의 `<small>` 라벨 줄(값 있을 때만 보이고 없을 때 `invisible`로 자리만 차지하던 것, D55 addendum 3)과 `PersonCellView`의 `level <= 2 && 'underline decoration-2'`를 전부 제거 — 이제 판정 신호는 **색(REACH_STYLE) + 집단 미달 테두리** 둘뿐이다. 라벨 자리를 비워 두던 스페이서 `<small>`이 없어지면서 숫자 한 줄(`text-[21px]`)만 남았고, 기존 `align-middle`로 세로 가운데 정렬이 그대로 유지된다(별도 처리 불필요).
2. **`mockData.ts`의 죽은 코드 정리** — `levelLabel(avg)` 함수와 `AggCell`의 `label?: '취약' | '주의'` 필드, `aggregateCell()`이 그 필드를 채우던 부분을 전부 삭제. 취약·주의 판정 기준(<2.5·2.5~2.7)을 둘러싼 반복 질문("취약이랑 주의를 넣는 기준이 궁금해" 2회)이 결국 그 라벨 자체를 없애는 결론으로 이어졌다 — 남겨 두면 어차피 안 쓰이는 죽은 값이라 완전히 걷어냈다.
3. **`HeatmapLegend.tsx`에서 밑줄 예시 스와치 제거** — D55 addendum(3건 반영) 라운드에서 테두리·밑줄 예시 2개를 나란히 추가했는데, 밑줄 표시 자체가 사라졌으므로 그 예시도 짝을 잃었다. 테두리(집단 미달) 예시 하나만 남겼다.
4. **팀 뷰 그룹 문구 교체** — `'팀별 · 행 클릭 → 그 팀 팀원'` → `'팀을 클릭하여 개인으로 이동'`. 반별 뷰가 이미 "반을 클릭하여 팀 이동" 한 문장 형식이라 팀 뷰도 같은 형식으로 맞췄다.
5. **개인 뷰 그룹 문구 교체** — `'팀원 · 이름 클릭 → 교육생 이력 · ↑(재응시 후 상승)'` → `'이름을 클릭하여 교육생 이력으로 이동'`. **FYI:** 이 교체로 "↑는 재응시 후 상승"이라는 설명이 이 줄에서 빠졌다. 셀의 `↑` 표시 자체(`PersonCellView`의 `retried` 마크)는 그대로 남아 있어 기능은 안 없어졌지만, 처음 보는 사람에게 `↑`의 뜻을 알려주던 유일한 문구가 사라졌다 — 지시대로 반영은 했고, 필요하면 범례(`HeatmapLegend`) 쪽에 다시 넣는 걸 다음에 검토하기로 코드 주석에 남겨 뒀다.
- **검산:** `/tmp` 격리 재검증(레포 전체 재복사) — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과(재작업 없음, 새 색 없음 — 토큰 41개·대비 21쌍 그대로).

**같은 세션 이어서 — 스크린샷 반영(계층 이름·문구·버그 질문 7건):**
1. **계층 버튼 라벨 "반별/팀/개인" → "반/팀/팀원"** — `HeatmapToolbar.tsx`. 셋 다 명사형으로 맞추고, "개인"을 그 계층이 실제로 보여주는 단위(팀 소속 개인 = 팀원)로 바꿨다.
2. **"담당 반" → "반 전체(n기)"** — `n`은 `COHORT_NAME`(이미 존재하던 `'7기'` 상수)을 그대로 꽂았다. `buildAggRow('total', `반 전체(${COHORT_NAME})`, ...)`.
3. **"반 전체 탭에서 Graph 구성에 주의 알림(⚠)이 안 뜨는 게 버그냐" — 예, 놓친 부분이었다.** 코드를 다시 짚어보니 `HeatmapTable.tsx`가 `result.level === 'class'`일 때 `flaggedConcepts`를 무조건 `[false,false,false]`로 덮어쓰고 있었다 — `ClassViewResult` 타입 자체에 그 필드가 없어서 나온 임시방편이었다. 반면 team/person 뷰는 "상위 스코프(그 반) 평균이 그 개념에서 집단 미달인가"로 헤더 ⚠를 매긴다(`classAvgRow.cells`). class 뷰의 상위 스코프는 코호트 전체이고, 그 평균이 바로 `avgRow`(반 전체 행)다 — 같은 규칙을 그대로 적용해 `avgRow.cells.map(flagged)`로 계산하도록 고쳤다. `ClassViewResult` 타입에 `flaggedConcepts: boolean[]`를 추가하고, `HeatmapTable.tsx`의 하드코딩 분기를 제거해 세 뷰 모두 `result.flaggedConcepts`를 그대로 쓰게 했다. **개별 셀 테두리(그 반 자체의 집단 미달)는 원래도 정상 동작하고 있었다** — 이번에 고친 건 열 머리 ⚠ 하나뿐이다.
4. **↑ 화살표를 크고 진하게 + "(재응시 후 상승)" 설명 추가** — 두 갈래로 나눠 반영했다. ① 셀 안의 실제 화살표(`PersonCellView`)는 `text-[11px] opacity-70` → `text-[16px]`로 키우고 `opacity-70`(흐리게 하던 것)을 없앴다. ② 설명 문구는 셀 안에 그대로 붙이면 184px 고정폭 칸이 좁아 글자가 넘친다 — 지난 라운드에 그룹 안내 문구에서 뺀 뒤 "필요하면 범례 쪽에 검토"라고 남겨 둔 방향 그대로, `HeatmapLegend.tsx`에 새 예시(`2단↑` 스와치 + "↑ = 재응시 후 상승")를 추가했다. 개인 뷰에서만 의미가 있어 `showLevelMeaning`으로 반·팀 뷰에는 안 보이게 가렸다.
5. **그룹 안내 문구에서 "으로" 제거** — 팀 뷰 "팀을 클릭하여 개인으로 이동" → "팀을 클릭하여 개인 이동", 개인 뷰 "이름을 클릭하여 교육생 이력으로 이동" → "이름을 클릭하여 교육생 이력 이동". 반별 뷰 문구("반을 클릭하여 팀 이동")는 애초에 "으로"가 없어 그대로 뒀다.
6. **"N명 응시"에서 "응시" 제거** — 팀 뷰의 반 평균 행 `countLabel`. `${classIds.length}명 응시` → `${classIds.length}명`.
7. **결석·미응시 구분 가능 여부 질문 → 구분 수단이 없어 "미응시"로 통합** — `PersonRowLine`을 다시 짚어보니 결석/미응시는 텍스트만 다르고 배경색·아이콘 등 다른 시각 신호가 전혀 없었다(둘 다 `bg-neutral-soft` 동일). 사용자 지시대로 "미응시" 한 가지로 통일했다 — `row.status`(ABSENT/NOT_STARTED) 값 자체는 데이터에 그대로 남겨 뒀으니, 나중에 실제로 구분 표시가 필요해지면 텍스트만 다시 갈라 쓰면 된다.
- **검산:** `/tmp` 격리 재검증(레포 전체 재복사) — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과(재작업 없음).

**같은 세션 이어서 — 범례 줄맞춤 버그 + "화살표가 사라졌다" 질문:**
1. **범례의 "2단↑" 예시가 줄바꿈되는 버그** — 바로 위 4번 addendum에서 넣은 예시가 스크린샷으로 확인해 보니 "2"·"단"·"↑"이 두 줄로 갈라져 나왔다. 원인은 두 가지가 겹친 것: ① 스와치 박스가 기존 `w-9`(36px) 고정 폭이었는데(테두리 예시 "2.0"에 맞춘 값), "2단↑"이 그보다 넓어 못 들어갔다. ② 화살표만 `text-[15px]`로 나머지 글자(`text-[11px]`)와 크기를 다르게 얹어, 좁은 공간 안에서 줄바꿈이 더 잘 일어나는 조건을 만들었다. **고침:** 스와치를 고정 `w-9` → `min-w-9` + 가로 패딩(`px-1.5`) + `whitespace-nowrap`으로 바꿔 내용 길이에 맞게 늘어나고 줄바꿈이 원천적으로 안 되게 했다(테두리 예시에도 같이 적용 — 폭이 이미 여유로워 부작용 없음). 화살표 크기도 `text-[15px]` 별도 지정을 없애고 스와치 전체와 같은 크기(`text-[11px] font-bold`, 상속)로 통일해 애초에 다른 줄에 걸릴 이유를 없앴다.
2. **"개인 히트맵에서 화살표가 사라진 것 같다" — 코드 자체는 안 지워졌다.** `PersonCellView`의 `{retried && <span>↑</span>}` 분기, `buildPersonRow`의 `retried: entry.priorLevels?.[idx] !== undefined` 계산 둘 다 이번 세션 전체에서 로직을 건드린 적이 없다(스타일만 두 차례 조정 — 크기·투명도). **다시 보기(재응시) 기록이 있는 사람 자체가 적어서 눈에 안 띄었을 가능성이 크다.** 3차(기본 회차) 기준 `priorLevels`가 있는 사람은 **A반 2팀 김민준**·**C반 2팀 최유나** 단 둘, 그것도 개념 1(HITL Trigger) 칸 하나뿐이다. 개인 뷰에 처음 들어가면 항상 그 반의 **1팀**이 기본 선택(`HeatmapScreen.tsx`의 "첫 팀을 기본값으로" 판단, D55)인데, A반 1팀·C반 1팀엔 재응시 기록이 아예 없어 화살표가 안 보이는 게 정상이다 — 반 select는 그대로 두고 팀 select만 "2팀"으로 바꿔 보면 화살표가 그대로 뜬다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`(`HeatmapLegend.tsx`)·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — "A반·1팀·강민준 같은 클릭 가능한 글자가 그냥 글자 같다":**
- **배경:** `AggRowLine`(반·팀 드릴 행 라벨)과 `PersonRowLine`(개인 이름)이 이미 `cursor-pointer`·`hover:text-primary`·옅은 `›`(`text-fg-subtle`)를 갖고 있었지만(D55 초기 구현부터), 전부 **호버해야만** 드러나는 신호였다. 사용자 지적대로 처음 보는 사람은 마우스를 올려보기 전엔 이게 눌리는 글자인지 알 방법이 없었다.
- **고침:** 두 곳 다 호버 여부와 무관하게 **`text-primary` + 옅은 밑줄**(`decoration-primary/40 underline-offset-2`)을 기본으로 깔고, 호버 시 밑줄만 진해지게(`hover:decoration-primary`) 바꿨다. `›`도 `text-fg-subtle` → 굵은 `text-primary`로 맞춰 같은 신호로 보이게 했다. 색만 쓰지 않고 밑줄을 같이 준 것은 색각 이상 사용자도 구분 가능하게 하려는 의도(전통적 하이퍼링크 관례) — `Badge.tsx`가 색+아이콘을 같이 쓰는 것과 같은 원칙.
- **범위:** `onClick`(반·팀 드릴)·`Link`(개인 이름)가 있는 요소에만 조건부로 붙는다 — 클릭할 수 없는 평균 행("반 전체")이나 개인 히트맵 셀 값은 그대로 `text-fg`.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — "⚠ 반 문제만" 질문에 답한 뒤, "문제"라는 말이 애매하다는 지적으로 토글 라벨 교체:**
- **배경:** "반 문제만이 뭐냐"는 질문에 "집단 미달(과반 2단 이하)인 반만 남기는 필터"라고 코드 근거(`getHeatmap`의 `problemOnly` 분기)를 들어 설명했고, 직접 확인할 사례를 물어봐 실제로 `getHeatmap`을 라운드별로 돌려 검산했다 — 3차(기본 회차)는 A반·B반·C반 전부 하나 이상 집단 미달이라 필터를 켜도 아무것도 안 사라지고, **1·2차는 C반만 집단 미달**이라(A반·B반은 세 개념 평균이 전부 2.7 이상, 필터 대상이 없음, tsx 스크립트로 실측) "미프 1차 + 반별 탭 + 이 토글"이면 C반만 남는 걸 눈으로 확인할 수 있다고 안내했다.
- **이어서 사용자가 "문제"라는 단어 자체가 애매하다고 지적** — "반 문제만" → "반: 위험", 개인(팀원) 토글 "위험만" → "팀원: 위험"으로 바꿨다. 집단 미달·위험 배지 둘 다 결국 "이 스코프에 위험 신호가 있는가"라는 같은 개념이라 "대상: 위험" 형식으로 어휘를 통일했다.
- **후속(추가 지시):** 두 토글 다 위험 아이콘(⚠)을 붙였다 — 처음엔 반 쪽에만 있었는데, 개인 토글에도 붙여 `⚠ 반: 위험` / `⚠ 팀원: 위험`으로 아이콘·어휘가 대칭이 되게 했다.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — 콜론 간격 통일("라벨: 값" → "라벨 : 값"):**
- **배경:** 표 안 축 라벨("세로: 반"·"가로: 이 회차의…")과 방금 바꾼 위험 토글("⚠ 반: 위험"·"⚠ 팀원: 위험")이 전부 콜론 앞은 붙이고 뒤만 한 칸 띄우는 형태였는데, 사용자가 넷 다 콜론 앞뒤로 **한 칸씩** 띄우라고 지시.
- **고침:** `HeatmapTable.tsx`의 `<i>세로</i>: {axis.vertical}` → `<i>세로</i> : {axis.vertical}`(가로도 동일), `HeatmapToolbar.tsx`의 `⚠ 반: 위험` → `⚠ 반 : 위험`(팀원도 동일). 네 곳 다 같은 간격 규칙이라는 걸 주석에 서로 참조하게 남겨 뒀다 — 다음에 또 하나만 고치고 나머지를 놓치는 일이 없게.
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — 위험 토글이 0행이 되면 조용히 비어 보이는 문제 → 빈 결과 안내 문구:**
- **배경:** 두 가지 지적을 받았다. ① "반: 위험을 눌러도 위험 안 한 반도 나오는 것 같다(위험한 반이 없으면 변화가 없다)" ② "팀원: 위험을 누르면 사라지긴 하는데 아무 문구가 없는 게 어색하다". **필터 로직 자체는 버그가 아니었다** — `getHeatmap`을 실제로 돌려 검산했다: 3차(기본 회차)는 A반·B반·C반 **셋 다** 이미 집단 미달이라 토글을 켜도 셋 다 그대로 남는 게 정상이고(①이 "변화가 없다"고 느낀 원인), A반 1팀(팀원 뷰 기본값)은 3차에 위험 배지(단계 하락·지속 저점)를 가진 사람이 **0명**이라 "팀원: 위험"을 켜면 실제로 행이 0개가 되는 게 맞다(②의 "사라진다"가 정상 동작). 문제는 **0행이 됐을 때 표가 그냥 비어 보이기만 하고 아무 설명이 없어**, 필터가 고장났는지 정상 동작인지 구분이 안 됐던 것.
- **고침:** `HeatmapTable.tsx`에 `EmptyFilterRow` 컴포넌트를 추가 — `result.rows.length === 0`이면 행 목록 대신 4열 병합 안내 행("위험에 해당하는 반이 없습니다." / "위험에 해당하는 팀원이 없습니다.")을 그린다. class·person 뷰에만 적용했다(team 뷰는 위험 필터 자체가 없어 0행이 될 일이 없다).
- **검산:** `tsx`로 `getHeatmap` 직접 호출해 실측 — `classFilter:'A반', teamFilter:'A-1', riskOnly:true`(3차) → `rows.length === 0` 확인(②가 재현되는 정확한 조건). `problemOnly`는 1·2차 `['C반']`, 3차 `['A반','B반','C반']`로 반 전체 재확인(①의 "변화 없음"이 3차에서만 일어나는 정상 동작임을 재확인). `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

**같은 세션 이어서 — 팀 계층에도 위험 토글 추가 + 빈 결과 문구 가운데 정렬을 히트맵 기준으로:**
1. **"팀에도 위험 필터가 있으면 좋지 않을까?" → 그대로 추가.** 팀 행도 반 행과 완전히 같은 `cell.flagged`(집단 미달) 구조를 갖고 있어서, `mockData.ts`의 `problemOnly` 분기를 team 레벨에도 그대로 복제해 넣었다(반 분기와 순서까지 동일 — 필터 후 정렬). 상태(`problemOnly`)는 새로 안 만들고 기존 걸 그대로 공유한다 — class·team 두 탭은 서로 배타적으로만 보여서 하나의 상태를 같이 써도 안 꼬인다. `HeatmapToolbar.tsx`의 토글 노출 조건을 `level === 'class'`에서 `level === 'class' || level === 'team'`으로 넓히고, 라벨을 `⚠ {반|팀} : 위험`처럼 계층에 따라 바꿔 달게 했다. 실측(`tsx`로 9개 팀 × 1~3차 `problemOnly:true` 전부 조회) — 1차 A반·B반은 `[]`(팀이 하나도 안 남음, 새로 만든 빈 안내 문구가 여기서 뜬다), 2차 A반은 `['2팀']`만 남는 등 필터가 실제로 걸러내는 걸 확인했다.
2. **팀 레벨도 0행이 되면 "위험에 해당하는 팀이 없습니다." 안내가 뜨게** — `HeatmapTable.tsx`의 3단 분기(person/class/team)를 전부 `rows.length > 0` 체크로 통일했다(이전엔 team 분기만 빈 상태 처리가 없었다).
3. **빈 안내 문구를 히트맵(색칠된 3칸)과 같은 중심선에 오게** — 스크린샷으로 "히트맵에 중간선을 그었을 때 같은 선상에 있으면 좋겠다"는 지적을 받았다. 원래 `EmptyFilterRow`가 y축 칸까지 포함한 4칸을 통째로 `colSpan`해 `text-center`를 줬는데, y축 열(220px)만큼 왼쪽으로 치우쳐 보였다. y축 자리는 빈 `<td aria-hidden="true" />`로 남겨 `colgroup` 폭만 차지하게 하고, 개념 3칸만 `colSpan={3}`으로 묶어 그 안에서 가운데 정렬하도록 바꿔 메시지 중심이 히트맵 3칸의 중심과 정확히 겹치게 했다.
4. **"위험" 판정 기준 도움말 버튼 추가** — 반·팀 위험(집단 통계: 개념 하나라도 인원 절반 이상이 2단 이하)과 팀원 위험(개인 배지: 단계 하락·지속 저점, 직전 회차 비교가 필요해 1·2차엔 항상 0명)의 기준이 서로 다른데 이름만으로는 안 드러난다는 점("위험 팀원이 왜 항상 0명이냐"는 질문이 실제로 나왔다) — `CircleHelp` 아이콘 + "위험 기준이 뭐예요?" 트리거 버튼을 툴바에 계층 무관하게(항상 보이게) 추가하고, 클릭하면 `Popover`로 위 설명을 쉬운 말로 풀어 보여준다. 컴포넌트는 새로 안 만들고 `src/components/ui/Popover.tsx`(Base UI 위에 이미 있던 것, `UiPreviewScreen.tsx`의 "SC-M10 코치마크" 용례를 그대로 재사용)를 썼다(CLAUDE.md §7 "공용 컴포넌트를 최우선으로"). 트리거 스타일은 일부러 위험 토글(⚠, 앰버 필)과 다르게(중립색·다른 아이콘) 줬다 — 같은 모양이면 이 버튼도 필터처럼 보여 헷갈린다.
- **검산:** `/tmp` 격리 재검증(레포 전체 재복사) — `tsc --noEmit`(app·node) 통과, `prettier --check` 1차 어긋남(새 Popover 설명 문단 줄바꿈) → `--write` 반영 후 실제 워크스페이스 파일에도 같은 줄바꿈으로 반영, 재검증 통과. `check-design.mjs` 통과(새 색 없음).

**같은 세션 이어서 — 도움말 팝오버 문구 다듬기 + 트리거 오른쪽 정렬:**
- **문구 3건:** `"단계 하락"·"지속 저점"`을 굵게(다른 강조 문구들과 같은 `<b className="font-semibold text-fg">`), "배지가 붙어 있는 사람만 걸러요" → "…해당돼요"(더 정중한 어투로), "3차부터 확인할 수 있어요" → "최소 3차부터 확인할 수 있어요"(4차도 있을 수 있다는 걸 "최소"로 명시).
- **트리거 라벨 축약 + 오른쪽 정렬:** "위험 기준이 뭐예요?" → "위험 기준?", `ml-auto`로 툴바 오른쪽 끝에 붙였다. **§6 "툴바는 전부 좌측, 우측 비움"과 결이 다르다는 걸 알고 반영했다** — 이건 필터가 아니라 도움말이라, 좌측 필터 무리에 나란히 쌓이는 것보다 줄 끝에 따로 떨어뜨려 "이건 다른 종류의 버튼"이라는 걸 위치로도 드러내는 쪽이 사용자 지시와도 맞고 더 명확하다고 판단했다.
- **부수 조정:** 트리거가 오른쪽 끝으로 옮겨가면서 `PopoverContent`의 `align`도 `start` → `end`로 바꿨다 — `start`로 두면 팝오버가 트리거 왼쪽 끝에서부터 오른쪽으로 펼쳐지려다 화면 밖으로 나갈 수 있어서, 트리거 오른쪽 끝 기준으로 왼쪽으로 펼쳐지게 했다(지시받지 않았지만, 위치를 옮긴 데 따른 당연한 후속 조정이라 바로 반영).
- **검산:** `/tmp` 격리 재검증 — `tsc --noEmit`(app·node) 통과, `prettier --check` 1차 어긋남(중첩 `<b>` 줄바꿈) → `--write` 반영 후 실제 파일에도 반영, 재검증 통과. `check-design.mjs` 통과.

**같은 세션 이어서 — 기본 프로젝트를 항상 최신으로 + 팀원 위험 기준에 0·1단 추가:**
1. **새로고침·최초 진입 시 프로젝트 필터가 항상 "가장 최근에 만들어진 프로젝트"** — `viewState.ts`의 `INITIAL_VIEW.round`를 하드코딩 `'3'`에서 `ROUND_OPTIONS[ROUND_OPTIONS.length - 1].value`로 바꿨다. `ROUND_OPTIONS`가 생성 순서대로 정렬돼 있다는 전제 위에 마지막 항목을 최신으로 본다. 새로고침도 이 모듈이 다시 로드되며 `sessionView`가 `null`로 리셋돼 최초 진입과 같은 경로(`INITIAL_VIEW`)를 타므로 한 곳만 고치면 둘 다 해결된다. **부수 효과:** 지금 데이터로 최신 회차는 4차(PENDING, 결과 없음)라 새로고침·최초 진입 시 곧바로 "이 회차는 아직 결과가 없어요" 빈 상태가 뜬다 — 지시대로 "항상 최신"을 우선했고, 결과 있는 회차로 되돌려 보여주지 않았다(실제로 가장 최근 프로젝트가 아직 안 끝났다는 뜻이라 오히려 정확한 신호).
2. **팀원 위험 기준에 0단·1단 추가** — 원래 `riskOnly`는 배지(단계 하락·지속 저점) 유무만 봤는데, "0단·1단이 하나라도 있으면 위험에 추가해 달라"는 지시로 `mockData.ts`의 필터를 `!!r.badge || (r.cells?.some(c => c.level !== null && c.level <= 1) ?? false)`로 넓혔다. 미응시·결석(`cells === null`)은 판정할 값이 없어 그대로 제외된다.
3. **도움말 팝오버 문구도 새 기준에 맞춰 갱신** — "0단·1단을 받았으면 해당돼요"를 추가하고, 더는 사실이 아니게 된 "1·2차엔 위험 팀원이 안 나온다" 문장을 걷어냈다 — 0·1단 기준은 그 회차 점수만 보므로 회차와 상관없이 바로 적용된다(배지 기준만 여전히 직전 회차 비교가 필요해 최소 3차부터).
- **검산:** `tsx`로 직접 실측 — `INITIAL_VIEW.round === '4'` 확인. `riskOnly:true`를 1~3차 전체 반×팀에 돌려, 이전엔 항상 `[]`였던 1·2차에서도 이제 위험 팀원이 나오는 걸 확인(예: 1차 C반 1팀 `오세훈·신아린`, 1차 C반 2팀 `최유나·오세림` — 전부 0·1단 점수가 있어 새로 걸림, 배지는 없음). `/tmp` 격리 재검증 — `tsc --noEmit`(app·node) 통과, `prettier --check`는 한글·영문 혼용 줄에서 두 번 왕복해도 줄바꿈 지점이 흔들리는 걸 발견해(known prettier 폭 계산 이슈로 추정) `--write`를 안정될 때까지 반복 실행한 뒤(동일 결과 재확인) 그 결과로 실제 파일을 맞춰 최종 통과시켰다. `check-design.mjs` 통과.

**같은 세션 이어서 — `npm run lint` 실행 결과 반영: `REACH_STYLE`·`NA_PATTERN`을 공용 위치로 승격:**
- **배경:** 사용자가 `npm run lint`(oxlint)를 실행해 에러 2건을 보고했다. `.oxlintrc.json`의 `no-restricted-imports` 규칙(`src/features/**`는 `@/features/{다른 도메인}/**`를 못 가져온다, D15)에 `HeatmapTable.tsx`·`HeatmapLegend.tsx`의 `@/features/manager/trainees/lib/reach` import 2건이 걸렸다. 이번 세션 내내 `tsc`·`prettier`·`check-design.mjs`만 돌리고 **lint는 한 번도 실행하지 않았다** — 샌드박스의 oxlint 네이티브 바인딩이 깨져 있어(`Cannot find module '@oxlint/binding-linux-x64-gnu'`, CLAUDE.md가 미리 경고한 known issue) 직접 재현은 못 했고, 정적으로 `.oxlintrc.json`을 읽어 원인을 특정했다.
- **decision-log D55의 판단이 틀렸다.** D55는 "표시 로직 공유는 cross-import 금지 대상이 아니다"라고 적었는데, 실제 lint 규칙에는 그런 예외가 없다 — 규칙 메시지 자체가 "공통은 components/common으로 올린다"고 명시한다. 코드를 다시 뒤져보니 이미 같은 문제를 겪은 전례가 있었다: `projects/components/PersonResultPanel.tsx`가 `REACH_STYLE`을 trainees에서 바로 안 가져오고 **로컬에 복제**해 두면서 정확히 이 이유(oxlint 교차 import 금지)와 decision-log D14("두 번은 기록, 세 번째에 올린다" — trainees 1번째, projects 2번째라 아직 안 올림)를 주석에 남겨 뒀다. heatmap이 정확히 **세 번째 도메인**이라, D14가 예고해 둔 승격 조건이 이번에 실제로 발동한 셈이다.
- **고침:** `src/components/common/reach.ts`를 새로 만들어 `REACH_STYLE`·`NA_PATTERN`(도메인 타입에 의존하지 않는 순수 상수 부분)만 승격했다. `roundBadgeKind`·`riskBadgeKind`(`RoundRecord` 타입에 의존)는 도메인 지식이라 `trainees/lib/reach.ts`에 그대로 남기고, 그 파일은 이제 `REACH_STYLE`·`NA_PATTERN`을 공용 위치에서 재수출만 한다(같은 도메인 내 상대경로 참조자 3곳 — `RoundReachGrid.tsx`·`Timeline.tsx`·`TraineeListScreen.tsx` — 는 변경 없이 그대로 동작). `HeatmapTable.tsx`·`HeatmapLegend.tsx`의 import를 `@/components/common/reach`로 바꿨고, D14가 예고한 대로 `PersonResultPanel.tsx`의 복제본도 지우고 같은 곳에서 가져오게 정리했다. 관련 파일 머리말 주석을 전부 이 결정과 근거를 가리키게 갱신했다(`mockData.ts`의 예전 "cross-import 금지 대상이 아니다" 주장도 정정).
- **검산:** `src/**`에서 `@/features/{도메인}/{도메인}/` 패턴의 남은 교차 import를 repo 전체 grep으로 재확인 — `src/mocks/cohortRounds.ts` 1건만 남았는데 이 파일은 `src/features/**` 밖이라 이 규칙 적용 대상이 아니다(다른 사전 존재 사안, 이번 범위 밖). `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`(영향받은 6개 파일)·`check-design.mjs` 전부 1차 통과. oxlint 자체는 샌드박스에서 여전히 실행 불가라 사용자 쪽에서 재실행 확인이 필요하다.

**같은 세션 이어서 — 팀 계층에도 위험 필터 추가 + 빈 결과 문구 가운데 정렬 버그:**
- **배경:** 두 가지 요청. ① "팀에도 위험 : 팀 필터가 있으면 좋지 않을까?"(제안형 질문) — 팀 행도 반 행과 똑같이 `cell.flagged`(집단 미달)를 갖고 있어 반별과 같은 필터를 그대로 재사용할 수 있다고 판단, 되묻지 않고 바로 추가했다. ② 방금 넣은 "위험에 해당하는 팀원이 없습니다." 문구가 스크린샷으로 보니 히트맵(색칠된 3칸) 중심보다 왼쪽에 치우쳐 있었다 — "히트맵에 중간선을 그었을 때 같은 선상에 있으면 좋겠다"는 지적.
- **① 팀 필터:** `mockData.ts`의 `HeatmapQuery.problemOnly`를 "반별에서만"에서 "반별·팀에서"로 넓히고, `getHeatmap`의 `team` 분기에도 반별과 같은 필터(`rows.filter(r => r.cells.some(c => c.kind==='value' && c.flagged))`)를 추가했다. `HeatmapToolbar.tsx`는 새 상태를 안 만들고 **기존 `problemOnly` 하나를 반·팀이 공유**하게 했다 — 두 탭은 서로 배타적으로만 보여서 상태를 같이 써도 꼬이지 않는다. 토글 라벨도 계층에 맞게 동적으로 바뀐다: `⚠ 반 : 위험`(반별) / `⚠ 팀 : 위험`(팀).
- **② 가운데 정렬 버그:** `EmptyFilterRow`가 처음엔 4칸(y축 220px + 개념 3칸)을 통째로 `colSpan`해서 `text-center`를 줬는데, y축 열까지 정렬 기준에 포함되면서 중심이 왼쪽으로 밀렸다. y축 칸은 빈 `<td>`로 남겨 `colgroup` 폭만 차지하게 하고, 나머지 개념 3칸만 `colSpan={3}`으로 묶어 그 안에서만 가운데 정렬하도록 고쳤다 — 이제 문구 중심이 히트맵 3칸의 중심과 정확히 겹친다. 팀 계층용 빈 메시지("위험에 해당하는 팀이 없습니다.")도 같은 컴포넌트로 추가했다.
- **검산:** `tsx`로 `getHeatmap` 팀 분기를 반 3개 × 회차 3개 전수 실행 — 1차 A반·B반은 `[]`(빈 결과, `EmptyFilterRow`가 뜨는 조건 재현), 2차 A반은 `['2팀']`(부분 필터링도 재현), 나머지도 예상대로 동작 확인. `/tmp` 격리 재검증 — `tsc --noEmit`(app·node)·`prettier --check`·`check-design.mjs` 전부 1차 통과.

## D56 · 용어 체계 재정의 + 알림 기능 범위 축소 — **프론트엔드 전체 적용**

> **범위 주의 — 이 결정은 매니저 화면에 한정되지 않는다.** 교육생(trainee)·운영(operator)을 포함한 프론트엔드 전체에 적용한다(사용자 명시).
>
> **아직 코드에 반영되지 않았다.** 이 항목은 결정 기록이고, 구현은 사용자 착수 지시 이후다. 아래 D 영역 일부는 **팀 논의 대기로 의도적으로 보류**한다.

- **배경:** MG-01 대시보드 구현 중 `[독촉]` 버튼 문구를 `[알림]`으로 바꾸는 작은 요청에서 출발했는데, 확인 과정에서 **용어 체계 전반이 갈라져 있다는 것**과 **백엔드 미구현·일정 촉박으로 알림 발송 기능 자체를 재검토해야 한다는 것**이 드러나 결정 범위가 프론트 전체로 커졌다. 사용자가 20년차 기획자 관점의 단일 권고를 요청했고(CLAUDE.md §3 — 제안하지 폴링하지 않는다), 항목별로 채택/보류를 확정했다.

### A. `재응시` vs `다시 보기` — 두 단어는 동의어가 아니라 서로 다른 두 기능이다 **[채택]**

- **발견:** 팀에서 "다시 보기 vs 재응시 중 뭐가 맞나"로 의견이 갈렸는데, 두 단어는 **한 기능의 후보 이름이 아니라 이미 서로 다른 두 기능의 이름**이다. `docs/plan/v2/14-verification-design.md` §11 표가 명시한다.

  | 상태 | 재시험 | 개입 |
  |---|---|---|
  | 무효 응시(0단) | **재응시** — 1차로 취급 | 면담 |
  | 1단·2단 | **재시험** | 개입 / 위험 판정에 따라 |

  `TR-03-session.md` §6+는 한 문장으로 못 박아 뒀다 — *"무효 응시(0단)는 다시 보기가 아니라 재응시다(11절). 1차로 취급하므로 1차 화면을 그대로 쓴다."*

- **단어가 셋(재시험·재응시·다시 보기)이지만 기능은 둘이다.** `재시험`은 §11이 규칙을 서술할 때 쓰는 **스펙 용어**, `다시 보기`는 같은 것을 화면에서 부르는 **UI 용어**다. 즉 `재시험 = 다시 보기`이고, `재응시`만 다른 기능이다.
- **점수 반영은 "어느 시점의 점수냐"로 갈린다**(사용자 지적으로 정정 — 첫 설명에서 대상을 안 밝혀 뜻이 뒤집혀 전달됐다):

  | | 원래 응시 결과 | 다시 본 결과 |
  |---|---|---|
  | 무효 응시 | **반영 안 됨** — 채점하지 않고 판정·평균에서 뺀다(9-4) | 1차로 취급해 **반영**(§11) |
  | 2단 이하 | **반영됨** — 정상 채점, 위험 판정·평균에 들어감 | **반영 안 됨** — `조직 단위 수치는 1차만`(§11) |

- **판단:** ① 2단 이하 재시험은 **`다시 보기`**로 되돌린다. ② **`재응시`는 무효 응시(0단) 전용**으로 남긴다. ③ 직전 세션에 매니저 쪽을 전부 `재응시`로 일괄 변경했던 것을 되돌린다(교육생 상세 타임라인·히트맵·면담 브리프 ④조치·면담 목록 빈 상태·대시보드).
- **근거:**
  1. **재응시는 이 기능을 잘못 약속하는 말이다.** §11이 *"재시험은 재측정이 아니라 '리포트를 읽고 이해했는가'를 확인하는 것이 된다. 그래서 어떤 집계에도 쓰지 않는다"*고 성격 자체를 규정했다. 같은 문항·힌트 없음·막힌 개념만·개념당 1회·판정 미반영이다. "재응시"라고 부르면 학생은 확정된 점수를 뒤집을 두 번째 기회로 읽고, 다 보고 나서 도달 단계가 그대로인 걸 확인하는 순간 속았다고 느낀다.
  2. **무효 응시 쪽은 반대로 `재응시`가 정확하다.** 원 점수가 아예 없어서 1차를 새로 만드는 것이라 실제로 "응시"를 다시 한다.
  3. **두 기능에 같은 이름을 주면 매니저가 화면에서 구분할 수 없다.** 실제로 지금 MG-03의 `[무효로 처리하고 재응시 안내]`(1차 취급·점수 남음)와 MG-08의 `재응시 대상`(2단 이하·점수 안 남음)이 같은 단어인데 정반대 뜻이다.
  4. **교육생 화면은 이미 전부 `다시 보기`다**(TR-01 홈 `RETRY_AVAILABLE` · TR-03 세션 · TR-04 리포트 — `trainee/home/types.ts` 주석이 *"목업·TR-03·TR-04 전부 '다시 보기'로 통일돼 있다"*고 남겨 뒀다). 지금은 매니저가 "재응시"라 부르는 걸 학생은 "다시 보기"로 보고 있어 **면담에서 둘이 다른 말을 쓴다.** A 적용은 매니저를 학생 쪽에 맞추는 것이라 이 어긋남이 함께 해소된다.
- **기각한 대안:** `다시 보기`의 약점(VOD 느낌, 조회로 읽혀 대화형 세션이라는 무게가 안 실림)은 실재하고 팀이 갈린 이유이기도 하다. 다만 대안인 `재시험`도 "시험=채점"을 암시해 판정 미반영과 어긋나고 학생에게 톤이 딱딱하다. 두 약점을 비교해 `다시 보기` 유지로 판단.

### B. `응시 전` vs `미응시` — 마감 전/후를 다른 말로 가른다 **[채택]**

- **배경:** 마감 전 아직 안 본 사람과, 마감이 지나도록 끝내 안 본 사람이 지금 같은 말(`미응시`)을 쓰고 있다.
- **판단:** 마감 **전** = **`응시 전`**(잔여 기한 병기) / 마감 **후** = **`미응시`**(확정).
- **근거:**
  1. **두 상태의 차이는 시간이 아니라 "아직 바뀔 수 있는가"다.** 앞은 연락하면 뒤집히는 진행 상태고, 뒤는 더 손쓸 게 없는 확정된 결과다. 그래서 앞은 기회가 남았음을 말하는 말로, 뒤는 결과를 말하는 말로 갈라야 한다.
  2. **`미응시`를 마감 후에 몰아주는 이유는 그게 이미 집계에 쓰이는 말이기 때문이다.** MG-08 결과 탭의 "미응시 N명"은 다시 보기 대상을 세는 확정 숫자인데, 마감 전 사람까지 섞이면 숫자가 계속 흔들린다. 반대로 마감 전 사람은 세는 대상이 아니라 **처리할 대상**이라 카운트가 아니라 액션이 붙는다.
  3. **라벨만으로 못 가르는 부분은 옆 값이 가른다** — 마감 전엔 항상 잔여 기한을 병기하고(`응시 전 · 3시간 남음`), 마감 후엔 붙일 값 자체가 없어 자연히 갈린다.
- **같이 정리할 것 — `AttendanceStatus`의 기존 드리프트:** `projects/mockData.ts`에서 ① `NOT_STARTED` 라벨은 "미응시"인데 타입 주석은 *"마감이 지나도록 응시를 시작조차 안 한"*(마감 후)이라 해놓고 mock 데이터는 `deadlineLabel: 'D-2'`(마감 전)인 사람에게 이 값을 준다 — 한 값이 두 뜻으로 쓰인다. ② `AVAILABLE`(응시 가능)과 `NOT_STARTED`가 실질적으로 같은 상태라 무엇으로 갈리는지가 코드에 없다. ③ 대시보드는 `ABSENT`를 "미응시 · 3시간 남음"(마감 전)으로 쓰는데 MG-08 결과 탭의 "미응시"는 마감 후다 — **같은 단어가 두 화면에서 반대 뜻**이다.
- **정리 후 상태값:** `응시 완료` / `응시 전`(창 열림, 기한 병기) / `미응시`(창 닫힘, 확정) + `BLOCKED`(응시 창 자체가 안 열림, 현재 "—") 넷.

### C. 교육생에게 나가는 알림 채널을 자른다 — 워크플로와 상태 변경은 남긴다 **[채택]**

- **배경:** 백엔드가 전부 구현되지 않았고 API 연동 전이며 일정이 촉박하다. 사용자가 "응시 전 알림·미응시 재응시 발송·무효 응시 재응시 발송을 없애고 매니저 통보·면담 선에서 끝낼지" 검토 중이라고 밝혔다.
- **판단 — 세 기능을 한 덩어리로 보지 않는다. 성격도 비용도 다르다.**

  | 기능 | 성격 | 결정 |
  |---|---|---|
  | 응시 전 알림 | 순수 메시지 — 보내도 시스템 상태가 안 바뀐다 | **자른다** |
  | 다시 보기 열기 | **상태 변경** — 그 학생에게 응시 창을 여는 행위 | **남긴다** |
  | 무효 응시 처리 | **사람 판정 필수**(9-4) | **남긴다**(발송만 뺀다) |

- **절단면: 알림 "채널"만 자르고 "상태"는 남긴다.**
  1. **비용 구조상 이게 정확한 절단이다.** 채널(이메일/푸시 연동·템플릿·발송 실패·재시도·수신 확인)이 백엔드 비용의 대부분이고, 상태 변경은 DB 필드 하나(`retryDueAt`)다. 시간이 촉박할수록 비싼 쪽을 자르고 싼 쪽을 남긴다.
  2. **알림을 자를 수 있는 결정적 근거 — 정보 전달 경로가 이미 따로 있다.** 학생 홈(`RETRY_AVAILABLE`)과 리포트가 "너는 다시 보기 대상"임을 **이미 화면에 띄운다.** 알림은 학생을 앱으로 데려오는 수단일 뿐이고, 부트캠프는 학생이 매일 접속하는 환경이며 매니저가 오프라인으로 직접 말할 수 있다 — 푸시의 한계효용이 낮다.
  3. **상태를 없애면 안 되는 이유 — 이미 만든 교육생 화면 3개가 이 상태를 읽는다**(TR-01 홈 다시 보기 카드 · TR-03 RETRY 세션 · TR-04 리포트). 없애면 학생이 다시 보기에 진입할 경로가 사라져서 **기능 하나 자르기가 아니라 만들어 둔 것 버리기**가 된다.
- **화면 반영:** 버튼을 없애지 않고 **`[알림]` → `[연락함]`**으로 바꾼다. 매니저가 슬랙 등으로 직접 연락한 뒤 누르면 그 행이 처리됨으로 내려간다. 백엔드는 boolean 하나. **버튼을 아예 빼면 대시보드가 읽기만 하는 목록이 되어 MG-01 정의서 §2 *"행에서 바로 처리한다"*가 무너진다.**
- **무효 응시:** `[무효로 처리하고 재응시 안내]` → **`[무효로 처리]`** — 안내(발송)만 뺀다. 판정 자체는 9-4가 *"못한 것과 안 한 것은 다르고, 그 차이는 화면이 알 수 없다"*고 못 박아 사람이 해야 한다.
- **잃는 것 · 되돌리는 조건:** 발송 기록이 없어 "안내받은 적 없다"는 분쟁에 시스템 근거를 못 댄다(`[연락함]` 체크는 매니저 자기 신고라 증빙력이 약하다). **백엔드 여유가 생기거나 실제 분쟁이 발생하면 알림 채널을 얹는다** — 상태는 이미 있으므로 발송만 추가하면 된다.

### D. MG-08 결과 탭 — **팀 논의 대기로 보류**, 단 A·B·C가 강제하는 변경은 반영

- **보류(팀 결정 전까지 현행 유지):**
  1. **다시 보기 창 자동 열기** — §11이 대상(2단 이하 전원)·기한(3일 또는 다음 제출일 중 빠른 쪽)·시점(리포트와 동시)을 전부 정해놔 계산으로 나오지만, 아래 기한 재량과 한 덩어리라 같이 보류.
  2. **발송 모달(`RetrySendDialog`) 제거** — 위와 연쇄. 기한을 사람이 정하는 이상 받을 UI가 필요하다.
  3. **기한 수동 지정 제거** — **사용자가 매니저 재량 유지를 검토 중**이라 보류. ⚠ §11의 "다음 제출일로 자르는 이유"(겹치면 학생이 다음 과제를 우선 → 재시험 미응시 → 그 미응시가 다시 위험 신호로 잡히는 **"시스템이 만든 위험"**)와 충돌한다는 것을 **알고 남긴다.** 팀 결정이 나면 이 항목만 뒤집는다.
  4. **대상 체크박스 제거 — 기각(현행 유지).** 처음에 §11 *"재시험과 개입은 별개로 병행한다. 도달 단계가 낮다고 재시험 기회를 빼지 않는다"*를 근거로 제거를 권했으나, **사용자 지적으로 인용이 과했음을 인정하고 철회했다.** 그 문장은 "재시험"과 "개입(면담)"의 **관계**를 규정한 것이지(면담 대상이 됐다고 재시험 기회까지 뺏지 말라) 매니저의 개별 대상 조정을 금지하는 조항이 아니다. 실무적으로도 이미 면담에서 직접 짚어준 학생·개인 사정 등 예외가 있고, "규칙이 뽑아준 기본 대상 + 되돌릴 수 있는 예외"는 MG-03 `[제외]` 버튼과 같은 구조다.
  5. **발송 취소(`cancelRetry`) 재검토** — 4번과 묶여 보류(기능 유지, 이름만 아래 6번에서 조정).
- **A·B·C가 강제하는 변경(보류와 무관하게 반영):**
  1. **`재응시` → `다시 보기`** — MG-08 결과 탭이 다루는 건 2단 이하 대상이라 A-① 적용 대상이다. `재응시 대상` 카드·`RetrySendDialog`·`retryStatus` 라벨 3종이 전부 바뀐다. 남기기로 한 것도 정확히는 **"다시 보기 기한 설정"**이다.
  2. **`발송` → `활성화`** — C로 메시지 채널이 없어지므로 "보낸다"는 말이 성립하지 않는다. 매니저가 하는 일은 **그 학생에게 다시 보기 세션의 응시 권한과 기한을 부여하는 것**이다(실제로 `sendRetry`가 저장하는 것도 메시지가 아니라 `retrySentAt`·`retryDueAt` 둘뿐이고, 학생 화면은 그 값을 읽어 세션 진입을 허용한다 — 메시지는 원래부터 부수적이었고 본체가 권한 부여였다). 버튼·모달 제목·`retrySentAt` 필드 의미("보낸 시각" → "활성화한 시각")까지.
  3. **`retryStatus` 3분류 재정의 + B 적용** — 현재 `재응시 가능 / 재응시 발송 완료 / 재응시 완료`인데 "발송"이 없어지는 데다, 다시 보기 창에도 기한이 있으므로 B-마감 전/후 구분이 그대로 걸린다. 지금은 "열렸지만 아직 안 봄"이 마감 전인지 후인지 구분이 안 된다(`retryDueAt`이 있어 계산은 가능). → `활성화 전 / 응시 전(기한 병기) / 미응시(기한 지남) / 완료`.
  4. **`cancelRetry` 이름** — "발송 취소"가 성립하지 않으므로 **`비활성화`**로 재정의(기능 자체는 보류로 유지).

- **`활성화 / 비활성화` 채택 경위 — 계정 어휘 충돌 우려를 사용자가 기각:**
  - 처음 후보는 `열기`였는데, 이 레포에 이미 `[브리프 열기]`(매니저 자신이 여는 화면)가 있어 **같은 동사가 두 뜻**이 되는 문제가 있었다. 그래서 `열어주기`(수혜자가 학생임을 어미로 표시)를 권했다.
  - **사용자가 `활성화`를 제안했고, 이쪽이 낫다.** 취소 동작까지 짝이 맞기 때문이다 — `열어주기`는 대응하는 반대말이 어색해 `cancelRetry` 자리를 "창 닫기"로 어정쩡하게 남겨야 했는데, `활성화 ↔ 비활성화`는 그 자리를 그대로 메운다.
  - **내가 제기한 반대 근거는 `활성/비활성`이 이미 계정 상태 어휘라는 것이었다** — MG-05 명부 헤더(`활성 23 · 초대 대기 1 · 비활성 1`), `auth/inviteApi.ts`의 계정 활성화 API, `Badge.tsx`의 `success=활성`/`neutral=비활성` 색 정의. 같은 학생을 두고 "비활성"이 두 뜻이 되는 것을 우려했다.
  - **사용자가 이 우려를 기각했고 그 논리가 맞다 — *"비활성화 계정은 다시 보기를 애초에 못 한다."*** 두 상태는 경쟁하는 두 의미가 아니라 **선후 관계**다. 계정이 비활성이면 다시 보기 대상이 될 수조차 없으므로 한 사람에게 두 "비활성"이 동시에 성립하는 상황 자체가 없다. 충돌이라 본 것이 실은 겹치지 않는 영역이었다.
  - **부수 효과:** 이 관계가 명시되면 나중에 "비활성 계정인 학생이 다시 보기 대상에 잡히면 어떻게 하나"라는 케이스도 답이 이미 정해져 있다 — **대상에서 제외**다(권한 부여 자체가 성립하지 않는다). 구현 시 이 전제를 코드에도 반영한다.
  5. **같은 화면의 `NudgeDialog`**(제출 현황 탭 `[알림 보내기]`) — C-알림 절단 대상이라 제거하고 `[연락함]` 체크로 전환.

### D-20. 다시 보기 기한에 **상한(다음 프로젝트 제출일)이 구현에 없다** — 보류 항목과 별개

- **발견:** §11은 기한을 `3일 또는 다음 프로젝트 제출일 중 빠른 쪽`으로 정했는데, `RetrySendDialog.tsx` 실측 결과 **"빠른 쪽" 규칙의 절반이 아예 없다.**
  - `defaultDeadline()` → `TODAY + 3일, 18:00`만 계산하고 다음 제출일과 비교하지 않는다
  - 달력 `disabled` → `[{ before: TODAY }]` — 하한(과거 차단)만 있고 **상한이 없다**
- **판단:** 이건 **D 보류 항목과 성격이 다르다.** 12~15번은 "매니저에게 재량을 줄 것인가"라는 팀 논의 사항이지만, 이건 재량을 주든 안 주든 규칙의 절반이 빠져 있는 상태라 어느 쪽으로 결정되든 채워야 한다.
- **오히려 이걸 채우면 매니저 재량이 안전해진다** — 하한(오늘)·상한(다음 제출일)을 달력이 막아주면 재량을 줘도 §11이 경고한 사고가 안 난다. 팀 논의 때 절충안으로 쓸 수 있는 형태다. 데이터는 이미 있다(`PROJECTS` 각 회차의 `dueAt` — mif-3의 다시 보기면 mif-4의 `2026-07-23T18:00`).
- **부수 — 기산점 차이:** §11 다이어그램은 기산점을 **리포트 발행**으로 그렸는데 코드는 **`TODAY`(발송일)** 기준으로 +3일을 잡는다. 다만 이건 학생에게 3일을 온전히 주는 쪽이라 나쁘다고 단정하기 어렵다 — **상한만 있으면 실질 문제는 안 생긴다.** 기록만 하고 넘긴다.

### ⚠ 이 논의에서 내가 틀렸던 것 2건 — 재발 방지용으로 남긴다

1. **"점수 반영" 표에서 대상을 안 밝혀 뜻이 정반대로 전달됐다.** `무효 응시 = 반영 O / 2단 이하 = 반영 X`라고만 적었는데, 앞은 **재응시 결과**, 뒤는 **다시 본 결과** 얘기였다. 사용자는 **원래 응시 결과**를 말하고 있었고 그 기준으론 정확히 반대다. 둘 다 맞는 얘기였는데 **무엇의 점수인지를 안 적어서** 충돌처럼 보였다. → 표에 값만 쓰지 말고 **측정 대상을 열 이름에 명시할 것.**
2. **§11의 `통보 | 리포트와 동시`를 근거로 "창이 자동으로 열려야 한다"고 주장했다.** 사용자 지적대로 §11 표에서 `통보`와 `응시 창`은 **별개 행**이고, "통보"는 학생에게 리포트가 전달되는 것이지 창을 여는 행위가 아니다. 두 행을 임의로 붙인 잘못된 추론이라 철회했다. 같은 우려는 **다른 행(응시 창의 상한 누락, 위 D-20)**에서 훨씬 구체적인 형태로 성립한다. → **스펙 표의 행을 근거로 쓸 때 다른 행과 묶지 말 것.**

- **목적·효과:** ① 한 기능에 두 이름, 두 기능에 한 이름이던 용어 혼선을 정리하고 매니저·교육생이 같은 말을 쓰게 한다. ② 마감 전/후를 다른 말로 갈라 집계 숫자가 흔들리지 않게 한다. ③ 백엔드 비용이 가장 큰 알림 채널을 걷어내되 이미 만든 교육생 화면 3개가 물려 있는 상태 변경은 지켜, 일정 압박 아래서 잃는 것을 최소화한다.

### D56 후속(2026-08-08) · MG-01 대시보드 — 항목별 "해소" 신호를 확정한다: `면담`은 실제 종결, 나머지는 자기 신고(체크)

- **배경:** D56 C절 구현 뒤 사용자가 대시보드 `[체크]`(옛 `[연락함]`) 버튼 자체의 필요성을 재검토하자고 제기했다. 논의 과정에서 밴드별로 "무엇이 이 항목을 할 일 목록에서 없애는가"가 서로 다르다는 게 드러났다 — 하나로 뭉뚱그려 두면 나중에 API를 연동할 때 잘못된 자리(전부 자기 신고로)에 붙이기 쉽다.
- **판단 — 항목 종류에 따라 "해소" 메커니즘이 둘로 갈린다:**
  1. **`INTERVIEW`(면담 대기) — 실제 종결이 유일한 해소 신호다.** `[브리프 열기]`는 다른 화면(`interviews/*`)으로 이동만 할 뿐 그 자체로 아무것도 끝내지 않는다 — 그 화면에서 면담을 실제로 마치고 **종결 처리**해야 비로소 끝난다. API 연동 후에는 그 종결 이벤트가 대시보드 `ITEMS`에서 해당 항목을 지우고, `RESOLVED_HISTORY`에 `{kind:'INTERVIEW', doneFlag:'종결함 · 방금', nextAction}` 형태로 넣는다 — 이 스키마는 이미 목업 시드(`r-parkdohyun`)에 존재해서 새로 설계할 게 없다, 지금 정적으로 박아 둔 값을 실제 종결 이벤트가 채우도록 배선만 하면 된다.
  2. **`ABSENT`·`RETRY`·`UNSUBMITTED`·`ANALYSIS_FAILED` — 실제 신호(응시·제출)가 있지만, `[체크]`는 그와 별개인 자기 신고다.** 이 넷은 화면을 안 옮기고 이 화면 안에서 끝나므로(파일 머리말 "미응시·다시 보기·미제출·분석 실패는 다른 화면으로 안 옮긴다") 종결을 알려줄 별도 화면이 없다 — 대신 응시·제출이 실제로 일어나면(다른 시스템 이벤트) 그 자체가 해소 신호가 되어 항목이 사라져야 한다. `[체크]`는 그 실제 해소와 무관하게, "내가 이미 손을 썼다"는 매니저 개인 기록일 뿐이다(SubmissionTab 쪽은 이 자기 신고 기능 자체를 이미 제거함 — 위 세션 논의, 좁은 명단이라 옆 열만 봐도 충분하다는 판단). 대시보드는 여러 날짜에 걸친 여러 유형의 백로그를 다루므로 이 자기 신고를 남긴다.
- **근거:** 두 메커니즘을 하나로 착각하면(예: `[체크]`를 누르면 항목이 사라진다고 잘못 구현하면) 실제로 응시·제출·면담종결이 없었는데도 목록에서 빠져버려 매니저가 진짜 미해결 건을 놓친다 — "내가 확인했다"와 "문제가 실제로 풀렸다"는 다른 사실이고, 화면은 그 둘을 구분해서 보여줘야 한다.
- **목적·효과:** API 연동 시점에 배선을 어디에 걸어야 하는지(면담=종결 이벤트 구독, 나머지=응시/제출 이벤트 구독) 미리 못 박아 둔다 — 지금 목업은 두 메커니즘 다 아직 안 걸려 있다(면담은 원래 화면 전환만, 나머지 셋은 `[체크]`가 상태만 바꾸고 실제 응시·제출을 시뮬레이션하는 트리거가 mock에 없다).
