import PlaceholderScreen from '@/app/PlaceholderScreen'

/** AU-02 회원가입 · 계정 활성화 — 토큰이 변형(가입/활성화)을 결정한다 — 화면 이식 전 자리표시. 실제 화면이 들어오면 이 파일 내용만 바뀐다. */
export default function InviteScreen() {
  return <PlaceholderScreen code="AU-02" title="회원가입 · 계정 활성화" />
}
