# SC-M06 · 개별 교육생 상세

> 소스: 기능명세서 v8 DASH-16(개별 상세) + ENG-02(회차 변화·추이) + ENG-03(기여 편차·귀속) + RPT-03(온디맨드 면담 체크리스트) + INTV-08(상담·메모) + AUTH-11(이력 권한).
> 배치: P0 · 매니저 조회 종착. **매니저 전용** — 교육생 리포트(T05)와 정반대로 점수·세션 전문·근거가 보인다.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 역할 | **매니저 전용**(DASH-16) |
| 진입 경로 | SC-M11(리스트)·SC-M04(위험 목록)에서 행 클릭 |
| 연계 FR | **DASH-16**, ENG-02(추이)·ENG-03(귀속)·ENG-01(근거)·RPT-03(면담)·INTV-08(메모)·AUTH-11 |
| 이번 배치 범위 | 회차별 5축 추이, 세션 질답 전문, 채점 근거, 기여 귀속, 개입·상담 이력, 온디맨드 면담 체크리스트 |
| 보이는 것(교육생과 반대) | **점수·5축·세션 전문·채점 근거·시그널** — 매니저 전용(A5의 반대편) |
| 규범 | 기수 평균=회차 스냅샷(B1) · 1회차 단독 판정 금지(B4) · 기여량 단독 낙인 금지(B5) · 면담은 온디맨드(RPT-03) |

---

## 2. 목적 & UX 설계 근거

- **한 사람을 통째로 이해하는 화면.** **종합·세션·개입 3탭**(근거·귀속은 별도 탭이 아니라 종합·세션에 흡수·D21). 매니저가 근거를 눌러 확인하며 판단.
- **매니저 전용 = 근거가 보인다.** 교육생에겐 숨기는 점수·세션 전문·채점 근거를 여기선 노출(DASH-16·DASH-09). 단 **교육생 직접 접근은 차단**.
- **추이는 스냅샷 의미론(B1).** 기수 평균 대비는 회차별 스냅샷 기준. 절대 점수·직전 대비·최초 대비를 분리 표시(ENG-02). **1회차 단독은 판정 금지**(표본 부족).
- **귀속은 낙인 금지(B5).** 기여 편차를 단독으로 프리라이더라 부르지 않는다. 기여×이해도×지속성 교차, **서술형 표현**("기여·이해도 불균형").
- **면담 준비는 온디맨드(RPT-03).** 자동 생성 금지 — 개입 탭 [＋ 면담·관찰 기록 추가] 시 요약·체크리스트가 프리필됨(**별도 '면담 질문 추출' 버튼 폐기·D34/D43**). 코드 재질문 아님.
- **상담 메모는 이력 보존(INTV-08).** 작성자·시각 보존, 수정 시 이력 추가.

---

## 3. 정보 구조 & 레이아웃 (매니저 앱 셸 · 데스크톱) — 3탭 재설계(D21)

**헤더:** 아바타·이름·기수·반·팀·상태·응시. (면담 버튼 없음 — 면담 준비는 개입 탭 기록 추가에 흡수·D34/D43.)
**탭 3개(종합·세션·개입):**
1. **종합(합성):** ①**헤드라인 신호 한 줄**(예: "반례대응 2회 연속 하락 → 개입 검토") + [개입 생성] ②**종합점수 추이 라인**(회차별 총점·기수 평균 대비, D27) ③**레이더 5축**(본인 채움 + 기수 평균 윤곽 겹침) ④**축별 행**(현재 점수·Δ·스파크라인·기수 대비 — 정확값·추이 보완) ⑤**기여·이해도 불균형 신호 카드**(신호 있을 때만, 아래 참조)
2. **세션(근거 흡수):** 회차 선택 → **DP별 접힘 카드**(주제·근거코드 위치·도달 depth 핀·판정 색). **SURFACE 지점 경고색으로 강조** → "어디서 막혔나" 즉시 스캔. 펼치면 질답 전문 + **근거 구간**(ENG-01, 별도 탭 아님 — 점수·턴에 붙음)
3. **개입(행동·기록 · D43 재구성):** **개입별 활동 카드 스트림.** 개입 하나 = 카드, 그 안에 **근거 시그널 · 활동 기록(면담/멘토링 후기/관찰) · 상태(진행/완료 전이) · 효과 판정**이 한 덩어리. + 하단 **일반 메모**(개입 무관 관찰, INTV-08 비연결). 상담 기록을 개입 안으로 넣어 "어느 개입의 활동인지" 명확. **"면담 준비" 별도 버튼 폐기** → [＋ 면담·관찰 기록 추가] 시 면담이면 AI 요약·체크리스트 프리필(RPT-03 흡수).

**귀속 = 독립 탭 없음(D21).** 코드량은 품질·적극성의 지표가 아니므로(B5·ENG-03/04) 라인·파일 표를 전면에 두지 않는다. 기여 데이터는 **종합 탭의 기여·이해도 불균형 신호 카드**에서만 — 기여편차 AND 이해도저조 AND 지속성 **세 조건 동시 충족 시에만**, "낮은 기여량 ≠ 낮은 실력" 명시, 원시 숫자는 "참고용" **접기(disclosure)**.
**개입 데이터 흐름 (D43·D44 — 한 실체, 두 화면):** `Intervention`은 **학생(targetId)으로 묶인 하나의 서버 레코드.** 생성(시그널 경로)·상태 전이(진행/완료)·활동 기록 추가는 **M06(이 학생 맥락)·M07(전체 관리) 양쪽 어디서나** 같은 레코드에 반영. M07 = 담당 전체 개입 관리(테이블·정렬·방치·export), M06 = 이 학생 개입의 활동·기록. **효과 판정은 완료 후 다음 회차에만**(진행·대기 중엔 없음).

**면담 준비(RPT-03) = 면담 기록 추가 시 프리필:** 별도 버튼 아님. [＋ 면담·관찰 기록 추가]에서 면담 유형이면 입력창에 ①상황 한 줄 요약(코드 용어 없이) ②확인 체크리스트(학습 페이스·도움요청·계기·지속의지, AI가 못 보는 것·코드 재시험 아님)가 프리필. 매니저가 코드를 몰라도 지원 대화 가능. 질문 대본 생성 아님.

**세션 질문 수(예산 N):** M06은 소비만. **N은 회차별 설정(기본 4)** — SC-M03 소관(D28). 상대 위치 표기는 **점수 대비 기수 평균(같은 5점 단위·차이)**, σ·순위 배제(D29). 반응형: 웹 기준.

---

## 4. 데이터 & 컴포넌트

| 영역 | 표시 데이터 | 출처 FR | UI 컴포넌트 |
|---|---|---|---|
| 헤더 | 이름·기수·반·팀·상태 | DASH-16 | `DetailHeader` |
| 추이 | 회차별 5축·총점·기수 평균 대비 | ENG-02, B1 | `ScoreTrendLine`(D27) + `AxisRadar`(5축) + `BenchmarkDelta` |
| 세션 | 질문·답변 전문 | DASH-16 | `TranscriptView`(회차 선택) |
| 근거 | 원본 응답·근거 구간·버전 | ENG-01, DASH-09 | `EvidencePanel` |
| 귀속 | 기여 맵·편차(서술형) | MEAS-02A, ENG-03 | `ContribView` + `NarrativeSignal` |
| 개입·활동 | 개입별 카드(근거·활동 기록·상태·효과) + 일반 메모 | INTV-04~08 | `InterventionCard` + `ActivityLog` + `StateBtn` + `GeneralMemo` |
| 면담 | 질문+근거 체크리스트 | RPT-03 | `MeetingChecklist`(온디맨드) |

> 화면 밖(서버): Score(원본·버전·비공개)·ScoreDelta·ContribDeviation·Session 로그·Intervention·Memo(작성자·시각)·면담 체크리스트(온디맨드 생성)·AuditLog. **교육생 직접 접근 차단.**

---

## 5. 화면 액션 → 데이터/API 요구사항

| 액션 | 트리거 | 오퍼레이션 | 요청 데이터 | 응답 · 부수효과 | 관련 FR | 검증 · 권한 규칙 |
|---|---|---|---|---|---|---|
| 상세 로드 | 진입 | `GET /trainees/{id}/detail` | id | 추이·세션·근거·귀속·개입(매니저 범위) | DASH-16 | 담당/권한·**교육생 접근 차단** |
| 세션 전문 조회 | 세션 탭·회차 선택 | `GET /trainees/{id}/sessions/{r}` | id·회차 | 질답 전문 | DASH-16 | 매니저 전용 |
| 근거 조회 | 근거 탭 | `GET /scores/{id}/evidence` | scoreId | 원본 응답·근거 구간·버전 | ENG-01, DASH-09 | 매니저 전용·루브릭 원문 비노출(교육생) |
| 면담 체크리스트 프리필 | 개입 탭 [＋ 면담·관찰 기록 추가](면담 유형) | `POST /trainees/{id}/meeting-checklist` | id | 요약+근거 체크리스트 프리필(**온디맨드만**, 별도 버튼 없음) | RPT-03 | 세션 로그 필요·자동 생성 차단·근거 필수 |
| 활동 기록 추가 | 개입 카드 [＋ 기록 추가] / 일반 [＋ 메모] | `POST /interventions/{id}/activities` 또는 `POST /trainees/{id}/memos`(비연결) | `{ type(면담/관찰/메모), content }` | 기록 저장(작성자·시각)·개입에 연결 또는 일반 메모 · M07 상세 공유 | INTV-08 | 빈 메모·길이·권한·원작성 보존 |
| 개입 상태 전이 | 개입 카드 [진행으로]/[완료로] | `PATCH /interventions/{id}/status` | `{ status }` | 상태 변경 + 이력 · **M07·대시보드 반영**(같은 레코드) | INTV-05 | 대기→진행→완료만·역전 차단·권한 |

---

## 6. 상태별 UI (예외 case — 게이트 A)

**DASH-16 7 + RPT-03 7 + INTV-08 7 + ENG-02/03 표시 5 = 26.**

### 상세 공통 (DASH-16)
| # | 상태 | 표현 / 다음 행동 |
|---|---|---|
| D-1 | 조회 권한 없는 교육생 | `danger` 상세 차단 |
| D-2 | 일부 회차 결측 | 누락 구간 표시·나머지 추이 유지 |
| D-3 | 세션 전문 부재 | 세션 탭 비활성·데이터 없음 |
| D-4 | 채점 근거 부재 | 근거 없음·ENG-01 점검 |
| D-5 | 귀속 미수행 | 종합 탭 기여 신호 미표시·"분석 대기" |
| D-6 | 개입 이력 없음 | 이력 없음 |
| D-7 | 삭제·비활성 교육생 | 상태 배지·수정 제한 |

### 추이·귀속 (ENG-02/03)
| # | 상태 | 표현 / 다음 행동 |
|---|---|---|
| T-1 | 1회차 단독(표본 부족) | 변화 미산출·표본 부족 표시(B4) |
| T-2 | 특정 회차 점수 결측 | 보간 없이 해당 쌍 제외 |
| G-1 | 커밋 데이터 부족 | 기여 편차 판정 유보 |
| G-2 | git history 부재 | 기여 산출 중단/대체 |
| G-3 | 기여량 단독 프리라이더 요청 | `danger` 확정 차단·서술형 변환(B5) |

### 면담 체크리스트 (RPT-03)
| # | 상태 | 표현 / 다음 행동 |
|---|---|---|
| R-1 | 세션 로그 없음 | 버튼 비활성 |
| R-2 | 자동 생성 트리거 요청 | 차단(온디맨드만) |
| R-3 | 근거 없는 질문 | 제거 |
| R-4 | 기존 코드 질문 중복 | 제거·비코드 재구성 |
| R-5 | 유효 취약 이슈 없음 | 체크리스트 없음 |
| R-6 | 질문 생성 실패 | 재생성 |
| R-7 | 권한 없는 사용자 | 차단 |

### 상담 메모 (INTV-08)
| # | 상태 | 표현 / 다음 행동 |
|---|---|---|
| M-1 | 존재 않는 교육생 | 저장 차단 |
| M-2 | 작성 권한 없음 | 차단 |
| M-3 | 빈 메모 | 저장 차단 |
| M-4 | 최대 길이 초과 | 저장 차단 |
| M-5 | 연결 대상 부재 | 비연결 메모로 저장 |
| M-6 | 수정 시 원작성 보존 | 최초 작성자·시각 보존·이력 추가 |
| M-7 | 저장 실패 | 재시도 |

---

## 7. 인터랙션 & 화면 전이

- 진입: SC-M11·SC-M04에서.
- 개입 탭 [＋ 면담·관찰 기록 추가](면담 유형) → 온디맨드 요약·체크리스트 프리필.
- 개입 탭 → 개입 생성(SC-M07) 연계.
- 근거 → GOV-01 이의제기 검토(SC-M09)와 연결(원본·버전).

---

## 8. 컴포넌트 매핑

**신규:** `DetailHeader`·`ScoreTrendLine`(D27)·`AxisRadar`·`BenchmarkDelta`·`TranscriptView`·`EvidencePanel`·`ContribView`·`NarrativeSignal`·`InterventionCard`·`ActivityLog`·`StateBtn`·`GeneralMemo`·`MeetingChecklist`(기록 추가 프리필).
재사용: `ManagerShell`·`Tabs`·`StatusBadge`.
> D43 재구성 반영: 옛 `InterventionTimeline`·`MemoEditor`·`MeetingBtn`는 폐기 → 개입별 활동 카드(`InterventionCard`+`ActivityLog`+`StateBtn`) + 일반 메모(`GeneralMemo`)로 대체.

---

## 9. UX 준수 규칙

| 규칙 | 구현 | 근거 |
|---|---|---|
| 매니저 전용 | 점수·세션 전문·근거 노출, 교육생 접근 차단 | DASH-16, DASH-09 |
| 기수 평균 스냅샷(B1) | 회차별 스냅샷 기준 벤치마크 | ENG-02, DASH-04 |
| 1회차 단독 판정 금지(B4) | 표본 부족 표시 | ENG-02 |
| 기여량 단독 낙인 금지(B5) | 교차 검증·서술형 표현 | ENG-03, DASH-11 |
| 근거 없는 표시 금지(A2) | 점수마다 근거 구간 연결 | ENG-01, DASH-10 |
| 면담 온디맨드 | 자동 생성 금지·기록 추가(면담) 시에만 프리필 | RPT-03 |
| 메모 이력 보존 | 작성자·시각·수정 이력 | INTV-08 |

---

## 10. 와이어프레임

`docs/screen-def-v8/wireframes/manager/trainee-detail.html` — 헤더 + 3탭(종합/세션/개입) + 개입별 활동 카드 + 일반 메모 + 상태 매핑.

---

## 커버리지 자기검증 (건수)

- ① 예외 case: DASH-16 7 + RPT-03 7 + INTV-08 7 + ENG-02/03 5 = **26 → 상태 행 26** ✅
- ② 처리로직 → API: detail·session·evidence·meeting-checklist·memo 매핑 ✅
- ③ 입력 필드(메모·면담 트리거) → 화면 존재 ✅
- ④ 출력 부수효과: 면담 체크리스트 온디맨드 생성·메모 이력·조회 AuditLog → API 표 ✅
- ⑤ 비즈니스 규칙(매니저 전용·스냅샷·1회차 금지·낙인 금지·온디맨드·이력) → §9 + API 검증 ✅
- ⑥ 연계 FR(DASH-16·ENG-01/02/03·RPT-03·INTV-08 → M07·M09) → §7 전이 ✅

---

## 관련 결정 로그
> 단일 원천: [01-decision-log.md](01-decision-log.md)

- D21 3탭 재설계 · D25 세션 이해 상태 · D27 종합점수 추이 라인 · D29 상대 위치 표기 · D30/D32 RPT-03=코칭 브리핑(질문 생성 폐기) · D31 개입 엔티티 통합 · D34 면담=상담 메모 시작(헤더 버튼 폐기) · D42 메모=human layer 타임라인 · D43 개입 탭=활동 타임라인 · D44 개입 한 실체(M06·M07 양쪽) · D45 개입 상세 패널 구성
